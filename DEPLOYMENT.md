# Деплой на Beta сервер

## 🚀 Как это работает теперь

**Всё просто:** коммитите в `main` → GitHub Actions автоматически деплоит на beta.optioner.online

```bash
git add -A
git commit -m "Описание изменений"
git push origin main
```

Готово! Деплой запустится автоматически.

## 📊 Процесс деплоя

```
Вы пушите в main
    ↓
GitHub Actions запускается автоматически
    ↓
Собирает frontend (npm run build)
    ↓
Копирует build на beta сервер
    ↓
Обновляет код на сервере (git pull)
    ↓
Перезагружает nginx и PM2
    ↓
beta.optioner.online обновлён ✅
```

## ✅ Проверка статуса деплоя

1. Перейдите на https://github.com/transatlanticsyndicate-code/modular-code-methodology
2. Нажмите на вкладку "Actions"
3. Найдите последний workflow "Deploy to Beta Server"
4. Посмотрите статус (зелёная галочка = успех, красный крест = ошибка)

## 🔍 Если деплой не сработал

### Проверить логи
1. Actions → последний workflow
2. Нажмите на job "deploy"
3. Посмотрите логи в разделе "Deploy to Beta Server"

### Частые проблемы

**Проблема:** "Secrets not found"
- **Решение:** Добавьте secrets в GitHub (см. `.github/SETUP_SECRETS.md`)

**Проблема:** "SSH connection refused"
- **Решение:** Проверьте, что IP сервера правильный (89.117.52.143)

**Проблема:** "npm build failed"
- **Решение:** Проверьте, что frontend собирается локально (`npm run build`)

## 📝 Структура репозитория

```
modular-code-methodology/
├── frontend/
│   ├── src/
│   ├── build/              ← Создаётся при npm run build
│   └── package.json
├── backend/
├── .github/
│   ├── workflows/
│   │   └── deploy-beta.yml ← GitHub Actions workflow
│   └── SETUP_SECRETS.md    ← Инструкция по secrets
├── docs/
│   └── DEPLOYMENT_GUIDE.md
└── REPOSITORIES_MAP.md
```

## 🔐 Безопасность

- Пароли хранятся в GitHub Secrets (не видны в логах)
- SSH пароль не коммитится в репозиторий
- Деплой работает только при пуше в main branch

## 📚 Подробнее

- `docs/DEPLOYMENT_GUIDE.md` — полное руководство
- `REPOSITORIES_MAP.md` — архитектура системы
- `.github/SETUP_SECRETS.md` — настройка secrets

## 🎯 Что изменилось

| Было | Стало |
|-----|-------|
| Два репозитория (origin + beta) | Один репозиторий (origin) |
| Ручной деплой через скрипт | Автоматический деплой через GitHub Actions |
| Нужно помнить про два пуша | Один пуш = автоматический деплой |
| Путаница с папками | Ясная структура |

## ⚡ Быстрый старт

1. Убедитесь, что secrets добавлены в GitHub (`.github/SETUP_SECRETS.md`)
2. Сделайте коммит и пуш:
   ```bash
   git add -A
   git commit -m "Ваше описание"
   git push origin main
   ```
3. Проверьте Actions вкладку на GitHub
4. Готово! Деплой произойдёт автоматически

## 🚨 Важно

- **Никогда** не пушьте в beta remote (его больше нет)
- Пушьте только в `origin main`
- GitHub Actions сделает всё остальное автоматически
