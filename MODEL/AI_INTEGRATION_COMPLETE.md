# Интеграция AI Волатильности в Калькулятор Опционов

## 📋 Обзор Задачи

**Цель:** Интегрировать AI прогнозирование волатильности во все компоненты фронтенд-калькулятора опционов для обеспечения согласованных расчетов P&L при включении AI режима.

**Дата выполнения:** Декабрь 2025
**Статус:** ✅ Завершено

## 🏗️ Архитектурные Решения

### Общая Архитектура

1. **Backend API**
   - ONNX модель для прогнозирования волатильности (`options_model.onnx`)
   - REST API эндпоинт `/ai/predict-volatility` в `ai_prediction.py`
   - Сервис `AIPredictionService` для обработки запросов

2. **Frontend Интеграция**
   - Глобальное состояние AI (`isAIEnabled`, `aiVolatilityMap`) в `OptionsCalculatorBasic.jsx`
   - Кэширование прогнозов по ключу `${ticker}_${strike}_${date}_${targetPrice}_${daysToExpiration}`
   - Автоматический запрос AI данных при изменении параметров

3. **Компонентная Структура**
   - **OptionsTable** - таблица опционов с P&L расчетами
   - **PLChart** - основной график P&L
   - **OptionsMetrics** - метрики стратегии
   - **ExitCalculator** - калькулятор выхода из позиции (2 сценария + график)
   - **ExitTimeDecayChart** - график временного распада

## 🔧 Детали Реализации

### 1. Backend Компоненты

#### Новые Файлы:
- `backend/app/routers/ai_prediction.py` - API эндпоинт для прогнозов
- `backend/app/services/ai_prediction_service.py` - сервис AI предсказаний
- `backend/app/ai_models/options_model.onnx` - ONNX модель волатильности
- `MODEL/options_model.onnx` - оригинальная модель

#### API Интерфейс:
```python
POST /ai/predict-volatility
{
  "ticker": "AAPL",
  "strike": 290,
  "expiration_date": "2026-03-20",
  "target_price": 273.4,
  "days_to_expiration": 68
}
```

### 2. Frontend Компоненты

#### Глобальное Состояние (`OptionsCalculatorBasic.jsx`)
- `isAIEnabled` - флаг включения AI режима
- `aiVolatilityMap` - кэш прогнозов волатильности
- `fetchAIVolatility` - функция запроса AI данных
- Автоматический запрос при изменении `targetPrice`, `selectedTicker`, `daysPassed`

#### Ключевые Функции:
- **Кэширование:** `${ticker}_${strike}_${date}_${targetPrice}_${daysToExpiration}`
- **Формат данных:** Волатильность в десятичном формате (0.188 = 18.8%)
- **Перерисовка:** Автоматическое обновление компонентов при изменении AI состояния

## 📊 Компонентные Изменения

### OptionsTable.jsx
**Изменения:**
- Добавлен проп `selectedTicker` для корректного ключа кэша
- Обновлена логика поиска AI волатильности в `calculateOptionPLValue`
- Добавлено детальное логирование использования AI

**Ключевой код:**
```javascript
const cacheKey = `${selectedTicker}_${option.strike}_${option.date}_${targetPrice.toFixed(2)}_${daysRemaining}`;
const aiVolatility = aiVolatilityMap[cacheKey];
if (aiVolatility) {
  console.log('🤖 [OptionsTable] Используем AI волатильность:', aiVolatility);
  overrideVolatility = aiVolatility;
}
```

### PLChart.jsx
**Изменения:**
- Добавлен проп `selectedTicker`
- Обновлена функция `calculatePLDataForMetrics` для AI поддержки
- Исправлена логика ключа кэша (использование `targetPrice` вместо `currentPrice`)
- Добавлена зависимость `isAIEnabled` и `aiVolatilityMap` в `useMemo`

**Проблема решена:** График не перерисовывался при включении AI из-за отсутствия AI зависимостей.

### OptionsMetrics.jsx
**Изменения:**
- Добавлен проп `selectedTicker`
- Передача `selectedTicker` в `calculatePLMetrics`
- Обновлены зависимости `useMemo` для корректной перерисовки

### ExitCalculator & usePositionExitCalculator
**Изменения:**
- Добавлены AI пропсы: `isAIEnabled`, `aiVolatilityMap`, `selectedTicker`
- Обновлены функции `calculateCloseOptionsScenario` и `calculateCloseAllScenario`
- **Исправлена проблема:** AI волатильность отображалась как `0.2%` вместо `18.8%`

**Критическое исправление:**
```javascript
// ВАЖНО: AI волатильность приходит в десятичном формате (0.188), нужно умножить на 100
const ivDisplayPercent = optionVolatility < 1 ? optionVolatility * 100 : optionVolatility;
const ivDisplay = ivDisplayPercent.toFixed(1);
```

### ExitTimeDecayChart.jsx
**Изменения:**
- Добавлены AI пропсы и поддержка в `calculateOptionPL`
- Использование `JSON.stringify(aiVolatilityMap)` для зависимостей `useMemo`
- Добавлено логирование для отладки

**Проблема решена:** График не реагировал на AI переключатель из-за объектных ссылок в зависимостях.

## 🐛 Проблемы и Решения

### 1. Несоответствие Ключей Кэша
**Проблема:** AI прогнозы кэшировались с `targetPrice = 273.4`, но компоненты искали с `(targetPrice || currentPrice) = 291.7`.

**Решение:** Унификация использования `targetPrice` во всех компонентах для построения ключей кэша.

### 2. Неправильное Отображение IV
**Проблема:** AI волатильность (0.188) отображалась как `IV: 0.2%` вместо `IV: 18.8%`.

**Решение:** Конвертация из десятичного формата в проценты перед отображением:
```javascript
const ivDisplayPercent = optionVolatility < 1 ? optionVolatility * 100 : optionVolatility;
```

### 3. Отсутствие Перерисовки Компонентов
**Проблема:** Компоненты не реагировали на изменение `aiVolatilityMap` из-за объектных ссылок в React зависимостях.

**Решение:**
- Добавление AI пропсов в зависимости `useMemo`/`useCallback`
- Использование `JSON.stringify(aiVolatilityMap)` для глубокого сравнения
- Принудительное обновление объекта в эффекте AI переключения

### 4. Дублирование Файлов Компонентов
**Проблема:** Существовали `ExitTimeDecayChart.jsx` и `ExitTimeDecayChart/index.jsx`, но использовался старый файл.

**Решение:** Обновлена AI поддержка в основном используемом файле `ExitTimeDecayChart.jsx`.

## 📁 Измененные Файлы

### Backend (новые файлы):
- `backend/app/routers/ai_prediction.py`
- `backend/app/services/ai_prediction_service.py`
- `backend/app/ai_models/options_model.onnx`
- `MODEL/options_model.onnx`

### Frontend (конфигурация):
- `frontend/src/config/aiConfig.js`
- `frontend/src/services/aiPredictionService.js`

### Основные компоненты:
- `frontend/src/pages/OptionsCalculatorBasic.jsx` - глобальное состояние AI
- `frontend/src/components/CalculatorV2/OptionsTable.jsx` - таблица опционов
- `frontend/src/components/CalculatorV2/PLChart.jsx` - основной график
- `frontend/src/components/CalculatorV2/OptionsMetrics.jsx` - метрики
- `frontend/src/components/CalculatorV2/ExitCalculator/index.jsx` - калькулятор выхода
- `frontend/src/components/CalculatorV2/ExitTimeDecayChart.jsx` - график распада
- `frontend/src/hooks/usePositionExitCalculator.js` - хук расчетов выхода

### Утилиты:
- `frontend/src/utils/optionPricing.js` - расчеты опционов
- `frontend/src/utils/metricsCalculator.js` - расчеты метрик

## 🧪 Тестирование и Отладка

### Логирование
Добавлено детальное логирование для отладки:
- `🤖 [ComponentName] Используем AI волатильность:` - успешное использование AI
- `🤖 [ComponentName] Перерисовка графика:` - проверка реактивности
- `🤖 [AI] isAIEnabled изменен на:` - изменения состояния AI

### Тестовые Сценарии
1. **Включение AI режима** - все компоненты должны перерисоваться
2. **Изменение Target Price** - автоматический запрос новых AI прогнозов
3. **Проверка кэша** - повторные запросы не должны вызывать API
4. **Отображение IV** - корректный формат процентов в описаниях

### Результирующая Функциональность
✅ OptionsTable реагирует на AI переключатель
✅ PLChart использует AI волатильность в расчетах
✅ OptionsMetrics отображают корректные метрики с AI
✅ ExitCalculator показывает правильные P&L с AI (IV в процентах)
✅ ExitTimeDecayChart перерисовывается при включении AI

## 🎯 Результат

Полностью интегрирована система AI прогнозирования волатильности во все компоненты калькулятора опционов. Пользователи могут переключать AI режим и видеть согласованные расчеты P&L во всех графиках, таблицах и метриках.

**Ключевые достижения:**
- Единая система кэширования AI прогнозов
- Корректное отображение волатильности в интерфейсе
- Автоматическая перерисовка компонентов при изменении AI состояния
- Детальное логирование для поддержки и отладки

## 📚 Использование

1. Включить AI тоггл в интерфейсе калькулятора
2. При изменении параметров (цена, дата) автоматически запрашиваются AI прогнозы
3. Все расчеты P&L используют прогнозируемую волатильность
4. В описаниях опционов отображается текущая IV с указанием AI прогноза

## 🔮 Будущие Улучшения

- Кэширование на уровне браузера для ускорения загрузки
- Визуальные индикаторы загрузки AI прогнозов
- Расширенная аналитика использования AI функций
- Оптимизация производительности при большом количестве опционов
