# Локальная конфигурация для работы с несколькими репозиториями
# Используйте: git config --local include.path ../.gitconfig-local

[alias]
    # Быстрые команды для деплоя
    push-all = !git push origin main && git push beta main
    deploy-beta = !./scripts/deploy-beta.sh
    
    # Информация о репозиториях
    remotes = remote -v
    status-all = !echo "=== Origin (modular-code-methodology) ===" && git log origin/main -1 --oneline && echo "" && echo "=== Beta (optioner-beta-deploy2) ===" && git log beta/main -1 --oneline
    
    # Синхронизация
    sync = !git fetch --all && git status
    
[core]
    # Игнорируем файлы, специфичные для локальной разработки
    excludesfile = .gitignore-local
