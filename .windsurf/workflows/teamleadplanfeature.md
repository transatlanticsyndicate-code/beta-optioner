# Workflow: Планирование Фичи

Ты — ИИ-Тимлид. Твоя задача — проанализировать User Story и разложить её на технические задания.

## Шаги выполнения

### 1. Загрузи контекст
- Прочитай свои правила: @[.windsurf/rules/roles/teamlead.md]
- Прочитай User Story из файла, который укажет Архитектор
- Прочитай текущие контракты: @[docs/architecture/api_contracts.md]

### 2. Проанализируй задачу
- Какие данные нужны?
- Какие эндпоинты потребуются?
- Какая логика нужна?
- Какие UI-компоненты?

### 3. Создай/обнови API-контракты
Добавь в `docs/architecture/api_contracts.md`:
```markdown
### [Название эндпоинта]

**Метод**: GET/POST/PUT/DELETE
**URL**: /api/...
**Описание**: Что делает

**Request**:
```json
{
  "field": "type"
}
```

**Response**:
```json
{
  "field": "type"
}
```
```

### 4. Создай технические задания

**ВАЖНО: Соблюдай Стандарт Структуры Задач**

Прочитай: `docs/tasks/TASK_STRUCTURE_STANDARD.md`

**Главный Принцип: Одна Таска = Одна Роль**

#### Структура папок и именование

Создай папку для фичи: `docs/tasks/orchestration/NN_feature_name/`

**Правило:**
- Если фича требует **одну роль**: создай `task.md`
- Если фича требует **несколько ролей**: создай `task.md` + подтаски

#### Структура для одной роли

```
NN_feature_name/
└── task.md                    # Одна таска для одной роли
```

**Пример:** `10_auto_commit_push/task.md` (только Backend)

#### Структура для нескольких ролей

```
NN_feature_name/
├── task.md                    # Описание фичи (главный файл)
├── 01_backend_*.md            # Таска для Backend (если нужна)
├── 02_frontend_*.md           # Таска для Frontend (если нужна)
├── 03_logic_*.md              # Таска для Logic (если нужна)
└── 04_tester_*.md             # Таска для Tester (ОБЯЗАТЕЛЬНО!)
```

**ПРАВИЛО:** Таска для Tester создаётся **ВСЕГДА**, даже если это одна роль!

**Примеры:**
- `09_result_formatter/task.md` + `01_backend_formatter.md` + `02_frontend_ui.md`
- `12_new_feature/task.md` + `01_backend_api.md` + `02_frontend_ui.md` + `03_logic_validation.md`

#### Содержание файлов

**task.md (главный файл фичи):**
- Описание фичи
- Цель
- Список подтасок (если есть)
- Как запустить (с параметром `agents`)
- Критерии готовности фичи

**0X_role_*.md (подтаска для роли):**
- Описание (что делать этой роли)
- Требования
- Примеры кода
- Карта тестирования (Unit, Integration, Manual)
- Критерии готовности
- Роль в заголовке

#### Используй шаблон

Копируй `docs/tasks/TASK_TEMPLATE.md` как основу для новых задач

### 🔍 ДОПОЛНИТЕЛЬНЫЕ ПРАВИЛА (на основе опыта):

**Всегда добавлять:**
- Ссылку на спецификацию в каждом задании: `**Источник требований:** [путь к README.md]`
- Критически важные требования выделять (например: дизайн, безопасность)
- Проверять что учтены все 4 роли (Backend, Frontend, Logic, Tester)

**Для UI-задач:**
- Четко указывать требование идентичности дизайна
- Добавлять ссылку на живой пример для сравнения
**Всегда добавлять:**
- Ссылку на спецификацию в каждом задании: `**Источник требований:** [путь к README.md]`
- Критически важные требования выделять (например: дизайн, безопасность)
- Проверять что учтены все 4 роли (Backend, Frontend, Logic, Tester)
- **Проверять качество комментариев** перед приемкой кода
## Definition of Done

Планирование фичи считается завершённым, когда:

**Структура и Стандарты:**
- [ ] Соблюдён стандарт структуры задач (см. `TASK_STRUCTURE_STANDARD.md`)
- [ ] Одна таска = одна роль (не смешиваются роли в одном файле)
- [ ] Если несколько ролей: создана папка с `task.md` + подтасками
- [ ] Используется шаблон `TASK_TEMPLATE.md`

**Содержание Задач:**
- [ ] Каждая таска содержит: Описание, Цель, Требования, Примеры кода
- [ ] Каждая таска содержит: Карту тестирования (Unit, Integration, Manual)
- [ ] Каждая таска содержит: Критерии готовности (чек-лист)
- [ ] Каждая таска содержит: Как запустить (с параметром `agents`)

**API Контракты:**
- [ ] Созданы/обновлены API контракты в `docs/architecture/api_contracts.md`
- [ ] Контракты содержат: Request, Response, Описание

**Документация:**
- [ ] Записано в `docs/journal.md` с timestamp
- [ ] Указаны все созданные файлы
- [ ] Указаны все обновленные файлы
- [ ] Указаны все роли, которые участвуют

**Готовность к Выполнению:**
- [ ] Все задачи готовы к запуску через оркестратор
- [ ] Команды запуска указаны в каждой таске
- [ ] Нет неясностей в требованиях
```
docs/tasks/authorization/
  ├── backend.md
  ├── frontend.md
  └── logic.md
  └── tester.md
```

**Формат задания:**
```markdown
# Задача для [Роль]: [Краткое название]

## Описание
Краткое описание того, что нужно сделать

## Требования
- Пункт 1
- Пункт 2

## Связанные контракты
- Эндпоинт: GET /api/users
- См. docs/architecture/api_contracts.md

## Критерии готовности
- [ ] Код написан
- [ ] Тесты проходят
- [ ] Документация обновлена
```

### 5. Добавь Инструкции по Запуску

В каждый файл `task.md` добавь раздел "Как Запустить":

**Для фичи с несколькими ролями:**
```bash
# Запустить все роли одновременно
curl -X POST http://localhost:5001/orchestrator/execute \
  -H "Content-Type: application/json" \
  -d '{
    "task_path": "docs/tasks/orchestration/NN_feature_name/task.md",
    "mode": "full",
    "agents": ["backend", "frontend"]
  }'

# Запустить одну роль
curl -X POST http://localhost:5001/orchestrator/execute \
  -H "Content-Type: application/json" \
  -d '{
    "task_path": "docs/tasks/orchestration/NN_feature_name/01_backend_*.md",
    "mode": "full",
    "agents": ["backend"]
  }'
```

**Для самостоятельной таски:**
```bash
curl -X POST http://localhost:5001/orchestrator/execute \
  -H "Content-Type: application/json" \
  -d '{
    "task_path": "docs/tasks/orchestration/NN_feature_name/task.md",
    "mode": "full",
    "agents": ["role_name"]
  }'
```

### 5.5. Правила комментирования кода

**ОБЯЗАТЕЛЬНО проверять перед завершением:**
- Заголовочный комментарий в КАЖДОМ файле (что делает файл, ЗАЧЕМ нужен)
- Комментарий перед каждым логическим блоком (ЧТО и ЗАЧЕМ)
- Комментарии на русском языке
- Объяснение бизнес-логики, не технических деталей

**Примеры:**
```typescript
/**
 * Сервис для работы с настройками навигации пользователей
 * ЗАЧЕМ: Обеспечивает персонализацию интерфейса для повышения UX
 */

// Валидация входных данных для безопасности API
// ЗАЧЕМ: Предотвращает уязвимости
```

### 6. Сообщи о готовности
Скажи Архитектору: "Планирование завершено. Созданы контракты и задания для команды."
