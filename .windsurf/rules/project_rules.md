---
trigger: always_on
---

# Глобальные Правила Проекта

# Глобальные Правила Проекта

## Архитектурные Принципы

1. **API-First подход**: Все взаимодействие между слоями через четко определенные контракты
2. **Separation of Concerns**: Строгое разделение ответственности между слоями
3. **Тестируемость**: Весь код должен быть покрыт тестами
4. **Документирование**: Все публичные API должны быть задокументированы

## Модульность Кода

**Цель**: Файлы должны быть достаточно компактны, чтобы ИИ мог их полностью прочитать и поместить в контекстное окно.

- **Максимум 300 строк на файл** — если больше, разбить на несколько модулей
- **Одна ответственность на файл** — один файл = одна функция/класс/компонент
- **Экспортировать только публичный API** — скрывать внутреннюю реализацию
- **Использовать индексные файлы** (`index.js`) для удобного импорта из папок

## Комментирование Кода

**Цель**: Код должен быть понятен не-программисту. Комментарии — не для синтаксиса, а для смысла.

- **Заголовочный комментарий в начале файла**: Что делает этот файл, зачем он нужен в системе, какие компоненты затрагивает (2-3 строки)
- **Комментарий перед каждым логическим блоком**: Объяснить ЧТО и ЗАЧЕМ (не КАК), какую бизнес-ценность несет
- **Избегать очевидных комментариев**: `x++` не нужен комментарий, а вот сложная бизнес-логика — нужна
- **На русском языке** для удобства архитектора

### Примеры правильных комментариев:

**✅ ПРАВИЛЬНО:**
```typescript
/**
 * Сервис для работы с настройками навигации пользователей
 * ЗАЧЕМ: Обеспечивает персонализацию интерфейса для повышения UX
 * Затрагивает: пользовательские настройки, тема, язык интерфейса
 */

// Валидация входных данных для безопасности API
// ЗАЧЕМ: Предотвращает уязвимости и обеспечивает корректность данных
if (typeof sidebarCollapsed !== "boolean") {
  throw new Error("Некорректный тип данных для sidebarCollapsed");
}
```

**❌ НЕПРАВИЛЬНО:**
```typescript
// Проверка типа
if (typeof x !== "boolean") {
  // Ошибка
  throw new Error("Invalid type");
}
```

**Комментарии должны помогать НЕ-ПРОГРАММИСТУ понять логику системы!**


## Стандарты Кода

- **Язык**: TypeScript для всего проекта
- **Форматирование**: Prettier с настройками по умолчанию
- **Линтер**: ESLint с рекомендуемыми правилами
- **Именование**: camelCase для переменных и функций, PascalCase для классов и типов

## Definition of Done

Задача считается выполненной, когда:
Задача считается выполненной, когда:
- [ ] Код написан, отформатирован и прокомментирован согласно правилам
- [ ] Все тесты проходят
- [ ] Код прошел проверку линтером
- [ ] Документация обновлена
- [ ] Изменения зафиксированы в `docs/journal.md`

## Правило: Не Переписывай Существующий Код

- Если функция уже работает — не трогай её
- Если нужны изменения — минимальный fix, не рефакторинг
- Всегда проверь, нет ли уже готовых функций/библиотек

## Правило: Логирование и Отладка

- Добавляй console.log/logger для ключевых точек
- Логи должны быть информативны: что произошло и почему
- На production убирай debug-логи

## Правило: Обработка Ошибок

- Никогда не игнорируй ошибки (try-catch везде)
- Всегда возвращай понятное сообщение об ошибке
- Логируй stack trace для отладки

## Правило: Версионирование API

- Если меняешь контракт API — обнови версию в `docs/architecture/api_contracts.md`
- Старые версии не удаляй — поддерживай backward compatibility

## Правило: Зависимости

- Перед добавлением новой библиотеки — проверь, нет ли аналога
- Документируй зачем нужна каждая зависимость
- Избегай конфликтов версий

## Правило: Актуальность Поля Статус в Тасках

- Каждая таска должна иметь поле **Статус:** в начале файла
- Статусы: 🔴 Не начато | 🟡 В процессе | 🟢 Завершено
- Обновляй статус по мере выполнения работы
- Перед перемещением в `done/` статус должен быть 🟢 Завершено
- Подробнее: [TASK_STATUS_REQUIREMENTS.md](docs/orchestration/TASK_STATUS_REQUIREMENTS.md)

## Запреты

- **Запрещено**: Пересечение зон ответственности (бэкенд не касается UI, фронтенд не обращается к БД напрямую)
- **Запрещено**: Коммиты без прохождения тестов
- **Запрещено**: Изменение контрактов API без обновления документации
