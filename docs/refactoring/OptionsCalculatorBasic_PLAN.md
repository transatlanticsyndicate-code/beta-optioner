# План Рефакторинга: OptionsCalculatorBasic.jsx

**Текущий размер:** 2,139 строк  
**Целевой размер:** ~8 модулей по <300 строк каждый  
**Приоритет:** 🔴 Критический

---

## 📋 Анализ Структуры Файла

### Импорты (строки 1-58)
- React hooks
- UI компоненты (lucide-react, shadcn/ui)
- Модульные компоненты калькулятора
- Утилиты и хуки

### Константы (строки 59-67)
- `demoOptions` - демо-данные для опционов

### Основной Компонент (строки 71-2140)
- **State переменные** (~150 строк)
  - Тикер и цены
  - Позиции и опционы
  - Настройки калькулятора
  - UI состояния
  
- **useEffect хуки** (~200 строк)
  - Загрузка данных
  - Синхронизация состояний
  - localStorage
  
- **Функции-обработчики** (~800 строк)
  - Управление позициями
  - Загрузка данных опционов
  - Расчеты
  - Стратегии
  
- **JSX рендер** (~900 строк)
  - Блоки калькулятора
  - Диалоги
  - Графики

---

## 🎯 План Разделения на Модули

### Модуль 1: `constants.js` (~50 строк)
**Что выносим:**
- `demoOptions`
- Конфигурация блоков
- Магические числа
- Enum-ы

### Модуль 2: `hooks/useCalculatorState.js` (~250 строк)
**Что выносим:**
- Все useState объявления
- Базовая логика состояния
- localStorage синхронизация

### Модуль 3: `hooks/useDataLoading.js` (~200 строк)
**Что выносим:**
- Загрузка дат экспирации
- Загрузка страйков
- Загрузка деталей опционов
- Загрузка dividend yield
- IV Surface

### Модуль 4: `utils/positionHandlers.js` (~250 строк)
**Что выносим:**
- `addOption`
- `updateOption`
- `deleteOption`
- `toggleOptionVisibility`
- Управление позициями

### Модуль 5: `utils/strategyHandlers.js` (~200 строк)
**Что выносим:**
- Применение стратегий
- Сохранение кастомных стратегий
- Управление стратегиями

### Модуль 6: `utils/configurationHandlers.js` (~150 строк)
**Что выносим:**
- Сохранение конфигураций
- Загрузка конфигураций
- Режим редактирования
- Lock/Unlock логика

### Модуль 7: `components/CalculatorBlocks.jsx` (~250 строк)
**Что выносим:**
- JSX блоки калькулятора
- Условный рендеринг блоков
- Layout компоненты

### Модуль 8: `OptionsCalculatorBasic.jsx` (~280 строк)
**Что остается:**
- Основной компонент
- Импорты модулей
- Композиция хуков
- Главный JSX layout

---

## 📝 Порядок Выполнения

1. ✅ Создать `constants.js`
2. ✅ Создать `hooks/useCalculatorState.js`
3. ✅ Создать `hooks/useDataLoading.js`
4. ✅ Создать `utils/positionHandlers.js`
5. ✅ Создать `utils/strategyHandlers.js`
6. ✅ Создать `utils/configurationHandlers.js`
7. ✅ Создать `components/CalculatorBlocks.jsx`
8. ✅ Обновить основной файл `OptionsCalculatorBasic.jsx`
9. ✅ Создать `index.js` для централизованных экспортов
10. ✅ Тестирование

---

## ⚠️ Важные Моменты

1. **Не ломать функциональность** - все должно работать как раньше
2. **Сохранить все комментарии** - особенно ЗАЧЕМ
3. **Не менять логику** - только структуру
4. **Тестировать после каждого шага**

---

**Статус:** 🔄 Готов к началу рефакторинга
