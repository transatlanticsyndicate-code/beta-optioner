# Шаблон Рефакторинга Файла

**Используй этот шаблон для рефакторинга любого файла >300 строк**

---

## 📋 Информация о Файле

- **Путь:** `[путь к файлу]`
- **Текущий размер:** [число] строк
- **Целевой размер:** <300 строк
- **Приоритет:** 🔴 Критический / 🟡 Высокий / 🟢 Средний
- **Дата начала:** [дата]

---

## 🔍 Анализ Структуры

### Импорты (строки X-Y)
- React hooks
- UI компоненты
- Утилиты
- Другие модули

### Константы (строки X-Y)
- Демо-данные
- Конфигурация
- Enum-ы

### State Переменные (строки X-Y)
- useState объявления
- useRef
- Другие hooks

### Функции (строки X-Y)
- Обработчики событий
- Утилиты
- Расчеты
- API вызовы

### JSX (строки X-Y)
- Основной рендер
- Подкомпоненты
- Условный рендеринг

---

## 🎯 План Разделения на Модули

### Модуль 1: `constants.js` (~50 строк)
**Что выносим:**
- Демо-данные
- Настройки по умолчанию
- Enum-ы
- Конфигурация

**Пример:**
```javascript
export const DEMO_DATA = [...];
export const DEFAULT_SETTINGS = {...};
export const MODES = {...};
```

### Модуль 2: `utils.js` (~100 строк)
**Что выносим:**
- Форматирование (даты, числа, строки)
- Валидация
- Преобразование данных
- Вспомогательные функции

**Пример:**
```javascript
export function formatDate(date) { ... }
export function validatePrice(price) { ... }
export function roundToStep(value, step) { ... }
```

### Модуль 3: `calculations.js` (~200 строк)
**Что выносим:**
- Бизнес-логика
- Сложные вычисления
- Алгоритмы
- Математические функции

**Пример:**
```javascript
export function calculatePnL(options, price) { ... }
export function calculateBreakEven(positions) { ... }
export function calculateGreeks(option) { ... }
```

### Модуль 4: `hooks/useCustomHook.js` (~150 строк)
**Что выносим:**
- State management
- useEffect логика
- Обработчики событий
- Подписки

**Пример:**
```javascript
export function useCustomHook(params) {
  const [state, setState] = useState();
  useEffect(() => { ... }, [deps]);
  return { state, handlers };
}
```

### Модуль 5: `components/SubComponent.jsx` (~150 строк)
**Что выносим:**
- Повторяющиеся блоки JSX
- Сложные UI элементы
- Элементы списков

**Пример:**
```javascript
export function SubComponent({ props }) {
  return <div>...</div>;
}
```

### Модуль 6: `index.js` (~15 строк)
**Централизованные экспорты:**
```javascript
export * from './constants';
export * from './utils';
export * from './calculations';
export { useCustomHook } from './hooks/useCustomHook';
export { SubComponent } from './components/SubComponent';
export { default } from './MainComponent';
```

### Модуль 7: `[FileName].jsx` (<300 строк)
**Что остается:**
- Основной компонент
- Импорты модулей
- Композиция хуков
- Главный JSX layout

---

## 📝 Порядок Выполнения

### 1. Подготовка
- [ ] Создать папку `[FileName]/`
- [ ] Создать подпапки `hooks/`, `components/`, `utils/`
- [ ] Создать файлы модулей

### 2. Вынос Кода
- [ ] Вынести константы в `constants.js`
- [ ] Вынести утилиты в `utils.js`
- [ ] Вынести расчеты в `calculations.js`
- [ ] Вынести hooks в `hooks/`
- [ ] Вынести компоненты в `components/`

### 3. Обновление Основного Файла
- [ ] Обновить импорты
- [ ] Использовать модули вместо локального кода
- [ ] Проверить, что все работает

### 4. Комментирование
- [ ] Добавить заголовочные комментарии в каждый файл
- [ ] Добавить комментарии перед логическими блоками
- [ ] Объяснить ЧТО и ЗАЧЕМ (не КАК)

### 5. Проверка
- [ ] `npm run check-file-size:all` - все файлы <300 строк
- [ ] `npm run check-forbidden:all` - нет антипаттернов
- [ ] `npm run build` - компиляция успешна
- [ ] Ручное тестирование UI

### 6. Коммит
- [ ] `git add -A`
- [ ] `git commit -m "refactor: [FileName] [old]→[new] строк"`
- [ ] `git push origin main`

---

## ✅ Чек-лист Качества

### Размер Файлов
- [ ] Все модули <300 строк
- [ ] Основной файл <300 строк
- [ ] index.js <50 строк

### Комментарии
- [ ] Заголовок файла (что, зачем, что затрагивает)
- [ ] Комментарии перед логическими блоками
- [ ] Все комментарии на русском
- [ ] Объяснение бизнес-ценности

### Код
- [ ] Нет mock данных в production
- [ ] Нет хардкода (все в константах)
- [ ] Нет console.log
- [ ] Нет TODO/FIXME без задач
- [ ] Нет дублирования кода

### Функциональность
- [ ] Компиляция без ошибок
- [ ] Все импорты корректны
- [ ] UI работает как раньше
- [ ] Нет регрессий

---

## 📊 Результаты

### До Рефакторинга
- **Файлов:** 1
- **Строк:** [число]
- **Читаемость:** ❌
- **Тестируемость:** ❌

### После Рефакторинга
- **Файлов:** [число] модулей
- **Строк:** [число] (экономия: [число] строк, [процент]%)
- **Читаемость:** ✅
- **Тестируемость:** ✅

### Созданные Модули
1. `constants.js` - [число] строк
2. `utils.js` - [число] строк
3. `calculations.js` - [число] строк
4. `hooks/useCustomHook.js` - [число] строк
5. `components/SubComponent.jsx` - [число] строк
6. `index.js` - [число] строк
7. `[FileName].jsx` - [число] строк

---

## 💡 Советы

1. **Начинай с простого** - сначала константы и утилиты
2. **Тестируй часто** - после каждого модуля
3. **Не спеши** - качество важнее скорости
4. **Сохраняй логику** - не меняй работающий код
5. **Комментируй** - объясняй ЗАЧЕМ, не КАК
6. **Проверяй** - используй скрипты перед коммитом

---

**Статус:** [🔄 В процессе / ✅ Завершено]
