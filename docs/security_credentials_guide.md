# Руководство по работе с учетными данными (Credentials)

## 🚨 ВАЖНО: Никогда не храните пароли в коде!

### Что случилось
GitHub обнаружил API ключи и пароли в коде проекта:
- `backend/app/services/coinmarketcap_service.py` - API ключ CoinMarketCap
- `backend/app/services/email_service.py` - пароль Gmail

Это критическая проблема безопасности - любые пароли в git истории становятся публичными.

### Как это исправлено
1. **Созданы файлы окружения:**
   - `.env` - содержит реальные пароли (не в git)
   - `.env.example` - шаблон для других разработчиков

2. **Код изменен:**
   - Пароли заменены на чтение из переменных окружения (`os.getenv()`)

3. **История git очищена:**
   - Все старые коммиты перезаписаны без паролей
   - Вместо реальных ключей теперь `REMOVED_API_KEY`

### Что делать разработчикам

#### 1. После pull изменений:
```bash
# Сделать pull последних изменений
git pull origin main

# Скопировать шаблон окружения
cp .env.example .env

# Отредактировать .env с вашими реальными ключами
nano .env
```

#### 2. Добавить свои ключи в `.env`:
```bash
# CoinMarketCap API (получить на https://pro.coinmarketcap.com/)
CMC_API_KEY=your_api_key_here

# Gmail пароль приложения (не обычный пароль!)
SENDER_PASSWORD=your_gmail_app_password
```

#### 3. Установить python-dotenv для бэкенда:
```bash
cd backend
pip install python-dotenv
```

### Правила работы с секретами

#### ✅ ПРАВИЛЬНО:
- Хранить ключи в `.env` файле
- Добавить `.env` в `.gitignore` (уже добавлено)
- Использовать `os.getenv()` в коде
- Создавать `.env.example` для команды

#### ❌ ЗАПРЕЩЕНО:
- Никогда не коммитить `.env` файл
- Не хранить пароли в коде даже временно
- Не использовать реальные ключи в примерах кода
- Не передавать ключи через мессенджеры

### Если случайно закоммитили пароль
1. Немедленно отозвать ключ в сервисе (CoinMarketCap, Gmail и т.д.)
2. Удалить из кода и добавить в `.env`
3. Очистить историю git: `git filter-repo --replace-text passwords.txt --force`
4. Сделать force push в репозиторий

### Проверка безопасности
Регулярно проверяйте репозиторий на наличие секретов:
- GitHub Security scanning
- GitGuardian
- `git log -p | grep -i "password\|key\|secret"`

### Куда обращаться
- **Безопасность:** Levon (levonmusoyan@gmail.com)
- **Технические вопросы:** В issues проекта

---
**Помните:** Один скомпрометированный ключ может стоить проекта и денег!
