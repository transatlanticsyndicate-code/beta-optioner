# Анализ функционала "Золотой кнопки" (Golden Button)

## Обзор

**"Золотая кнопка"** (Golden Button) — это функционал автоматического подбора опционов, расположенный в заголовке таблицы опционов на странице "Калькулятор опционов". Кнопка имеет золотой градиент и иконку короны (Crown).

**Назначение**: Альтернативный алгоритм подбора опционов для максимизации прибыли или хеджирования рисков в зависимости от текущего состояния портфеля.

**Отличие от "Волшебной кнопки"**: Золотая кнопка использует другой подход к подбору опционов, фокусируясь на максимизации прибыли при заданных параметрах роста/падения цены актива.

---

## Техническая реализация

### Компоненты

1. **`GoldenButton.jsx`** — UI компонент кнопки
   - Расположение: `frontend/src/components/CalculatorV2/GoldenSelection/`
   - Стиль: Золотой градиент `linear-gradient(135deg, #facc15 0%, #eab308 50%, #ca8a04 100%)`
   - Иконка: Crown (корона) из lucide-react
   - Тень: `0 2px 8px rgba(234, 179, 8, 0.4)`

2. **`GoldenSelectionModal.jsx`** — Модальное окно подбора
   - Расположение: `frontend/src/components/CalculatorV2/GoldenSelection/`
   - Размер: 450px ширина
   - Функционал: Проверка условий, ввод параметров, отображение прогресса

3. **`goldenSelectionLogic.js`** — Бизнес-логика подбора
   - Расположение: `frontend/src/components/CalculatorV2/GoldenSelection/`
   - Функции:
     - `filterDatesByRange()` — фильтрация дат экспирации
     - `findBestGoldenBuyCall()` — подбор Buy CALL опциона
     - `findBestGoldenBuyPut()` — подбор Buy PUT опциона (в разработке)

4. **`index.js`** — Экспорт компонентов
   - Экспортирует: `GoldenButton`, `GoldenSelectionModal`

### Интеграция

Компоненты импортируются в `OptionsTable.jsx`:
```javascript
import { GoldenButton, GoldenSelectionModal } from './GoldenSelection';
```

Кнопка размещена рядом с "Волшебной кнопкой" в заголовке таблицы опционов.

---

## Принцип действия

Функционал работает по **сценарному принципу**, анализируя текущее состояние калькулятора:

### Условия активации

Золотая кнопка доступна только при выполнении одного из условий:

1. **Сценарий 2 (SCENARIO_2)**: Калькулятор полностью пуст
   - Позиции базового актива: 0
   - Опционы: 0
   - **Цель**: Подобрать оптимальный Buy CALL для максимальной прибыли на росте

2. **Сценарий 3 (SCENARIO_3)**: Есть ровно один Buy CALL опцион
   - Позиции базового актива: 0
   - Опционы: 1 (тип: CALL, действие: Buy/long)
   - **Цель**: Подобрать хеджирующий Buy PUT для защиты от падения

### Если условия НЕ выполнены

Модальное окно показывает предупреждение:
- Отображает текущее состояние (количество позиций и опционов)
- Показывает галочки/крестики для каждого условия
- Требует очистить калькулятор или иметь ровно один Buy CALL

---

## Сценарий 2: Подбор Buy CALL (Максимизация прибыли)

### Назначение
Найти самый прибыльный Buy CALL опцион при ожидаемом росте цены актива.

### Параметры подбора

**Диапазон дат экспирации** (по умолчанию):
- Минимум: 60 дней от сегодня
- Максимум: 100 дней от сегодня

**Ожидаемый рост цены актива**:
- По умолчанию: 50%
- Используется для расчета целевой цены: `targetPrice = currentPrice * (1 + growthPercent / 100)`

**Параметры отображаются в сворачиваемом блоке** для удобства пользователя.

### Алгоритм подбора

#### Шаг 1: Фильтрация дат экспирации
```javascript
filterDatesByRange(availableDates, minDays, maxDays)
```
- Берет все доступные даты экспирации
- Фильтрует по диапазону дней от сегодня
- Логирует каждую дату для отладки

**Обработка ошибок**:
- Если дат нет → возвращает ошибку с информацией о ближайших датах

#### Шаг 2: Сбор кандидатов
Для каждой подходящей даты:
1. Запрос к API: `/api/polygon/ticker/${ticker}/options?expiration_date=${date}`
2. Фильтр по типу: только CALL опционы
3. Фильтр по цене: только с `ask > 0`
4. **Фильтр по страйкам**: Только 2 ближайших к текущей цене
   - Один снизу (≤ currentPrice)
   - Один сверху (> currentPrice)

**Пример**:
```
Текущая цена: $100
Доступные страйки: $90, $95, $100, $105, $110
Выбранные: $100 (снизу), $105 (сверху)
```

#### Шаг 3: Расчет прибыли
Для каждого кандидата:
```javascript
calculateOptionPLValue(
  option,
  targetPrice,      // Целевая цена при росте
  currentPrice,
  0,                // daysRemaining = 0 (на момент экспирации)
  null,             // volatility
  0                 // dividendYield
)
```

**Формула P&L**:
- Для Buy CALL: `P&L = (TheoreticalPrice - EntryPrice) × Quantity × Multiplier`
- На момент экспирации: `TheoreticalPrice = Max(0, TargetPrice - Strike)`
- Профит = `Max(0, TargetPrice - Strike) - Premium`

**Расчет ROI**:
```javascript
cost = premium * 100
roi = (profit / cost) * 100
```

#### Шаг 4: Выбор лучшего
- Сортировка кандидатов по прибыли (по убыванию)
- Выбор опциона с максимальной прибылью
- Если все убыточны → возвращает лучший с предупреждением

### Результат

**При успехе**:
- Опцион автоматически добавляется в таблицу
- Модальное окно закрывается
- Опцион содержит дополнительные поля:
  - `calculatedProfit` — расчетная прибыль
  - `calculatedRoi` — ROI в процентах
  - `targetPrice` — целевая цена

**При ошибке**:
- Отображается сообщение об ошибке
- Возможность изменить параметры и повторить поиск

### Возможные ошибки

1. **NO_DATES**: Нет дат в заданном диапазоне
   - Сообщение содержит информацию о ближайших датах
   
2. **NO_CANDIDATES**: Не найдено подходящих опционов
   - Проблема с фильтрацией или API

3. **NO_PROFITABLE_OPTIONS**: Все опционы убыточны
   - Слишком дорогой премиум или недостижимый таргет

---

## Сценарий 3: Подбор Buy PUT (Хеджирование)

### Назначение
Подобрать оптимальный Buy PUT опцион для компенсации убытков при падении цены актива.

### Параметры подбора

**Диапазон дат экспирации** (по умолчанию):
- Минимум: 5 дней
- Максимум: 10 дней

**Параметры падения**:
- Падение цены актива: -2.5%
- Выход на день: 5

**Параметры фильтрации**:
- Диапазон страйков: ±20% от текущей цены
- Минимальный Open Interest: 100 (для ликвидности)

### Статус реализации

⚠️ **В РАЗРАБОТКЕ** (NOT_IMPLEMENTED)

Функция `findBestGoldenBuyPut()` возвращает:
```javascript
{
  error: 'NOT_IMPLEMENTED',
  message: 'Логика подбора BuyPUT еще не реализована (Сценарий 3)'
}
```

### Планируемая логика (TODO)

1. Фильтр дат (ближние экспирации)
2. Сбор PUT опционов с нужными страйками
3. Выбор по ликвидности (Open Interest)
4. Оценка эффективности хеджа при падении на `dropPercent`

---

## UI/UX особенности

### Кнопка

**Визуальный стиль**:
- Золотой градиент (3 оттенка желтого)
- Иконка короны
- Тень с золотым оттенком
- Анимация при наведении: `opacity-90` + `scale-105`
- Высота: 8 (h-8)

**Состояния**:
- Активна: полный градиент + тень
- Отключена: без тени

### Модальное окно

**Шапка**:
- Золотой градиент (как у кнопки)
- Белый текст
- Иконка короны
- Отрицательный margin для полного покрытия

**Контент**:
- Максимальная ширина: 450px
- Белый фон для кнопки закрытия

**Сворачиваемый блок параметров**:
- Заголовок: "Параметры подбора"
- Иконка: ChevronDown/ChevronUp
- Серый фон при наведении
- Золотые разделители между группами параметров

**Этапы работы**:
1. **check/input**: Форма ввода параметров
2. **searching**: Индикатор загрузки + текст прогресса
3. **result**: Удален (автоматическое добавление)

**Обработка ошибок**:
- Красный фон (bg-red-50)
- Красная рамка
- Иконка AlertCircle
- Многострочный текст ошибки

---

## Интеграция с OptionsTable

### Props передаваемые в GoldenSelectionModal

```javascript
<GoldenSelectionModal
  isOpen={goldenModalOpen}
  onClose={() => setGoldenModalOpen(false)}
  positions={positions}              // Позиции базового актива
  options={options}                  // Текущие опционы
  currentPrice={currentPrice}        // Текущая цена актива
  selectedTicker={selectedTicker}    // Выбранный тикер
  availableDates={availableDates}    // Доступные даты экспирации
  onAddOption={(option) => {         // Callback добавления опциона
    if (onAddMagicOption) {
      onAddMagicOption(option);
    }
    setGoldenModalOpen(false);
  }}
/>
```

### Формат добавляемого опциона

```javascript
{
  ...result,                          // Все данные из API
  type: 'CALL',                       // Принудительно CALL
  side: 'long',                       // Buy
  strike: result.strike,
  premium: result.premium || result.ask || result.last_price,
  date: result.expiration_date,
  expiration_date: result.expiration_date,
  expirationDate: result.expiration_date,  // Для OptionsCalculatorBasic
  action: 'Buy',
  calculatedProfit: bestOption.profit,     // Дополнительно
  calculatedRoi: bestOption.roi,           // Дополнительно
  targetPrice: targetPrice                 // Дополнительно
}
```

---

## Зависимости

### Внешние библиотеки
- `lucide-react`: Иконки (Crown, AlertCircle, CheckCircle, Loader2, ChevronDown, ChevronUp)
- `react`: Хуки (useState, useEffect)

### Внутренние модули
- `../../ui/dialog`: Dialog, DialogContent, DialogHeader, DialogTitle
- `../../ui/button`: Button
- `../../ui/input`: Input
- `../../ui/label`: Label
- `../../../utils/optionPricing`: calculateOptionPLValue

### API эндпоинты
- `/api/polygon/ticker/${ticker}/options?expiration_date=${date}`

---

## Логирование и отладка

### Console logs

**GoldenSelectionModal**:
```javascript
console.log('🔍 GoldenModal Debug:', {
  isOpen,
  optionsCount,
  positionsCount,
  firstOption,
  isScenario3,
  check: { hasOneCall, isBuy }
});
```

**goldenSelectionLogic**:
```javascript
console.log('👑 Начинаем золотой подбор BuyCALL...');
console.log('🎯 Целевая цена: $...');
console.log('📅 Подходящие даты: ...');
console.log('🎯 Ближайшие страйки для цены ...');
console.log('📦 Всего кандидатов: ...');
console.log('✨ Лучший Golden Option:', bestOption);
```

**filterDatesByRange**:
```javascript
console.log(`📅 Дата: ${dateStr}, Дней: ${diffDays}, Диапазон: [${minDays}, ${maxDays}], Подходит: ${isMatch}`);
```

### TODO комментарии

1. `GoldenSelectionModal.jsx:64`: Удалить логи после отладки
2. `goldenSelectionLogic.js:283-287`: Реализовать логику подбора BuyPUT

---

## Сравнение с "Волшебной кнопкой"

| Характеристика | Волшебная кнопка | Золотая кнопка |
|----------------|------------------|----------------|
| **Иконка** | Sparkles (волшебная палочка) | Crown (корона) |
| **Цвет** | Фиолетовый градиент | Золотой градиент |
| **Условия активации** | Наличие позиции базового актива | Пустой калькулятор ИЛИ 1 Buy CALL |
| **Сценарии** | 4 сценария (Protective Put → Recovery Call) | 2 сценария (Buy CALL → Buy PUT) |
| **Фокус** | Хеджирование позиции | Максимизация прибыли |
| **Алгоритм подбора** | Защита + компенсация | Максимальная прибыль при росте |
| **Параметры** | Уровни риска, дни до экспирации | Рост цены, диапазон дат |
| **Фильтр страйков** | ±20% от цены | 2 ближайших к цене |

---

## Текущий статус разработки

### ✅ Реализовано

1. **UI компоненты**:
   - ✅ GoldenButton с золотым градиентом
   - ✅ GoldenSelectionModal с проверкой условий
   - ✅ Сворачиваемый блок параметров
   - ✅ Обработка ошибок и прогресса

2. **Сценарий 2 (Buy CALL)**:
   - ✅ Фильтрация дат экспирации
   - ✅ Сбор кандидатов через API
   - ✅ Фильтрация по типу и цене
   - ✅ Выбор 2 ближайших страйков
   - ✅ Расчет P&L через calculateOptionPLValue
   - ✅ Сортировка по прибыли
   - ✅ Автоматическое добавление в таблицу

3. **Интеграция**:
   - ✅ Экспорт компонентов через index.js
   - ✅ Импорт в OptionsTable.jsx
   - ✅ Размещение кнопки в UI
   - ✅ Передача всех необходимых props

### 🚧 В разработке

1. **Сценарий 3 (Buy PUT)**:
   - ⚠️ Функция создана, но возвращает NOT_IMPLEMENTED
   - ⚠️ UI готов, но логика не реализована
   - TODO: Реализовать алгоритм подбора хеджирующего PUT

### 📋 Планируется

1. Тестирование Сценария 2 на реальных данных
2. Реализация Сценария 3 (Buy PUT)
3. Удаление debug логов
4. Оптимизация производительности (параллельные запросы к API)
5. Добавление кэширования результатов

---

## Рекомендации по доработке

### Приоритет 1: Завершить Сценарий 3

**Задача**: Реализовать `findBestGoldenBuyPut()`

**Алгоритм**:
1. Фильтровать даты в диапазоне `minDays-maxDays`
2. Для каждой даты загрузить PUT опционы
3. Фильтровать по страйкам: `currentPrice * (1 ± strikeRangePercent/100)`
4. Фильтровать по ликвидности: `openInterest >= minOI`
5. Рассчитать эффективность хеджа:
   - Цена падения: `currentPrice * (1 + dropPercent/100)`
   - P&L на день `exitDay`
   - Выбрать PUT с лучшим соотношением защита/стоимость

### Приоритет 2: Оптимизация производительности

**Проблема**: Последовательные запросы к API замедляют подбор

**Решение**:
```javascript
// Вместо:
for (let i = 0; i < filteredDates.length; i++) {
  const response = await fetch(...);
}

// Использовать:
const promises = filteredDates.map(date => 
  fetch(`/api/polygon/ticker/${ticker}/options?expiration_date=${date}`)
);
const responses = await Promise.all(promises);
```

### Приоритет 3: Улучшение UX

1. **Прогресс-бар**: Показывать визуальный прогресс вместо текста
2. **Предпросмотр результата**: Показывать найденный опцион перед добавлением
3. **Сохранение параметров**: Запоминать последние использованные параметры
4. **Сравнение опционов**: Показывать топ-3 варианта вместо только лучшего

### Приоритет 4: Расширение функционала

1. **Дополнительные фильтры**:
   - Максимальная премия
   - Минимальный ROI
   - Предпочтительная дельта

2. **Альтернативные стратегии**:
   - Sell CALL (Covered Call)
   - Sell PUT (Cash-Secured Put)
   - Спреды (Bull Call Spread, Bear Put Spread)

3. **Бэктестинг**: Показывать исторические результаты подобранной стратегии

---

## Файловая структура

```
frontend/src/components/CalculatorV2/GoldenSelection/
├── index.js                      # Экспорт компонентов
├── GoldenButton.jsx              # UI кнопки (35 строк)
├── GoldenSelectionModal.jsx      # Модальное окно (460 строк)
└── goldenSelectionLogic.js       # Бизнес-логика (294 строки)
```

**Общий объем**: ~789 строк кода

---

## Заключение

"Золотая кнопка" — это мощный инструмент для автоматического подбора опционов с фокусом на максимизацию прибыли. Функционал частично реализован (Сценарий 2 работает полностью), требует завершения Сценария 3 и оптимизации производительности.

**Ключевые преимущества**:
- Простой и интуитивный интерфейс
- Автоматический расчет прибыли
- Фильтрация по ликвидности
- Гибкие параметры подбора

**Основные задачи**:
- Завершить реализацию Buy PUT подбора
- Оптимизировать запросы к API
- Провести тестирование на реальных данных
- Удалить debug логи
