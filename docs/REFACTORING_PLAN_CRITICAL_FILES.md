# 📋 Детальный План Рефакторинга Критических Файлов

**Дата:** 21 декабря 2025  
**Статус:** Планирование  
**Файлов:** 5 (5,311 строк)

---

## 📊 Анализ Структуры Файлов

### 1. OptionsAnalyzer/index.js (726 строк)

**Сложность:** Средняя  
**Оценка времени:** ~2.5 часа

#### Структура:
- **State (9 переменных):** ticker, aiModel, currentStep, stockData, metrics, aiAnalysis, aiProvider, shareUrl, analysisId, copied, error
- **Handlers (3):** handleAiModelChange, handleAnalyze, copyShareLink, startNewAnalysis
- **JSX:** Форма ввода, прогресс-бар, карточки метрик, AI анализ, спойлер с параметрами

#### План модулей (8 модулей):
1. **hooks/useAnalyzer.js** (~80 строк) - управление состоянием
2. **utils/analysisSteps.js** (~50 строк) - выполнение шагов анализа
3. **components/AnalyzerForm.jsx** (~60 строк) - форма ввода
4. **components/MetricsCards.jsx** (~70 строк) - карточки метрик
5. **components/DataSourceInfo.jsx** (~120 строк) - спойлер с параметрами
6. **components/AIAnalysisSection.jsx** (~80 строк) - секция AI анализа
7. **components/index.js** (~10 строк) - экспорты
8. **index.js** (~100 строк) - главный компонент

**Приоритет:** 1 (начать с этого)

---

### 2. TestChart.jsx (882 строки)

**Сложность:** Высокая  
**Оценка времени:** ~3 часа

#### Структура:
- **State (18 переменных):** ticker, data, loading, error, cachingEnabled, showEntries, showAverage, showStopLoss, showExits, showOptions, entry1, entry2, averageEntry, stopLoss, exit1, exit2
- **Refs (13):** chartContainerRef, chartRef, candlestickSeriesRef, entry1LineRef, entry2LineRef, и др.
- **Functions (15+):** getCachedData, setCachedData, savePageState, loadPageState, resetPageState, calculateAverageEntry, handleEntry1Change, handleEntry2Change, addEntryMarker, updatePriceLines, fetchData, createChartInstance
- **JSX:** Форма управления, график, чекбоксы отображения

#### План модулей (12 модулей):
1. **hooks/useChartState.js** (~90 строк) - управление состоянием
2. **hooks/useChartData.js** (~70 строк) - загрузка данных
3. **utils/cacheManager.js** (~50 строк) - работа с кэшем
4. **utils/stateStorage.js** (~60 строк) - localStorage
5. **utils/priceCalculations.js** (~40 строк) - расчеты цен
6. **utils/chartHelpers.js** (~80 строк) - вспомогательные функции графика
7. **components/ChartControls.jsx** (~100 строк) - элементы управления
8. **components/ChartCanvas.jsx** (~60 строк) - канвас графика
9. **components/DisplayToggles.jsx** (~80 строк) - чекбоксы отображения
10. **components/index.js** (~10 строк) - экспорты
11. **config/chartConfig.js** (~40 строк) - конфигурация
12. **index.jsx** (~120 строк) - главный компонент

**Приоритет:** 2

---

### 3. OptionsTable.jsx (1,100 строк)

**Сложность:** Высокая  
**Оценка времени:** ~3.5 часа

#### Структура:
- **Props (20+):** options, toggleOptionVisibility, deleteOption, addOption, setSaveDialogOpen, onSelectStrategy, onUpdateOption, и др.
- **Helper Functions:** formatDateForDisplay, formatPLValue, assessLiquidity, getLiquidityColor
- **JSX:** Таблица опционов, строки редактирования, выпадающие меню, кнопки действий

#### План модулей (14 модулей):
1. **utils/formatters.js** (~50 строк) - форматирование данных
2. **utils/calculations.js** (~60 строк) - расчеты P&L
3. **utils/liquidity.js** (~40 строк) - оценка ликвидности
4. **components/TableHeader.jsx** (~60 строк) - заголовок таблицы
5. **components/OptionRow.jsx** (~120 строк) - строка опциона
6. **components/EditableCell.jsx** (~80 строк) - редактируемая ячейка
7. **components/ActionButtons.jsx** (~70 строк) - кнопки действий
8. **components/StrategySelector.jsx** (~90 строк) - выбор стратегии
9. **components/LiquidityIndicator.jsx** (~50 строк) - индикатор ликвидности
10. **components/AddOptionButton.jsx** (~60 строк) - кнопка добавления
11. **components/TableFooter.jsx** (~80 строк) - футер таблицы
12. **components/index.js** (~15 строк) - экспорты
13. **hooks/useTableState.js** (~70 строк) - состояние таблицы
14. **index.jsx** (~150 строк) - главный компонент

**Приоритет:** 3

---

### 4. GradualStrategyCalculator.jsx (1,282 строки)

**Сложность:** Очень высокая  
**Оценка времени:** ~4 часа

#### Структура:
- **State (25+ переменных):** ticker, currentPrice, direction, entryPrice, stopLoss, targetPrice, riskPercent, и др.
- **Functions (20+):** calculatePositions, calculateRisk, validateInputs, handleSubmit, и др.
- **JSX:** Форма параметров, таблица позиций, графики, результаты расчетов

#### План модулей (18 модулей):
1. **hooks/useStrategyState.js** (~100 строк) - управление состоянием
2. **hooks/useCalculations.js** (~90 строк) - расчеты стратегии
3. **utils/positionCalculations.js** (~80 строк) - расчет позиций
4. **utils/riskCalculations.js** (~70 строк) - расчет рисков
5. **utils/validation.js** (~60 строк) - валидация
6. **utils/formatters.js** (~50 строк) - форматирование
7. **components/ParametersForm.jsx** (~120 строк) - форма параметров
8. **components/DirectionSelector.jsx** (~60 строк) - выбор направления
9. **components/EntrySettings.jsx** (~90 строк) - настройки входа
10. **components/ExitSettings.jsx** (~90 строк) - настройки выхода
11. **components/PositionsTable.jsx** (~110 строк) - таблица позиций
12. **components/ResultsPanel.jsx** (~100 строк) - панель результатов
13. **components/RiskMetrics.jsx** (~80 строк) - метрики риска
14. **components/StrategyChart.jsx** (~90 строк) - график стратегии
15. **components/index.js** (~20 строк) - экспорты
16. **config/constants.js** (~40 строк) - константы
17. **config/defaultValues.js** (~30 строк) - значения по умолчанию
18. **index.jsx** (~150 строк) - главный компонент

**Приоритет:** 4

---

### 5. StrikeScale.jsx (1,321 строка)

**Сложность:** Очень высокая  
**Оценка времени:** ~4 часа

#### Структура:
- **State (15+ переменных):** strikes, selectedStrikes, currentPrice, range, zoom, и др.
- **Functions (25+):** calculateStrikeMetrics, renderStrike, handleStrikeClick, handleZoom, и др.
- **JSX:** Шкала страйков, метки, индикаторы, всплывающие подсказки, элементы управления

#### План модулей (18 модулей):
1. **hooks/useStrikeScale.js** (~100 строк) - управление состоянием
2. **hooks/useStrikeMetrics.js** (~90 строк) - расчет метрик
3. **hooks/useZoom.js** (~70 строк) - управление зумом
4. **utils/strikeCalculations.js** (~80 строк) - расчеты страйков
5. **utils/scaleCalculations.js** (~70 строк) - расчеты шкалы
6. **utils/formatters.js** (~50 строк) - форматирование
7. **utils/colorHelpers.js** (~60 строк) - цветовые схемы
8. **components/ScaleContainer.jsx** (~90 строк) - контейнер шкалы
9. **components/StrikeMarker.jsx** (~80 строк) - маркер страйка
10. **components/CurrentPriceIndicator.jsx** (~60 строк) - индикатор цены
11. **components/StrikeTooltip.jsx** (~70 строк) - всплывающая подсказка
12. **components/ZoomControls.jsx** (~80 строк) - управление зумом
13. **components/RangeSelector.jsx** (~90 строк) - выбор диапазона
14. **components/MetricsOverlay.jsx** (~100 строк) - наложение метрик
15. **components/index.js** (~20 строк) - экспорты
16. **config/scaleConfig.js** (~50 строк) - конфигурация шкалы
17. **config/constants.js** (~40 строк) - константы
18. **index.jsx** (~150 строк) - главный компонент

**Приоритет:** 5

---

## 📈 Общая Статистика

| Файл | Строк | Модулей | Оценка | Приоритет |
|------|-------|---------|--------|-----------|
| OptionsAnalyzer | 726 | 8 | 2.5ч | 1 |
| TestChart | 882 | 12 | 3ч | 2 |
| OptionsTable | 1,100 | 14 | 3.5ч | 3 |
| GradualStrategyCalculator | 1,282 | 18 | 4ч | 4 |
| StrikeScale | 1,321 | 18 | 4ч | 5 |
| **ИТОГО** | **5,311** | **70** | **17ч** | - |

---

## 🎯 Стратегия Выполнения

### Этап 1: OptionsAnalyzer (2.5ч)
1. Создать структуру папок
2. Вынести хук useAnalyzer
3. Создать утилиты analysisSteps
4. Разбить на компоненты
5. Тестирование
6. Коммит

### Этап 2: TestChart (3ч)
1. Создать структуру папок
2. Вынести хуки состояния и данных
3. Создать утилиты кэша и расчетов
4. Разбить на компоненты
5. Тестирование
6. Коммит

### Этап 3: OptionsTable (3.5ч)
1. Создать структуру папок
2. Вынести утилиты форматирования
3. Создать компоненты строк и ячеек
4. Разбить на подкомпоненты
5. Тестирование
6. Коммит

### Этап 4: GradualStrategyCalculator (4ч)
1. Создать структуру папок
2. Вынести хуки и расчеты
3. Создать компоненты форм
4. Разбить на подкомпоненты
5. Тестирование
6. Коммит

### Этап 5: StrikeScale (4ч)
1. Создать структуру папок
2. Вынести хуки и расчеты
3. Создать компоненты шкалы
4. Разбить на подкомпоненты
5. Тестирование
6. Коммит

---

## ✅ Критерии Успеха

Для каждого файла:
- ✅ Все модули <300 строк
- ✅ Компиляция успешна
- ✅ Функциональность сохранена
- ✅ Русские комментарии сохранены
- ✅ Тесты проходят

---

## 💡 Рекомендации

1. **Начинать с простого** - OptionsAnalyzer первым
2. **Тестировать после каждого модуля**
3. **Коммитить после каждого файла**
4. **Документировать изменения**
5. **Сохранять оригиналы** с суффиксом -old

---

## 🚀 Ожидаемые Результаты

После завершения всех 5 файлов:
- **Файлов отрефакторено:** 20/20 (100%)
- **Модулей создано:** 171 (101 + 70)
- **Строк оптимизировано:** ~12,000 → ~3,500 (-70%)
- **Средняя экономия:** ~70%

---

**Дата создания:** 21 декабря 2025  
**Статус:** Готов к выполнению
