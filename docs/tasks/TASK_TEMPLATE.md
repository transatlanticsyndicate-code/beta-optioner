# 📋 Шаблоны Тасок для Оркестратора

**Версия:** 3.0.0  
**Дата:** 2025-11-16

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

Оркестратор ищет **строго определённые** файлы:
- `README.md` - ОБЯЗАТЕЛЬНО (главный файл)
- `backend.md` - если нужен Backend
- `frontend.md` - если нужен Frontend
- `logic.md` - если нужен Logic
- `tester.md` - если нужен Tester

**НЕ используй:** task.md, 01_backend_*.md, prefixes, suffixes

---

## 🔴 ГЛАВНОЕ ПРАВИЛО: ИНТЕГРАЦИЯ ОБЯЗАТЕЛЬНА!

⚠️ **ЕСЛИ ТАСКА ТРЕБУЕТ ИНТЕГРАЦИИ → РАЗБИТЬ НА ПОДТАСКИ!**

```
❌ НЕПРАВИЛЬНО:
   faze4_01/
   └── backend.md
       - Создание ✅
       - Интеграция (опционально) ❌ ОШИБКА!

✅ ПРАВИЛЬНО:
   faze4_01_creation/
   └── backend.md (только создание)
   
   faze4_01b_integration/
   └── backend.md (только интеграция)
   
   faze4_01c_testing/
   └── tester.md (только тестирование)
```

**Правило разбиения:**
- Только создание → 1 таска
- Создание + Интеграция → 2 таски
- Создание + Интеграция + Тестирование → 3 таски

**Обязательная интеграция включает:**
- Добавить импорты в __init__.py
- Обновить config.yaml
- Добавить в requirements.txt
- Обновить main_orchestrator.py
- Добавить в .env.example
- Обновить документацию

**Подробнее:** [TASK_COMPLETION_CHECKLIST.md](TASK_COMPLETION_CHECKLIST.md)

---

## 📝 README.md - Шаблон

```markdown
# Название Таски

**Статус:** 🔴 Не начато  
**Роль:** Backend  
**Время:** 1 час  
**Приоритет:** 🟡 Высокий

## 📋 Описание

Краткое описание (1-2 предложения).

**ЗАЧЕМ:** Бизнес-ценность  
**Затрагивает:** `файлы/папки`

## 🎯 Цель

- Цель 1
- Цель 2

## ✅ Критерии Готовности

- [ ] Критерий 1
- [ ] Критерий 2
```

---

## 📝 backend.md - Шаблон

⚠️ **ВАЖНО ДЛЯ GEMINI:**
- Явно указывай ПОЛНЫЕ ПУТИ к файлам
- Используй секцию "📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ"
- Давай примеры кода С КОММЕНТАРИЯМИ
- Указывай точное количество строк

```markdown
# Backend: Название

**Статус:** 🔴 Не начато  
**Время:** 1 час

## 📋 Описание

Что должен сделать Backend.

## 📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ

⚠️ **СОЗДАЙ ЭТИ ФАЙЛЫ:**

### 1. `/orchestrator/имя_файла.py` (~150 строк)

```python
"""
Описание модуля
ЗАЧЕМ: Для чего этот файл
"""

class ClassName:
    """
    Описание класса
    ЗАЧЕМ: Зачем этот класс
    """
    
    def method_name(self, param: Type) -> ReturnType:
        """Описание метода"""
        # Реализация
        pass
```

### 2. `/orchestrator/tests/test_файл.py` (~200 строк)

```python
"""Тесты для имя_файла.py"""

def test_method_name():
    """Тест: что проверяем"""
    # Arrange
    obj = ClassName()
    
    # Act
    result = obj.method_name(param)
    
    # Assert
    assert result == expected
```

## 🔍 CHAIN OF THOUGHT (для Gemini)

⚠️ **ПЕРЕД НАПИСАНИЕМ КОДА ОТВЕТЬ:**

1. **Какие классы/функции уже есть в проекте?**
   - Проверь: есть ли `orchestrator/models.py`?
   - Проверь: есть ли `orchestrator/agents/`?
   - **ЕСЛИ ЕСТЬ** → импортируй, **НЕ СОЗДАВАЙ** свои

2. **Это production код или прототип?**
   - **ВСЕГДА** production
   - **ЗАПРЕЩЕНО**: random, mock данные, фейковая логика

3. **Нужна ли интеграция с другими модулями?**
   - Читай секцию "🔗 Связанные Файлы"
   - Используй реальные импорты

4. **Какие именно файлы создавать?**
   - Читай секцию "📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ"
   - Используй ТОЧНЫЕ пути оттуда

## 📚 СУЩЕСТВУЮЩИЙ КОД ДЛЯ ИСПОЛЬЗОВАНИЯ

### Доступные импорты:

```python
# Модели данных
from orchestrator.models import AgentResult, Task

# Агенты (если нужно вызвать)
from orchestrator.agents.logic_agent import LogicAgentRunner
from orchestrator.agents.backend_agent import BackendAgentRunner
from orchestrator.agents.frontend_agent import FrontendAgentRunner
from orchestrator.agents.tester_agent import TesterAgentRunner

# Утилиты
from pathlib import Path
import asyncio
```

### ❌ НЕ СОЗДАВАЙ mock классы:

```python
# ❌ НЕПРАВИЛЬНО (создаёшь свой класс):
class AgentResult:
    def __init__(self, agent, status):
        self.agent = agent
        self.status = status

# ✅ ПРАВИЛЬНО (импортируешь существующий):
from orchestrator.models import AgentResult
```

### ✅ Примеры реальных вызовов:

```python
# Вызов агента
agent = BackendAgentRunner()
result = await agent.execute("task.md")

# Проверка результата
if result.status == "success":
    print(result.output)
```

## ❌ СТРОГО ЗАПРЕЩЕНО

1. **random, mock, фейковые данные**
   ```python
   # ❌ НЕПРАВИЛЬНО:
   if random.random() > 0.5:
       return success
   
   # ✅ ПРАВИЛЬНО:
   result = await real_function()
   return result
   ```

2. **Создание mock классов**
   ```python
   # ❌ НЕПРАВИЛЬНО:
   class AgentResult: pass  # mock!
   
   # ✅ ПРАВИЛЬНО:
   from orchestrator.models import AgentResult
   ```

3. **Заглушки вместо реализации**
   ```python
   # ❌ НЕПРАВИЛЬНО:
   def execute():
       pass  # TODO: implement
   
   # ✅ ПРАВИЛЬНО:
   def execute():
       # Реальная логика
       return calculate_result()
   ```

## 📝 ЭТАПЫ ВЫПОЛНЕНИЯ

### Этап 1: Анализ (2 минуты)
- Прочитать секцию "🔍 CHAIN OF THOUGHT"
- Ответить на все вопросы
- Определить какие импорты нужны

### Этап 2: Создание структуры (5 минут)
- Создать файлы по путям из "📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ"
- Добавить импорты
- Создать классы/функции с сигнатурами

### Этап 3: Реализация логики (15 минут)
- Написать РЕАЛЬНУЮ логику (не mock!)
- Добавить обработку ошибок
- Добавить комментарии ЗАЧЕМ

### Этап 4: Тесты (10 минут)
- Unit тесты для каждой функции
- Integration тесты для сценариев
- Граничные случаи

### Этап 5: Интеграция (5 минут)
- Интегрировать в указанные файлы
- Проверить что всё работает

## 💻 Примеры Использования

```python
# Как использовать
from orchestrator.имя_файла import ClassName

obj = ClassName()
result = obj.method_name(param)
```

## 🧪 Тесты

### Unit (N шт)

1. **test_название**
   - Что: проверяем X
   - Ожидаем: Y

### Integration (N шт)

1. **test_integration**
   - Сценарий: полный цикл
   - Ожидаем: успех

## ✅ Критерии Готовности

- [ ] Создан файл `/orchestrator/имя_файла.py`
- [ ] Создан файл `/orchestrator/tests/test_файл.py`
- [ ] Все N тестов проходят
- [ ] Код прокомментирован на русском

## 🔗 Связанные Файлы

- `/orchestrator/main_orchestrator.py` - интегрировать сюда
```

---

## 🎯 КАК GEMINI ДОЛЖЕН ЧИТАТЬ backend.md

### ✅ Правильное Понимание

1. **Читай секцию "📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ"**
   - Там ТОЧНЫЕ пути
   - Там примеры кода
   - Там количество строк

2. **СОЗДАВАЙ файлы по ТОЧНЫМ путям**
   - НЕ `/server/src/api_result.ts`
   - ДА `/orchestrator/retry_manager.py`

3. **Используй примеры кода**
   - Копируй структуру
   - Добавляй реализацию
   - Сохраняй комментарии

### ❌ Частые Ошибки Gemini

1. ❌ Создаёт файлы в неправильной папке
   - Решение: ЯВНО указывай `/orchestrator/` в начале

2. ❌ Игнорирует требования
   - Решение: Используй ⚠️ и 📁 эмодзи для важного

3. ❌ Создаёт не те файлы
   - Решение: Список "СОЗДАЙ ЭТИ ФАЙЛЫ:" в начале

---

## 📝 frontend.md - Шаблон

```markdown
# Frontend: Название

**Статус:** 🔴 Не начато

## 📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ

### 1. `/client/src/components/Component.tsx`

```typescript
interface Props {
  // props
}

export const Component: React.FC<Props> = (props) => {
  return <div>...</div>
}
```

## 🔍 CHAIN OF THOUGHT (для Gemini)

⚠️ **ПЕРЕД НАПИСАНИЕМ КОДА ОТВЕТЬ:**

1. **Какие компоненты уже есть?**
   - Проверь: `client/src/components/`
   - **ЕСЛИ ЕСТЬ** → переиспользуй, **НЕ ДУБЛИРУЙ**

2. **Это production компонент или mock?**
   - **ВСЕГДА** production
   - **ЗАПРЕЩЕНО**: фейковые данные, mock API

3. **Какой API эндпоинт использовать?**
   - Читай секцию "📚 СУЩЕСТВУЮЩИЙ КОД"
   - Используй реальные fetch/axios запросы

## 📚 СУЩЕСТВУЮЩИЙ КОД ДЛЯ ИСПОЛЬЗОВАНИЯ

### Доступные компоненты:

```typescript
// UI компоненты
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'

// Hooks
import { useState, useEffect } from 'react'
import { useQuery } from '@tanstack/react-query'
```

### ❌ НЕ используй mock данные:

```typescript
// ❌ НЕПРАВИЛЬНО:
const mockData = [{ id: 1, name: "fake" }]

// ✅ ПРАВИЛЬНО:
const { data } = useQuery({
  queryKey: ['items'],
  queryFn: () => fetch('/api/items').then(r => r.json())
})
```

## ❌ СТРОГО ЗАПРЕЩЕНО

1. **Hardcoded данные вместо API**
   ```typescript
   // ❌ НЕПРАВИЛЬНО:
   const data = [{ id: 1 }, { id: 2 }]  // mock!
   
   // ✅ ПРАВИЛЬНО:
   const { data } = await fetch('/api/items')
   ```

2. **Игнорирование ошибок**
   ```typescript
   // ❌ НЕПРАВИЛЬНО:
   fetch('/api').then(r => r.json())  // нет обработки ошибок!
   
   // ✅ ПРАВИЛЬНО:
   try {
     const data = await fetch('/api')
     if (!data.ok) throw new Error('API error')
   } catch (error) {
     setError(error.message)
   }
   ```

## ✅ Критерии Готовности

- [ ] Компонент создан
- [ ] UI работает
```

---

## 📝 logic.md - Шаблон

```markdown
# Logic: Название

**Статус:** 🔴 Не начато

## 📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ

### 1. `/core/logic/functions.ts`

```typescript
/**
 * Описание функции
 * ЗАЧЕМ: Бизнес-логика
 */
export function pureFunctionName(input: Type): ReturnType {
  // Pure function - без side effects
  return result
}
```

## 🔍 CHAIN OF THOUGHT (для Gemini)

⚠️ **ПЕРЕД НАПИСАНИЕМ КОДА ОТВЕТЬ:**

1. **Есть ли похожие функции в core/?**
   - Проверь: `core/logic/`, `core/validation/`
   - **ЕСЛИ ЕСТЬ** → переиспользуй, **НЕ ДУБЛИРУЙ**

2. **Это pure function?**
   - **ВСЕГДА** чистая функция (no side effects)
   - **ЗАПРЕЩЕНО**: API вызовы, БД, DOM, мутации

3. **Все ли входы типизированы?**
   - TypeScript для ВСЕХ параметров
   - Immutable данные

## 📚 СУЩЕСТВУЮЩИЙ КОД ДЛЯ ИСПОЛЬЗОВАНИЯ

### Доступные утилиты:

```typescript
// Валидация
import { validateEmail, validatePhone } from '@/core/validation'

// Преобразования
import { formatDate, parseJSON } from '@/core/utils'
```

### ❌ НЕ создавай side effects:

```typescript
// ❌ НЕПРАВИЛЬНО (мутация):
function addItem(array: any[], item: any) {
  array.push(item)  // мутация!
  return array
}

// ✅ ПРАВИЛЬНО (immutable):
function addItem<T>(array: T[], item: T): T[] {
  return [...array, item]  // новый массив
}
```

## ❌ СТРОГО ЗАПРЕЩЕНО

1. **Мутации данных**
   ```typescript
   // ❌ НЕПРАВИЛЬНО:
   obj.value = 10  // мутация!
   
   // ✅ ПРАВИЛЬНО:
   const newObj = { ...obj, value: 10 }
   ```

2. **Side effects (API, БД, DOM)**
   ```typescript
   // ❌ НЕПРАВИЛЬНО:
   function calculate() {
     fetch('/api')  // side effect!
   }
   
   // ✅ ПРАВИЛЬНО:
   function calculate(data: Data): Result {
     return data.value * 2  // чистая логика
   }
   ```

3. **random или Date.now()**
   ```typescript
   // ❌ НЕПРАВИЛЬНО:
   function getId() {
     return Math.random()  // недетерминировано!
   }
   
   // ✅ ПРАВИЛЬНО:
   function getId(seed: number): string {
     return hash(seed)  // детерминировано
   }
   ```

## ✅ Критерии Готовности

- [ ] Функции реализованы
- [ ] Все pure functions
- [ ] Покрытие ≥ 90%
```

---

## 📝 tester.md - Шаблон

```markdown
# Tester: Название

**Статус:** 🔴 Не начато

## 🧪 План

### Unit (N шт)
1. Test Backend
2. Test Frontend

### Integration (N шт)
1. Полный цикл

### Manual (N шт)
1. Ручная проверка

## 🔍 CHAIN OF THOUGHT (для Gemini)

⚠️ **ПЕРЕД НАПИСАНИЕМ ТЕСТОВ ОТВЕТЬ:**

1. **Что именно тестируем?**
   - Читай секцию "🔗 Связанные Файлы"
   - Тестируй РЕАЛЬНУЮ логику, не mocks

2. **Какие граничные случаи?**
   - Пустые данные
   - Некорректный ввод
   - Ошибки API/БД

3. **Нужно ли мокировать?**
   - **ДА**: Внешние API, БД
   - **НЕТ**: Саму бизнес-логику!

## ❌ СТРОГО ЗАПРЕЩЕНО

1. **Тесты которые всегда проходят**
   ```python
   # ❌ НЕПРАВИЛЬНО:
   def test_always_pass():
       assert True  # бессмысленный тест!
   
   # ✅ ПРАВИЛЬНО:
   def test_retry_logic():
       result = retry_manager.execute(agent, task)
       assert result.status == "success"
   ```

2. **Мокирование тестируемого кода**
   ```python
   # ❌ НЕПРАВИЛЬНО:
   @patch('module.execute_with_retry')
   def test_retry(mock_retry):
       # Мокаем то что тестируем!
   
   # ✅ ПРАВИЛЬНО:
   @patch('module.external_api')  # Мокаем внешнее
   def test_retry(mock_api):
       result = execute_with_retry()  # Тестируем реальное
   ```

3. **Нет проверки граничных случаев**
   ```python
   # ❌ НЕПРАВИЛЬНО:
   def test_add():
       assert add(2, 3) == 5  # только happy path!
   
   # ✅ ПРАВИЛЬНО:
   @pytest.mark.parametrize("a,b,expected", [
       (2, 3, 5),      # обычный
       (0, 0, 0),      # нули
       (-1, 1, 0),     # отрицательные
       (None, 1, None) # некорректный ввод
   ])
   def test_add(a, b, expected):
       assert add(a, b) == expected
   ```

## ✅ Критерии

- [ ] Все тесты проходят
- [ ] Критерии из README выполнены
```

---

## 🚀 Примеры Правильных Тасок

### Пример: Retry Manager (Backend)

```
retry_manager/
├── README.md
└── backend.md

README.md:
- Статус, описание, цель, критерии

backend.md:
- 📁 СОЗДАТЬ: /orchestrator/retry_manager.py
- 📁 СОЗДАТЬ: /orchestrator/tests/test_retry_manager.py
- Примеры кода для КАЖДОГО файла
- Тесты (5 unit + 2 integration)
```

### Пример: Result Formatter (Backend + Frontend)

```
result_formatter/
├── README.md
├── backend.md
└── frontend.md

backend.md:
- 📁 СОЗДАТЬ: /orchestrator/formatter.py

frontend.md:
- 📁 СОЗДАТЬ: /client/src/components/ResultView.tsx
```

---

## ⚠️ Правила для Gemini

1. **ВСЕГДА читай секцию "📁 ФАЙЛЫ ДЛЯ СОЗДАНИЯ"**
2. **СОЗДАВАЙ файлы по ТОЧНЫМ путям из этой секции**
3. **НЕ придумывай свои пути - используй указанные**
4. **Копируй структуру из примеров кода**
5. **Сохраняй комментарии ЗАЧЕМ на русском**

---

**Версия:** 3.0.0  
**Статус:** ✅ Готово
