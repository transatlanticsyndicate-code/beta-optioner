# Test Environment Configuration
# Используется для test.optioner.online

# Database
DATABASE_URL=postgresql://test_user:test_password_123@localhost:5432/test_optioner

# OpenAI API (используем тот же ключ)
OPENAI_API_KEY=${OPENAI_API_KEY}

# Environment
ENVIRONMENT=test

# Server
HOST=0.0.0.0
PORT=8001

# Redis (если используется)
REDIS_URL=redis://localhost:6379/1

# CORS (разрешаем тестовый домен)
ALLOWED_ORIGINS=https://test.optioner.online,http://localhost:3001
