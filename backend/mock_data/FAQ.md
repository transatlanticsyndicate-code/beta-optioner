# ❓ FAQ - Mock Data

## 1. Как узнать какие данные я вижу - mock или реальные?

### В UI (Frontend)

В правом нижнем углу экрана отображается **индикатор источника данных**:

- 🧪 **Mock Data (Local)** - желтый badge "DEMO"
  - Данные: демо-данные для разработки
  - Могут быть устаревшими
  - Список доступных тикеров показан в индикаторе

- 📊 **IB Client Portal Gateway** - зеленый
  - Данные: реальные от Interactive Brokers
  - Актуальные real-time данные

### Через API

```bash
curl http://localhost:8000/api/data-source/info
```

Ответ:
```json
{
  "source_name": "Mock Data (Local)",
  "environment": "local",
  "is_mock": true,
  "is_production": false,
  "available_tickers": ["SPY", "AAPL", "MSTR"],
  "icon": "🧪",
  "description": "Демо-данные для разработки..."
}
```

---

## 2. Что делать если нет mock данных для нужного тикера?

### Сценарий: Хочу протестировать MSTR локально

**Проблема:** Открываю калькулятор, ввожу MSTR → получаю ошибку "Mock данные не найдены"

**Решение 1: Создать mock данные (1 минута)**

```bash
cd backend
python3 create_mock_data.py MSTR 350
```

Готово! Теперь MSTR доступен локально.

**Решение 2: Использовать существующие тикеры**

Доступные тикеры:
- SPY
- AAPL
- MSTR (после создания)

**Решение 3: Переключиться на IB Gateway**

```bash
# В .env файле
REACT_APP_ENV=production
```

Теперь данные берутся с real-time IB Gateway.

---

## 3. Когда использовать mock данные?

### Используйте mock данные когда:
- ✅ Локальная разработка
- ✅ Тестирование UI
- ✅ Разработка новых функций
- ✅ Демонстрация приложения
- ✅ Тестовый сервер (test.optioner.online)

### Используйте IB Gateway когда:
- ✅ Production сервер
- ✅ Нужны реальные актуальные данные
- ✅ Тестирование с real-time данными
- ✅ Проверка точности Greeks/IV

---

## 4. Как работает автоматический выбор источника?

```
REACT_APP_ENV=local  → Mock Data
REACT_APP_ENV=test   → Mock Data
REACT_APP_ENV=production → IB Gateway
```

Или явно:

```
DATA_SOURCE=mock → Mock Data
DATA_SOURCE=ib   → IB Gateway
```

---

## 5. Mock данные устарели - как обновить?

### Вариант 1: Вручную

Отредактируйте JSON файлы в `backend/mock_data/stocks/`

### Вариант 2: Пересоздать

```bash
python3 create_mock_data.py SPY 685
```

Перезапишет существующие данные с новой ценой.

### Вариант 3: Auto-Capture (планируется)

В будущем production будет автоматически сохранять данные как mock.

---

## 6. Ошибка "Mock file not found"

**Полное сообщение:**
```
❌ Mock данные для TSLA не найдены.

📁 Ожидаемый файл: backend/mock_data/stocks/TSLA.json

💡 Решения:
1. Создайте файл: python3 create_mock_data.py TSLA
2. Используйте существующие тикеры: SPY, AAPL
3. Переключитесь на IB Gateway (REACT_APP_ENV=production)
```

**Что делать:**
Следуйте инструкциям из сообщения ошибки.

---

## 7. Доступные mock тикеры

Посмотреть список:

```bash
ls backend/mock_data/stocks/*.json
```

Или через API:

```bash
curl http://localhost:8000/api/data-source/info | jq '.available_tickers'
```

---

## 8. Разница mock vs real данных

| Параметр | Mock Data | IB Gateway |
|----------|-----------|------------|
| **Цены** | Статичные | Real-time |
| **Greeks** | Расчетные | От IB (с подпиской) |
| **Volume** | Статичный | Real-time |
| **OI** | Статичный | Real-time |
| **Обновление** | Нет | Каждую секунду |
| **Стоимость** | Бесплатно | Может требовать подписку |

---

## 9. Как добавить свой тикер с реальными данными?

### Шаг 1: Создать template

```bash
python3 create_mock_data.py NVDA 450
```

### Шаг 2: Получить real данные от IB

На production:
```bash
# Переключаемся на IB
REACT_APP_ENV=production

# Запрашиваем NVDA
# Данные придут от IB Gateway
```

### Шаг 3: Скопировать в mock (вручную)

Скопируйте значения из IB ответа в `backend/mock_data/stocks/NVDA.json`

---

## 10. UI индикатор не показывается

**Проверьте:**

1. Компонент добавлен в App:

```jsx
import DataSourceIndicator from './components/DataSourceIndicator';

function App() {
  return (
    <>
      <DataSourceIndicator />
      {/* остальной код */}
    </>
  );
}
```

2. Endpoint доступен:

```bash
curl http://localhost:8000/api/data-source/info
```

3. CORS настроен для API

---

**Еще вопросы?** См. `backend/mock_data/README.md`
