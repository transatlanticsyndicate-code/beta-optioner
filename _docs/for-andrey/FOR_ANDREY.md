# 📋 Инструкции для Андрея

## ✅ Что готово на test сервере:

1. **Telegram Auth** - авторизация через Telegram бота работает
2. **Модерация** - админ может одобрять/отклонять пользователей через бота
3. **База данных** - PostgreSQL с таблицами users и auth_logs
4. **Frontend** - обновленный UI с авторизацией

## 🔄 Нужно ли тебе обновлять локальный код?

### Вариант А: **Не трогать (рекомендуется)**
Если ты работаешь только с frontend UI (компоненты, стили) и не меняешь логику авторизации:
- **НЕ НУЖНО** обновлять код
- Продолжай работать в своей ветке
- Твои изменения UI не конфликтуют с авторизацией

### Вариант Б: **Обновить (если нужно тестировать авторизацию)**
Если хочешь протестировать полный флоу авторизации локально:

```bash
# 1. Закоммить свои изменения
git add .
git commit -m "WIP: мои изменения UI"

# 2. Получить последние изменения
git fetch origin
git pull origin main

# 3. Если есть конфликты - разрешить их
# (обычно в package-lock.json - можно просто взять версию из main)

# 4. Установить зависимости (если были обновления)
cd frontend
npm install
```

## ⚠️ Важно:

1. **Твой UI workflow** (`/andrey-push-test`) **НЕ ИЗМЕНИЛСЯ** - работает как прежде
2. **Backend изменения** не влияют на твою работу с UI
3. **Если есть вопросы** - пиши Левону

## 📝 Что изменилось в main:

### Backend:
- `backend/app/routers/telegram_webhook.py` - Telegram бот с получением фото профиля
- `backend/app/routers/auth.py` - эндпоинт авторизации через Telegram
- `backend/app/services/telegram_auth.py` - сервис авторизации и модерации
- `backend/app/models/user.py` - модели User и AuthLog

### Frontend:
- `frontend/src/components/AuthModal.jsx` - модальное окно авторизации (упрощенное)
- `frontend/src/components/UserMenu.jsx` - меню пользователя с аватаркой

### Инфраструктура:
- `.windsurf/workflows/quick-deploy.md` - новый workflow быстрого деплоя
- `scripts/quick-deploy-test.sh` - скрипт умного деплоя
- `.github/workflows/deploy-test.yml` - отключен автодеплой

## 🎯 Рекомендация:

**НЕ ОБНОВЛЯЙ КОД** если работаешь только с UI. Когда закончишь свою фичу - тогда смержим.

Если возникнут конфликты при мерже твоей ветки:
1. Левон поможет их разрешить
2. Или используй: `git merge main --strategy-option theirs` для автоматического разрешения (взять версию из main для конфликтов)
