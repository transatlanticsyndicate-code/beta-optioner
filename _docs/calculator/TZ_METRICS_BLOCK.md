# ТЗ: Блок "Метрики опционов" (metrics-block)

## 📋 Общая концепция

Блок показывает **агрегированные метрики всей стратегии** (суммарные по всем выбранным опционам).

Цель: дать трейдеру полную картину риска, прибыли и греков его позиции.

---

## 🎯 Структура блока: 3 секции

### Секция 1: Риск/Прибыль

Ключевые финансовые метрики стратегии:

- **MAX убыток** - максимальный возможный убыток (красным цветом)
- **MAX прибыль** - максимальная возможная прибыль (зеленым) или ∞ для неограниченной прибыли
- **Break-even** - точка безубыточности (цена актива при P&L = 0)
- **Risk/Reward** - соотношение риска к прибыли (например, 1:1.5)

**Пример отображения:**
```
MAX убыток: -$245.27
MAX прибыль: $255.90
Break-even: $220.50
Risk/Reward: 1:1.04
```

**Расчет:**
- MAX убыток: минимальное значение P&L на графике
- MAX прибыль: максимальное значение P&L на графике
- Break-even: цена актива, где P&L = 0
- Risk/Reward: |MAX прибыль| / |MAX убыток|

---

### Секция 2: Greeks (агрегированные)

Суммарные греки всей позиции:

- **Дельта (Δ)** - суммарная дельта всех опционов (направленная экспозиция)
  - Положительная = ставка на рост
  - Отрицательная = ставка на падение
  
- **Гамма (Γ)** - суммарная гамма (ускорение дельты при изменении цены)
  - Положительная = дельта растет при росте цены
  - Отрицательная = дельта падает при росте цены
  
- **Тета (Θ)** - суммарная тета (временной распад в $ за день)
  - Отрицательная = позиция теряет стоимость со временем
  - Положительная = позиция набирает стоимость со временем
  
- **Вега (ν)** - суммарная вега (чувствительность к волатильности)
  - Положительная = выигрываем от роста волатильности
  - Отрицательная = проигрываем от роста волатильности

**Пример отображения:**
```
Дельта: +0.43 (бычья позиция)
Гамма: +2.04
Тета: -19.04 (теряем $19/день)
Вега: +23.1
```

**Расчет:**
- Для каждого опциона: греки × количество × направление (Buy = +1, Sell = -1)
- Суммируем по всем опционам

---

### Секция 3: Капитал и Премия

Финансовые требования:

- **Общая премия** - сколько заплатили/получили
  - Дебет (отрицательная) = заплатили
  - Кредит (положительная) = получили
  
- **Требуемый капитал** - сколько нужно денег для открытия позиции
  
- **Маржа** - требования по марже (если применимо)
  - Для спредов и продажи опционов
  
- **Дней до экспирации** - сколько осталось до истечения ближайшего опциона

**Пример отображения:**
```
Премия: -$2,000 (дебет)
Требуется: $2,000
Маржа: $0
Дней до экспирации: 45
```

**Расчет:**
- Премия: сумма всех премий с учетом Buy/Sell
- Требуемый капитал: |Премия| + Маржа
- Дней до экспирации: минимальная дата среди всех опционов

---

## 📊 Дополнение: Greeks в таблице опционов

### Проблема
Трейдеру важно видеть Greeks не только агрегированные, но и **по каждому опциону отдельно**.

### Решение
Добавить в таблицу опционов (options-block) дополнительные колонки с Greeks.

### Структура таблицы с Greeks

| Тип | Дата | Страйк | КК | Премия | Bid | Ask | Объем | OI | **Δ** | **Γ** | **Θ** | **ν** |
|-----|------|--------|----|----|-----|-----|-------|----|----|----|----|-----|
| Buy CALL | 24.10 | 220 | 1 | $24.90 | $24.50 | $25.40 | 2,014 | 9,723 | **0.65** | **0.03** | **-0.45** | **0.12** |
| Sell PUT | 31.1 | 200 | -1 | $5.20 | $5.00 | $5.40 | 1,500 | 5,000 | **-0.35** | **-0.02** | **+0.30** | **-0.08** |

**Итого (суммарные):** Δ: +0.30, Γ: +0.01, Θ: -0.15, ν: +0.04

### Особенности отображения
- Мелкий шрифт для компактности
- Цветовое кодирование:
  - Зеленый = положительное значение
  - Красный = отрицательное значение
- Показывать только при наличии данных (после загрузки деталей опциона)
- Можно скрывать колонки через настройки (опционально)

---

## 🎨 Визуальное оформление

### Блок "Метрики опционов" (metrics-block)

**Концепция: Горизонтальный скролл с индикаторами**

Все метрики отображаются в одну строку с возможностью горизонтального скролла.
Иконки-индикаторы (ChevronLeft/ChevronRight) показывают наличие дополнительного контента.

**Визуальная структура:**
```
┌─────────────────────────────────────────────────────────────┐
│ ← [MAX убыток] [MAX прибыль] [Break-even] [Risk/Reward] → │
│    -$245.27     $255.90       $220.50      1:1.04           │
└─────────────────────────────────────────────────────────────┘
```

**Логика индикаторов:**
- **Левая стрелка (←):** показывается, если можно скроллить влево
- **Правая стрелка (→):** показывается, если можно скроллить вправо
- **Обе стрелки:** показываются в середине контента
- **Нет стрелок:** весь контент помещается на экране

**Примеры состояний:**
```
Начало:  [Метрика 1] [Метрика 2] [Метрика 3] →
         ↑ нет стрелки слева

Середина: ← [Метрика 3] [Метрика 4] [Метрика 5] →
          ↑ обе стрелки

Конец:   ← [Метрика 10] [Метрика 11] [Метрика 12]
         ↑ нет стрелки справа
```

### Приоритет метрик (слева направо)

**Уровень 1: Критические (всегда видны)**
1. **MAX убыток** - самое важное для риск-менеджмента
2. **MAX прибыль** - потенциал сделки
3. **Break-even** - точка безубыточности
4. **Risk/Reward** - соотношение риск/прибыль

**Уровень 2: Важные (видны при небольшом скролле)**
5. **Премия** - сколько платим/получаем
6. **Требуется капитал** - сколько нужно денег
7. **Дней до экспирации** - временной горизонт
8. **Вероятность прибыли** - шанс на успех (если доступно)

**Уровень 3: Продвинутые (для опытных, справа)**
9. **Дельта (Δ)** - направленность позиции
10. **Тета (Θ)** - временной распад
11. **Вега (ν)** - чувствительность к волатильности
12. **Гамма (Γ)** - ускорение дельты
13. **Маржа** - требования брокера

### Цветовое кодирование
- **Красный** (#ef5350) - убыток, риск, отрицательные значения
- **Зеленый** (#26a69a) - прибыль, положительные значения
- **Синий** (#2962FF) - нейтральные метрики (Break-even, дни)
- **Серый** (#666) - метки и описания

### Дополнительные визуальные элементы
- **Градиент по краям** - показывает наличие скрытого контента
- **Snap scroll** - метрики "прилипают" при скролле для удобства
- **Smooth scroll** - плавная анимация скролла

---

## 🔧 Техническая реализация

### Источники данных

1. **Риск/Прибыль:**
   - Рассчитывается на основе графика P&L
   - Используем существующую функцию `calculatePL()`

2. **Greeks:**
   - Загружаются через API: `/api/polygon/ticker/{ticker}/option-details`
   - Параметры: expiration_date, strike, option_type
   - Возвращает: delta, gamma, theta, vega

3. **Капитал:**
   - Премия: сумма всех премий из таблицы опционов
   - Маржа: расчет по формулам (зависит от типа стратегии)

### Компоненты

```
OptionsMetrics.jsx (главный компонент)
├── MetricCard.jsx (отдельная карточка метрики)
├── ScrollIndicator.jsx (иконки-индикаторы)
└── useScrollIndicators.js (хук для управления индикаторами)
```

### Props для OptionsMetrics

```javascript
{
  options: [],           // Массив опционов
  currentPrice: 245.27,  // Текущая цена актива
  plData: {},           // Данные графика P&L
}
```

### Структура компонента MetricCard

```javascript
<MetricCard
  priority={1}           // 1-3 (приоритет отображения)
  label="MAX убыток"     // Название метрики
  value="-$245.27"       // Значение
  color="red"            // red, green, blue, gray
  tooltip="Максимальный возможный убыток" // Подсказка
/>
```

### Хук useScrollIndicators

```javascript
const {
  canScrollLeft,    // Можно ли скроллить влево
  canScrollRight,   // Можно ли скроллить вправо
  scrollLeft,       // Функция скролла влево
  scrollRight,      // Функция скролла вправо
  scrollRef         // Ref для контейнера
} = useScrollIndicators();
```

### CSS структура

```css
.metrics-scroll-container {
  position: relative;
  width: 100%;
}

.metrics-scroll {
  display: flex;
  gap: 12px;
  overflow-x: auto;
  scroll-behavior: smooth;
  scroll-snap-type: x mandatory;
  scrollbar-width: none; /* Firefox */
  -ms-overflow-style: none; /* IE/Edge */
  padding: 16px 0;
}

.metrics-scroll::-webkit-scrollbar {
  display: none; /* Chrome/Safari */
}

.metric-card {
  scroll-snap-align: start;
  min-width: 140px;
  flex-shrink: 0;
}

.scroll-indicator {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  z-index: 10;
  background: white;
  border-radius: 50%;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  padding: 8px;
  cursor: pointer;
  transition: opacity 0.2s;
}

.scroll-indicator.left {
  left: -12px;
}

.scroll-indicator.right {
  right: -12px;
}

/* Градиенты по краям */
.metrics-scroll-container::before,
.metrics-scroll-container::after {
  content: '';
  position: absolute;
  top: 0;
  bottom: 0;
  width: 40px;
  pointer-events: none;
  z-index: 5;
}

.metrics-scroll-container::before {
  left: 0;
  background: linear-gradient(to right, white, transparent);
}

.metrics-scroll-container::after {
  right: 0;
  background: linear-gradient(to left, white, transparent);
}
```

---

## 🚀 Приоритет реализации

### Этап 1 (MVP) - Высокий приоритет ✅ ГОТОВО
1. ✅ **Секция 1: Риск/Прибыль** (MAX убыток, MAX прибыль, Break-even, Risk/Reward)
2. ✅ **Секция 2: Greeks агрегированные** (Δ, Γ, Θ, ν) - с загрузкой с Polygon API
3. ✅ **Базовый UI** (горизонтальный скролл, индикаторы, чистый HTML)
4. ✅ **Секция 3: Капитал** (Премия, Маржин, Дни до экспирации)
5. ✅ **Реактивность** (пересчет при изменении опционов)
6. ✅ **Tooltip'ы с пояснениями** (иконки помощи для каждой метрики)
7. ✅ **Загрузка Greeks с API** (mock данные для тестирования)

### Этап 2 - Средний приоритет ✅ ГОТОВО
8. ✅ **Маржинальные требования** (расчет по формулам для разных стратегий)
9. ✅ **Динамический расчет P&L метрик** (MAX убыток, MAX прибыль, Break-even)

### Этап 3 - Низкий приоритет
11. ⏳ **Настройки отображения** (скрыть/показать секции)
12. ⏳ **Анимации и переходы** (плавные эффекты)
13. ⏳ **Продвинутые метрики** (Вероятность прибыли, Rho, Charm)

---

## 📝 Примечания

### Формулы расчета Greeks для стратегии

```javascript
// Для каждого опциона
const optionGreeks = {
  delta: option.delta * option.quantity * (option.type === 'Buy' ? 1 : -1),
  gamma: option.gamma * option.quantity * (option.type === 'Buy' ? 1 : -1),
  theta: option.theta * option.quantity * (option.type === 'Buy' ? 1 : -1),
  vega: option.vega * option.quantity * (option.type === 'Buy' ? 1 : -1),
}

// Суммируем по всем опционам
const totalGreeks = options.reduce((acc, option) => ({
  delta: acc.delta + optionGreeks.delta,
  gamma: acc.gamma + optionGreeks.gamma,
  theta: acc.theta + optionGreeks.theta,
  vega: acc.vega + optionGreeks.vega,
}), { delta: 0, gamma: 0, theta: 0, vega: 0 })
```

### Особые случаи

1. **Нет опционов:** показать заглушку "Добавьте опционы для отображения метрик"
2. **Один опцион:** показать метрики, но подсветить, что это один опцион
3. **Неограниченная прибыль:** показать ∞ вместо числа
4. **Нет данных по Greeks:** показать "—" или "Загрузка..."

### Реактивность блока метрик

**Блок автоматически пересчитывается при:**

1. **Добавлении опциона** - новые метрики включаются в расчет
2. **Изменении количества опциона** - пропорционально меняются все метрики
3. **Удалении опциона** - метрики пересчитываются без этого опциона
4. **Нажатии глазика (visible/hidden)** - скрытые опционы исключаются из расчета
5. **Изменении параметров опциона** - дата, страйк, премия влияют на метрики

**Техническая реализация:**
```javascript
// Блок реагирует на изменения через useMemo
const calculatedMetrics = useMemo(() => {
  const visibleOptions = options.filter(opt => opt.visible !== false);
  // Пересчет при любом изменении массива options
}, [options]);
```

### Загрузка Greeks с Polygon API

**Проблема:** Greeks показывают 0, если не загружены с API

**Причины нулевых Greeks:**
1. **Страйк не выбран** - API не вызывался
2. **API не вернул данные** - опцион неликвидный или ошибка
3. **Опцион неполный** - нет даты, страйка или премии

**Момент загрузки Greeks:**
- ✅ **Сразу после выбора страйка** в функции `loadOptionDetails()`
- ✅ **Автоматически** при добавлении полного опциона
- ✅ **С сохранением в опцион** для дальнейшего использования

**Endpoint:** `GET /api/polygon/ticker/{ticker}/option-details`
**Параметры:** `expiration_date`, `strike`, `option_type`
**Результат:** `{ delta, gamma, theta, vega, impliedVolatility }`

**Код загрузки:**
```javascript
// В OptionsCalculatorV2.jsx - функция уже реализована
const loadOptionDetails = async (optionId, ticker, date, strike, optionType) => {
  // Загружаем Greeks и сохраняем в опцион
  setOptions(prevOptions => 
    prevOptions.map(opt => 
      opt.id === optionId ? {
        ...opt,
        delta: details.delta || 0,
        gamma: details.gamma || 0,
        theta: details.theta || 0,
        vega: details.vega || 0,
      } : opt
    )
  );
};
```

### Динамический расчет P&L метрик

**Проблема:** Текущие значения MAX убыток, MAX прибыль, Break-even статические и не отражают реальную стратегию.

**Решение:** Реализовать математический расчет P&L для всего диапазона цен актива.

#### Алгоритм расчета:

```javascript
// 1. Генерируем диапазон цен (±20% от текущей цены)
const priceRange = 0.20;
const minPrice = currentPrice * (1 - priceRange);
const maxPrice = currentPrice * (1 + priceRange);
const prices = generatePriceArray(minPrice, maxPrice, 200); // 200 точек

// 2. Для каждой цены рассчитываем общий P&L всех опционов
const plData = prices.map(price => {
  let totalPL = 0;
  
  options.forEach(option => {
    const { type, strike, action, quantity, premium } = option;
    let optionPL = 0;
    
    if (type === 'CALL') {
      const intrinsicValue = Math.max(0, price - strike);
      if (action === 'Buy') {
        optionPL = (intrinsicValue - premium) * quantity * 100;
      } else { // Sell
        optionPL = (premium - intrinsicValue) * Math.abs(quantity) * 100;
      }
    } else { // PUT
      const intrinsicValue = Math.max(0, strike - price);
      if (action === 'Buy') {
        optionPL = (intrinsicValue - premium) * quantity * 100;
      } else { // Sell
        optionPL = (premium - intrinsicValue) * Math.abs(quantity) * 100;
      }
    }
    
    totalPL += optionPL;
  });
  
  return { price, pl: totalPL };
});

// 3. Извлекаем метрики из массива P&L
const maxLoss = Math.min(...plData.map(p => p.pl));
const maxProfit = Math.max(...plData.map(p => p.pl));
const breakevens = plData.filter(p => Math.abs(p.pl) < 50).map(p => p.price);
const riskReward = maxProfit === Infinity ? '∞' : `1:${Math.abs(maxProfit / maxLoss).toFixed(2)}`;
```

#### Особенности реализации:

1. **Диапазон цен:** ±20% от текущей цены (как в PLChart)
2. **Точность:** 200 точек для плавного расчета
3. **Break-even:** Цены где |P&L| < $50 (допустимая погрешность)
4. **Неограниченная прибыль:** Проверка на бесконечность для стратегий типа Long Call
5. **Множественные break-even:** Поддержка сложных стратегий (Iron Condor, Butterfly)

#### Преимущества подхода:

- **✅ Независимость:** Не зависит от PLChart компонента
- **✅ Реактивность:** Автоматический пересчет при изменении опционов
- **✅ Точность:** Использует ту же математику, что и график
- **✅ Сложные стратегии:** Корректно обрабатывает любые комбинации
- **✅ Производительность:** Расчет выполняется в useMemo

#### Функции для реализации:

```javascript
// В metricsCalculator.js
export function calculatePLMetrics(options, currentPrice) {
  // Возвращает { maxLoss, maxProfit, breakevens, riskReward }
}

export function calculateOptionPL(option, price) {
  // Рассчитывает P&L одного опциона при заданной цене
}

export function findBreakevens(plData, tolerance = 50) {
  // Находит точки безубыточности с заданной точностью
}
```

### Блюр (размытие блока)

**Условие отображения блюра:**
```javascript
{(!selectedTicker || displayOptions.length === 0) && (
  <div className="absolute inset-0 bg-white/50 backdrop-blur-sm z-10 rounded-lg pointer-events-none" />
)}
```

**Логика:**
- Блюр показывается, если **НЕ выбран тикер** ИЛИ **нет опционов**
- Блюр снимается только когда **выбран тикер И добавлен хотя бы один опцион**
- Это правильно, потому что блок метрик бесполезен без опционов

**Источник:** Проверено по коду в `OptionsCalculatorV2.jsx` (строка 817)

---

## ✅ Критерии готовности

### Этап 1 (MVP) - ✅ ВЫПОЛНЕНО
- [x] **Блок отображается в режиме Advanced** - работает
- [x] **Все 3 секции работают корректно** - Риск/Прибыль, Greeks, Капитал
- [x] **Greeks рассчитываются правильно** - с загрузкой с Polygon API + mock данные
- [x] **Цветовое кодирование работает** - красный/зеленый/синий/серый
- [x] **Горизонтальный скролл** - с индикаторами и snap scroll
- [x] **Реактивность** - пересчет при изменении опционов
- [x] **Tooltip'ы** - иконки помощи с пояснениями
- [x] **Нет ошибок в консоли** - проверено
- [x] **Производительность** - рендер быстрый благодаря useMemo

### Этап 2 - ✅ ВЫПОЛНЕНО
- [x] **Маржинальные требования** - точные формулы для стратегий
- [x] **Динамический расчет P&L метрик** - реальные MAX убыток/прибыль/Break-even

--

**Дата создания:** 15 октября 2025  
**Дата завершения MVP:** 15 октября 2025  
**Дата завершения Этап 2:** 15 октября 2025  
**Статус:** ✅ ПОЛНОСТЬЮ ГОТОВ  
**Ответственный:** Cascade AI
