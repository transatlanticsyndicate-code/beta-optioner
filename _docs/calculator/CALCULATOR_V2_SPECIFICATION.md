# Техническое задание: Калькулятор опционов V2

## Общая информация

**Цель:** Создание нового калькулятора опционов с улучшенным UI/UX на базе опыта предыдущего калькулятора.

**Базовый калькулятор:** `/tools/options-calculator`  
**Новый калькулятор:** `/tools/options-calculator-v2`

**Эталон дизайна:** https://v0-financial-dashboard-seven-peach-31.vercel.app/calculators-new

---

## Структура блоков калькулятора

### Левая колонка (основная, flex-[3])

#### 1. Блок "Позиции базового актива" (w-[404px])

**Начальное состояние (без тикера):**
- Только поле поиска тикера с автокомплитом
- Плейсхолдер: "Введите тикер"
- **Все остальные блоки покрыты blur overlay** (backdrop-blur + opacity)

**После выбора тикера:**
- **Blur overlay исчезает** с блоков "Опционы" и "Календарь экспирации"
- Отображение текущей цены и изменения (справа от поля)
- Появляется секция "Позиции базового актива"
- Кнопка "Добавить" для создания позиций (LONG/SHORT)
- Если есть сохраненные позиции для этого тикера:
  - Показывается кнопка "Загрузить (N)" где N - количество позиций
  - При клике загружаются все сохраненные позиции
- Список добавленных позиций с кнопками управления

**Кеширование позиций:**
- Все добавленные позиции автоматически сохраняются в localStorage
- Привязка к тикеру
- История работы с каждым тикером

#### 2. Блок "Опционы" (flex-1)
- **Начальное состояние:** Покрыт blur overlay
- **После выбора тикера:** 
  - Blur исчезает
  - Показывается пустой блок с кнопками управления
  - Заголовок и кнопки: "Добавить опцион", "Выбрать стратегию", "Сохранить"
  - Пустая таблица (пользователь еще не добавил опционы)

#### 3. Блок "Календарь экспирации" (w-full)
- **Начальное состояние:** Покрыт blur overlay
- **После выбора тикера:**
  - Blur исчезает
  - Блок пустой с заголовком "Даты экспираций"
  - Показывается прогресс-бар (2px cyan линия по верхнему краю, как border-top)
  - Все даты экспирации загружаются с Polygon в фоне
  - После загрузки появляется календарь со всеми датами
  - Горизонтальный скролл с датами
  - Группировка по месяцам
  - Выбор активной даты

#### 4. Блок "Шкала цен" (w-full)
- Верхняя шкала (зеленые полосы - прибыль)
- Нижняя шкала (красные полосы - убыток)
- Горизонтальный скролл
- Интерактивное перетаскивание

#### 5. Блок "Метрики опционов" (w-full)
- MAX убыток
- MAX прибыль
- Точка безубытка
- Всего премии
- Риск/Прибыль
- Маржин
- Дельта (Δ)
- Гамма (Γ)
- Тета (Θ)
- Вега (ν)

#### 6. Блок "График прибыли/убытка" (w-full)
- Визуализация P&L
- Интерактивный график

#### 7. Блок "Доска опционов (Options Chain)" (w-full)
- Таблица с двумя секциями: Calls (слева) и Puts (справа)
- Колонки для Calls:
  - Time value, Rho, Vega, Theta, Gamma, Delta, Price, Ask, Bid, Volume, Strike
- Колонки для Puts:
  - Strike, Volume, Bid, Ask, Price, Delta, Gamma, Theta, Vega, Rho, Time value
- Центральная колонка Strike выделена
- Цветовые индикаторы:
  - Синие полосы для Calls (объем)
  - Красные полосы для Puts (объем)
- Сортировка по колонкам
- Подсветка строк при наведении
- Библиотека: AG Grid Community

---

### Правая колонка (flex-1)

#### 8. Блок "TradingView виджет" (h-[330px])
- Встроенный виджет графика
- Отображение выбранного тикера

#### 9. Блок "Калькулятор риска"
- 6 кнопок выбора тренда:
  - Медвежий вниз-влево
  - Медвежий волнистый
  - Нейтральный
  - Диапазон
  - Бычий волнистый
  - Бычий вверх-вправо
- Поле "Целевой уровень"
- Поле "Ограничить риск"
- Слайдер "вероятность"
- Кнопка "Показать подходящие стратегии"

#### 10. Блок "Настройки калькулятора"
- Дней до экспирации (слайдер)
- Волатильность (слайдер)
- Отображать график как:
  - Прибыль/убыток в $
  - Прибыль/убыток в %
  - Стоимость контракта
  - % от максимального риска

---

## Технологии

- **UI Framework:** React + Tailwind CSS
- **UI Components:** shadcn/ui
- **Таблицы:** AG Grid Community
- **Графики:** TradingView Widget
- **State Management:** React Hooks
- **Хранилище данных:** localStorage

## Работа с данными

### localStorage структура:

```javascript
// Ключ: 'calculator_positions'
{
  positions: [
    {
      id: "1697123456789",
      ticker: "AAPL",
      type: "LONG", // или "SHORT"
      quantity: 100,
      price: 175.50,
      createdAt: "2024-10-14T08:00:00.000Z"
    }
  ]
}
```

### Утилиты для работы с позициями:

- `getPositions()` - получить все позиции
- `addPosition(ticker, type, quantity, price)` - добавить позицию
- `removePosition(id)` - удалить позицию
- `updatePosition(id, updates)` - обновить позицию
- `getPositionsByTicker(ticker)` - получить позиции по тикеру
- `getUniqueTickers()` - получить список уникальных тикеров
- `clearPositions()` - очистить все позиции

### Логика работы:

1. При открытии калькулятора загружаются позиции из localStorage
2. При выборе тикера (например, AAPL):
   - Проверяется наличие позиций по этому тикеру
   - Если есть - показывается подсказка: "У вас есть сохраненные позиции"
   - Кнопка "Загрузить мои позиции" для быстрого добавления
3. При добавлении новой позиции - автоматическое сохранение в localStorage
4. При удалении позиции - обновление localStorage

## Следующие шаги

1. ✅ Создана базовая структура блоков (10 блоков)
2. ✅ Установлена библиотека AG Grid для доски опционов
3. ✅ Создана утилита для работы с localStorage (portfolioStorage.js)
4. ✅ Описана логика кеширования позиций
5. 🔄 Детальное описание функциональности каждого блока
6. ⏳ Интеграция утилит в компоненты
7. ⏳ Интеграция с данными из старого калькулятора
8. ⏳ Реализация интерактивности
9. ⏳ Тестирование и оптимизация

---

## Примечания

- Используется опыт и логика из `/tools/options-calculator`
- UI/UX соответствует эталону Андрея
- Все блоки адаптивны и работают в темной/светлой теме
- Padding блоков: 20px (основные), 16px (вложенные)

## Обзор
Новая версия опционного калькулятора с улучшенным UX и интеграцией с Polygon API.
Поддерживает 3 версии: Базовая, Продвинутая, Профессиональная.

## Основные изменения от V1
1. **Новый дизайн** - современный интерфейс с blur эффектами
2. **Реальные данные** - интеграция с Polygon API
3. **Улучшенный UX** - плавные переходы и состояния загрузки
4. **Модульная архитектура** - переиспользуемые компоненты
5. **Версионность** - 3 уровня функциональности

## Версии калькулятора

### 🟢 Базовая версия (Basic)
**Для начинающих трейдеров**
- Выбор тикера без позиций актива
- Блок управления опционами
- Календарь дат экспирации  
- График прибыли/убытка

### 🟡 Продвинутая версия (Advanced)
**Для опытных трейдеров**
- Все из базовой версии
- Позиции базового актива в блоке выбора тикера
- Блок метрик (MAX убыток/прибыль, Greeks)
- TradingView виджет актива

### 🔴 Профессиональная версия (Professional)
**Для профессиональных трейдеров**
- Все из продвинутой версии
- Доска опционов (Options Chain)
- Шкала страйков
- Конструктор подходящих стратегий
- Блок настройки калькулятора
- Блок рекомендаций

## Конфигурация версий

### Файл конфигурации
`/frontend/src/config/calculatorV2Blocks.js` - управляет отображением блоков для каждой версии.

### Переключение версий
```javascript
const ACTIVE_PRESET = 'professional'; // 'basic', 'advanced', 'professional'
```

### Структура блоков по версиям

#### 🟢 Базовая версия (Basic)
**Блоки:**
1. `ticker-selector-basic` - Выбор тикера без позиций актива
2. `options-block` - Управление опционами
3. `expiration-dates` - Календарь экспирации
4. `pl-chart` - График P&L

#### 🟡 Продвинутая версия (Advanced)
**Дополнительные блоки:**
5. `ticker-selector-advanced` - Выбор тикера с позициями актива
6. `metrics-block` - Метрики и Greeks
7. `tradingview-widget` - TradingView виджет

#### 🔴 Профессиональная версия (Professional)
**Дополнительные блоки:**
8. `calculator-settings` - Настройки (header)
9. `strike-scale` - Шкала страйков
10. `options-chain` - Доска опционов
11. `strategy-builder` - Конструктор стратегий
12. `recommendations` - Блок рекомендаций

### Условия отображения блоков
- `requiresTicker: true` - блок показывается только после выбора тикера
- `requiresPositions: true` - блок показывается только при наличии позиций
- `enabled: true/false` - включен/выключен блок

### Использование конфигуратора

#### В компоненте Layout:
```javascript
import { getActiveBlocks, getCurrentPreset } from '../config/calculatorV2Blocks';

const CalculatorV2Layout = ({ selectedTicker, hasPositions }) => {
  const headerBlocks = getActiveBlocks('header');
  const mainBlocks = getActiveBlocks('main');
  
  // Рендер блоков с условиями
  const shouldShowBlock = (block) => {
    if (block.requiresTicker && !selectedTicker) return false;
    if (block.requiresPositions && !hasPositions) return false;
    return true;
  };
};
```

#### Переключение версий:
```javascript
// В файле конфигурации
const ACTIVE_PRESET = 'basic'; // 'advanced', 'professional'
```

### Созданные файлы:
- `/frontend/src/config/calculatorV2Blocks.js` - конфигуратор блоков
- `/frontend/src/components/CalculatorV2Layout.jsx` - основной layout
- `/frontend/src/components/calculator/VersionSwitcher.jsx` - переключатель версий

## Технические требования
