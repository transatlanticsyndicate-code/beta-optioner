# 🎛️ Руководство по пресетам калькулятора

**Файл конфигурации:** `/frontend/src/config/calculatorBlocks.js`

---

## 📋 Доступные пресеты

### 1. **MINIMAL** - Минимальная версия

**Описание:** Только самое необходимое для быстрого расчета P&L

**Блоки:**
- **Header:** Пусто
- **Left:**
  - Ticker Selector (выбор тикера)
  - Position Form (форма добавления позиции)
  - Positions List (список позиций)
- **Right:**
  - P&L Chart (график прибыли/убытка)

**Когда использовать:**
- Быстрый расчет P&L
- Минималистичный интерфейс
- Для опытных пользователей

---

### 2. **STANDARD** - Стандартная версия

**Описание:** Оптимальный баланс функционала

**Блоки:**
- **Header:** Пусто
- **Left:**
  - Ticker Selector
  - **Strategy Presets** (готовые стратегии) ✨
  - Position Form
  - Positions List
- **Right:**
  - **Position Summary** (сводка и Greeks) ✨
  - P&L Chart

**Когда использовать:**
- Рекомендуется по умолчанию
- Баланс между простотой и функционалом
- Для большинства пользователей

---

### 3. **FULL** - Полная версия

**Описание:** Все возможности калькулятора

**Блоки:**
- **Header:**
  - **Commission Settings** (настройки комиссий) ✨
- **Left:**
  - Ticker Selector
  - Strategy Presets
  - Position Form
  - Positions List
- **Right:**
  - Position Summary
  - P&L Chart
  - **AI Chat** (AI ассистент) ✨

**Когда использовать:**
- Продвинутые пользователи
- Нужны все настройки
- Работа с AI ассистентом

---

## 🔧 Как переключить пресет

### Способ 1: Изменить константу (текущий)

Откройте файл `/frontend/src/config/calculatorBlocks.js` и измените:

```javascript
const ACTIVE_PRESET = 'full'; // Измените на: 'minimal', 'standard' или 'full'
```

**Примеры:**
```javascript
const ACTIVE_PRESET = 'minimal';   // Минимальная версия
const ACTIVE_PRESET = 'standard';  // Стандартная версия
const ACTIVE_PRESET = 'full';      // Полная версия
```

---

## 📊 Сравнительная таблица

| Блок | Minimal | Standard | Full |
|------|---------|----------|------|
| **Header** |
| Commission Settings | ❌ | ❌ | ✅ |
| **Left Column** |
| Ticker Selector | ✅ | ✅ | ✅ |
| Strategy Presets | ❌ | ✅ | ✅ |
| Position Form | ✅ | ✅ | ✅ |
| Positions List | ✅ | ✅ | ✅ |
| **Right Column** |
| Position Summary | ❌ | ✅ | ✅ |
| P&L Chart | ✅ | ✅ | ✅ |
| AI Chat | ❌ | ❌ | ✅ |
| **Итого блоков** | **4** | **6** | **8** |

---

## 🎨 Визуальное сравнение

### MINIMAL
```
┌─────────────────────────────────────────┐
│ Options Calculator                      │
├──────────────────┬──────────────────────┤
│ Ticker           │ P&L Chart            │
│ Position Form    │                      │
│ Positions List   │                      │
└──────────────────┴──────────────────────┘
```

### STANDARD
```
┌─────────────────────────────────────────┐
│ Options Calculator                      │
├──────────────────┬──────────────────────┤
│ Ticker           │ Position Summary     │
│ Strategy Presets │ P&L Chart            │
│ Position Form    │                      │
│ Positions List   │                      │
└──────────────────┴──────────────────────┘
```

### FULL
```
┌─────────────────────────────────────────┐
│ Options Calculator          [Settings]  │
├──────────────────┬──────────────────────┤
│ Ticker           │ Position Summary     │
│ Strategy Presets │ P&L Chart            │
│ Position Form    │ AI Chat              │
│ Positions List   │                      │
└──────────────────┴──────────────────────┘
```

---

## 🔍 Детали реализации

### Структура пресета

```javascript
const PRESET_EXAMPLE = {
  header: ['block-id-1', 'block-id-2'],  // Блоки в header
  left: ['block-id-3', 'block-id-4'],    // Блоки в левой колонке
  right: ['block-id-5', 'block-id-6']    // Блоки в правой колонке
};
```

### Доступные блоки

**Header:**
- `commission-settings` - Настройки комиссий

**Left:**
- `ticker-selector` - Выбор тикера (обязательный)
- `strategy-presets` - Готовые стратегии
- `position-form` - Форма добавления позиции
- `positions-list` - Список позиций

**Right:**
- `position-summary` - Сводка по позициям
- `pl-chart` - График P&L
- `ai-chat` - AI чат-ассистент

---

## 🚀 Создание кастомного пресета

Если стандартные пресеты не подходят, можно создать свой:

```javascript
// Добавьте в calculatorBlocks.js

const PRESET_CUSTOM = {
  header: [],
  left: ['ticker-selector', 'position-form'],
  right: ['pl-chart', 'position-summary']
};

// Обновите функцию getActivePreset
const getActivePreset = () => {
  const presets = {
    minimal: PRESET_MINIMAL,
    standard: PRESET_STANDARD,
    full: PRESET_FULL,
    custom: PRESET_CUSTOM  // Добавьте свой пресет
  };
  return presets[ACTIVE_PRESET] || PRESET_FULL;
};

// Используйте
const ACTIVE_PRESET = 'custom';
```

---

## 💡 Советы

1. **Для новых пользователей:** Используйте `standard` - оптимальный баланс
2. **Для быстрой работы:** Используйте `minimal` - меньше отвлекающих элементов
3. **Для продвинутых:** Используйте `full` - все возможности
4. **Тестирование:** Попробуйте все пресеты, чтобы найти подходящий

---

## 📝 API функции

### `getCurrentPreset()`
Возвращает название текущего активного пресета.

```javascript
import { getCurrentPreset } from './config/calculatorBlocks';

const preset = getCurrentPreset(); // 'minimal', 'standard' или 'full'
```

### `getPresetDescriptions()`
Возвращает описания всех пресетов.

```javascript
import { getPresetDescriptions } from './config/calculatorBlocks';

const descriptions = getPresetDescriptions();
console.log(descriptions.minimal.name); // "Минимальная"
console.log(descriptions.minimal.description); // "Только самое необходимое..."
```

### `getActiveBlocks(position)`
Возвращает активные блоки с учетом пресета.

```javascript
import { getActiveBlocks } from './config/calculatorBlocks';

const leftBlocks = getActiveBlocks('left');
const rightBlocks = getActiveBlocks('right');
const headerBlocks = getActiveBlocks('header');
```

---

## 🔮 Будущие улучшения

- [ ] UI переключатель пресетов в интерфейсе
- [ ] Сохранение выбора пресета в localStorage
- [ ] Пользовательские пресеты через UI
- [ ] Экспорт/импорт пресетов

---

**Дата создания:** 12 октября 2025  
**Версия:** 1.0
