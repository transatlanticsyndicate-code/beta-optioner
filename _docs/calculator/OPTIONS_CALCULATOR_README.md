# Options Calculator - MVP

## 📊 Описание

Options Calculator - инструмент для создания и анализа опционных стратегий с интерактивным графиком P&L.

## ✅ Реализованные функции (MVP)

### Frontend
- ✅ **Выбор тикера** - поиск и отображение текущей цены акции
- ✅ **Компактная форма** - горизонтальная форма добавления позиций
- ✅ **Список позиций** - управление позициями (добавить, удалить, скрыть, дублировать)
- ✅ **Сводка** - Max Profit/Loss, Breakeven Points, Total Cost, Commission
- ✅ **График P&L** - интерактивный график с Plotly.js:
  - Линии для каждой позиции
  - Суммарная линия P&L
  - Заливка зон прибыли/убытка
  - Вертикальные линии (текущая цена, breakeven points)
  - Интерактивность (zoom, pan, export)
- ✅ **Настройки** - комиссии, параметры графика
- ✅ **localStorage** - автосохранение позиций

### Backend API
- ✅ `GET /api/options/expirations?ticker=SPY` - даты экспирации
- ✅ `GET /api/options/chain?ticker=SPY&expiration_date=2024-10-18` - опционная цепочка
- ✅ `POST /api/options/calculate-pl` - расчет P&L для портфеля

### Интеграция
- ✅ Polygon.io API для получения опционных данных
- ✅ Автозаполнение цен опционов
- ✅ Расчет метрик (Max Profit/Loss, Breakeven)

## 🚀 Запуск

### Backend
```bash
cd backend
source venv/bin/activate
python -m uvicorn app.main:app --reload --port 8000
```

### Frontend
```bash
cd frontend
npm start
```

Откройте: http://localhost:3000/tools/options-calculator

## 📝 Использование

1. **Введите тикер** (например, SPY, AAPL)
2. **Выберите дату экспирации**
3. **Выберите страйк** - цена автоматически подставится
4. **Выберите тип** (Call/Put) и направление (Buy/Sell)
5. **Укажите размер** и при необходимости скорректируйте цену
6. **Нажмите "Add Position"** или иконку Eye
7. **Анализируйте график** - zoom, pan, export

## 🎯 Структура данных

### Позиция
```javascript
{
  id: "uuid",
  ticker: "SPY",
  strike: 432.5,
  type: "call" | "put",
  expiration: "2024-10-24",
  direction: "buy" | "sell",
  size: 1,
  price: 24.47,
  commission: 0.65,
  visible: true,
  iv: 0.28  // опционально
}
```

### Настройки
```javascript
{
  defaultCommission: 0.65,
  priceRange: 0.20,        // ±20% от текущей цены
  chartPoints: 200,        // точек на графике
  autoSave: true
}
```

## 📊 Формулы расчета

### P&L для опциона
**Buy Call:**
```
P&L = (max(0, spot - strike) - premium) * size * 100 - commission * size
```

**Buy Put:**
```
P&L = (max(0, strike - spot) - premium) * size * 100 - commission * size
```

**Sell Call:**
```
P&L = (premium - max(0, spot - strike)) * size * 100 - commission * size
```

**Sell Put:**
```
P&L = (premium - max(0, strike - spot)) * size * 100 - commission * size
```

### Breakeven Points
Находятся численно - точки где суммарный P&L = 0

### Max Profit / Max Loss
Максимальное и минимальное значение P&L в диапазоне цен

## 🔧 Технологии

### Frontend
- React
- Plotly.js - графики
- Axios - HTTP запросы
- Lucide React - иконки
- Tailwind CSS - стили

### Backend
- FastAPI
- Polygon.io API
- Pydantic - валидация
- SQLAlchemy - БД (для будущих функций)

## 📈 Следующие этапы (v1.1)

- [ ] Быстрые стратегии (пресеты)
- [ ] Greeks для портфеля
- [ ] Probability of Profit (PoP)
- [ ] Time to Expiration анализ
- [ ] Share functionality
- [ ] Интеграция с Options Analyzer

## 🐛 Известные ограничения

1. **Polygon API** - требует API ключ, ограничения на количество запросов
2. **Расчет P&L** - упрощенный (не учитывает Greeks, время до экспирации)
3. **IV данные** - не всегда доступны через API
4. **Мобильная версия** - требует доработки

## 📝 Changelog

### v0.1.0 (MVP) - 2025-10-10
- ✅ Базовая функциональность калькулятора
- ✅ Интерактивный график P&L
- ✅ Интеграция с Polygon.io
- ✅ localStorage для сохранения позиций
- ✅ Настройки комиссий

## 📞 Поддержка

Для вопросов и предложений создайте issue в репозитории.

---

**Статус:** MVP завершен ✅  
**Дата:** 10.10.2025  
**Версия:** 0.1.0
