# 👥 Гайд по командной работе - Простыми словами

## 🤔 Что такое Mock данные?

**Проблема:**
Андрей хочет делать UI, но твой backend еще не готов или работает медленно.

**Решение - Mock данные:**
Это **поддельные данные**, которые выглядят как настоящие.

**Аналогия:**
Представь, что Андрей строит витрину магазина. Ему не нужны настоящие товары - он ставит **манекены** (mock данные), чтобы посмотреть, как будет выглядеть витрина.

**Пример:**

Вместо того, чтобы ждать твой API:
```javascript
// Реальный запрос (который еще не работает):
const data = await fetch('http://localhost:8000/analyze/step3?ticker=SPY');
```

Андрей использует поддельные данные:
```javascript
// Mock данные:
const data = {
  ticker: "SPY",
  price: 669.21,
  ai_analysis: "Рынок показывает бычий тренд..."
};
```

Он делает весь UI, а потом **просто меняет** mock на реальный API - и все работает!

---

## 🔧 Пункт 5: ESLint/Prettier - Зачем?

### ❌ Твое возражение верное!

Если Андрей **не пишет код руками**, а генерирует на v0.app - то ESLint/Prettier **не нужны**.

### ✅ Когда они нужны:

Только если Андрей будет:
1. **Редактировать** сгенерированный код
2. **Дописывать** логику (например, подключать API)
3. **Исправлять** баги

**Тогда:**
- **ESLint** - проверяет код на ошибки (как spell-checker для кода)
- **Prettier** - автоматически форматирует код (отступы, пробелы)

**Пример без Prettier:**
```javascript
// Андрей пишет так:
function test(){const x=5;return x+10;}

// Prettier автоматически делает так:
function test() {
  const x = 5;
  return x + 10;
}
```

### 🎯 Вывод:
**Пока не нужно.** Если Андрей начнет редактировать код - тогда настроим.

---

## 🌿 Git Workflow - Что это?

**Простыми словами:**
Это **правила**, как вы с Андреем будете работать с Git, чтобы не мешать друг другу.

### Проблема без Git Workflow:

```
❌ Плохой сценарий:
1. Ты работаешь над backend
2. Андрей работает над frontend
3. Вы оба коммитите в main
4. Конфликты! Код ломается! 😱
```

### Решение - Git Workflow:

```
✅ Хороший сценарий:

main (production - рабочий код)
  ↓
develop (staging - тестовый код)
  ↓
├── feature/levon-api-history (твоя ветка)
└── feature/andrey-ui-homepage (ветка Андрея)
```

**Как работает:**

1. **Андрей создает ветку:**
   ```bash
   git checkout -b feature/andrey-ui-homepage
   ```

2. **Андрей работает в своей ветке** (делает UI)

3. **Ты работаешь в своей ветке** (делаешь backend)

4. **Вы не мешаете друг другу!**

5. **Когда готово - Merge:**
   ```bash
   # Андрей:
   git checkout develop
   git merge feature/andrey-ui-homepage
   
   # Ты:
   git merge feature/levon-api-history
   ```

6. **Все работает вместе!**

### Зачем это нужно:

- **Изоляция:** Ты не сломаешь код Андрея, он не сломает твой
- **Откат:** Если что-то пошло не так - легко откатить
- **Review:** Можно посмотреть код друг друга перед merge

---

## 📄 Что будет в API_CONTRACT.md

**Уже создан!** Смотри: `_docs/API_CONTRACT.md`

**Что там:**

1. **Список всех API endpoints:**
   - `POST /analyze/step1` - получить данные
   - `POST /analyze/step2` - рассчитать метрики
   - `POST /analyze/step3` - AI анализ

2. **Примеры запросов и ответов:**
   ```
   Запрос: POST /analyze/step3?ticker=SPY
   Ответ: { "status": "success", "ticker": "SPY", ... }
   ```

3. **Mock данные для Андрея**

4. **Как обрабатывать ошибки**

**Зачем:**
Андрей открывает этот файл и **точно знает**, какие запросы делать и что ожидать.

---

## 🎯 Итоговая схема работы

```
┌─────────────────────────────────────────────────┐
│                   GitHub Repo                    │
│                                                  │
│  main ──────────────────────────────────────────│
│    ↓                                             │
│  develop ────────────────────────────────────────│
│    ↓                                             │
│    ├── feature/levon-backend-api                │
│    │   (Левон работает здесь)                   │
│    │                                             │
│    └── feature/andrey-ui-components             │
│        (Андрей работает здесь)                  │
│                                                  │
└─────────────────────────────────────────────────┘
```

**Андрей:**
1. Читает `API_CONTRACT.md`
2. Генерирует UI на v0.app
3. Копирует в свою ветку
4. Использует mock данные для тестов
5. Когда готово - делает Pull Request

**Левон (ты):**
1. Работаешь над backend в своей ветке
2. Когда API готов - обновляешь `API_CONTRACT.md`
3. Делаешь Pull Request
4. Андрей подключает реальный API вместо mock

---

## ✅ Что действительно нужно сейчас

1. **API_CONTRACT.md** - ✅ Создан
2. **Git branches** - Настроить
3. **Mock данные** - Создать для Андрея
4. **Инструкция для Андрея** - Как запустить проект

**ESLint/Prettier** - Пока не нужны!

---

Нужно ли мне создать:
- Инструкцию для Андрея по запуску проекта?
- Mock данные файл?
- Настроить Git workflow?
