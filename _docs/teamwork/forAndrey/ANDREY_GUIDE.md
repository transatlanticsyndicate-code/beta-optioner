# 👨‍💻 Инструкция для Андрея - UI инженер

## 🎯 Твоя зона ответственности

### 1. **Frontend (UI/UX)**
- `/frontend/src/components/` - все компоненты
- `/frontend/src/pages/` - все страницы
- `/frontend/src/App.css`, стили
- `/frontend/package.json` - зависимости

### 2. **AI Промпты**
- `/backend/app/prompts/` - все промпты для AI моделей
- Можешь улучшать и тестировать промпты
- Создавать новые промпты для разных моделей

---

## 🚀 Первый запуск проекта

### Шаг 1: Клонировать репозиторий

```bash
git clone https://github.com/levonmusoyan-cell/syn1.git
cd syn1
```

### Шаг 2: Настроить Frontend

```bash
cd frontend
npm install
npm start
```

Откроется: `http://localhost:3000`

### Шаг 3: (Опционально) Запустить Backend

Если хочешь тестировать с реальным API:

```bash
cd backend
python3.12 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Создать .env (попроси у Левона API ключи)
cp .env.example .env

# Запустить
uvicorn app.main:app --reload --port 8000
```

---

## 📋 Как работать с v0.app

### Процесс работы:

1. **v0.app → Push to GitHub**
   - Генерируешь компонент в v0.app
   - Жмешь "Push to GitHub"
   - v0.app создает ветку автоматически (например, `v0/product-card`)

2. **Windsurf → Скачать код**
   ```
   Команда: /andrey-pull-v0
   Указать: v0/product-card
   ```
   - Код скачается в Windsurf

3. **Дорабатываешь**
   - Подключаешь API (смотри `API_CONTRACT.md`)
   - Адаптируешь стили
   - Добавляешь в нужную страницу

4. **Деплоишь на тест**
   ```
   Команда: /andrey-test
   ```
   - Коммитит + пушит + деплоит на test.optioner.online

5. **Тестируешь САМ**
   - Открываешь `https://test.optioner.online`
   - Проверяешь UI
   - Тестируешь функционал

6. **Создаешь PR когда доволен**

---

## 🎯 Твои команды Windsurf

### Основные команды:

```
/andrey-pull-v0  - скачать код из v0.app (из GitHub)
/andrey-test     - коммит + пуш + деплой на тест (все в одном)
```

### Локальная разработка:

```
/local-start     - запустить backend и frontend
/local-stop      - остановить серверы
/local-restart   - перезапустить
/local-test-api  - протестировать API
```

**Используй `/andrey-test` для быстрой работы!**

---

## 📋 Создание Pull Request

1. **Зайти на GitHub:** https://github.com/levonmusoyan-cell/syn1
2. **Нажать "Compare & pull request"**
3. **Написать описание:**
   - Что сделал
   - Что протестировал
   - Скриншоты (если UI)
4. **Нажать "Create pull request"**
5. **Левон проверит и смержит**

---

## 🤖 Работа с AI промптами

### Где находятся:

```
backend/app/prompts/
├── gemini_prompt.py      # Промпт для Gemini
├── claude_prompt.py      # Промпт для Claude
└── base_prompt.py        # Базовый шаблон
```

### Как редактировать:

1. **Открой файл промпта:**
   ```python
   # backend/app/prompts/claude_prompt.py
   
   CLAUDE_SYSTEM_PROMPT = """
   Ты эксперт по опционному рынку...
   """
   ```

2. **Измени текст промпта**

3. **Протестируй:**
   - Запусти backend
   - Сделай запрос с `ai_model=claude`
   - Посмотри результат

4. **Коммитишь:**
   ```bash
   git add backend/app/prompts/claude_prompt.py
   git commit -m "feat(prompts): улучшил промпт для Claude"
   git push
   ```

### Создание нового промпта:

```python
# backend/app/prompts/my_new_prompt.py

MY_PROMPT = """
Твой новый промпт здесь...

Используй переменные:
- {ticker}
- {price}
- {metrics}
"""

def get_my_prompt(ticker: str, metrics: dict) -> str:
    return MY_PROMPT.format(
        ticker=ticker,
        metrics=metrics
    )
```

---

## 📡 Работа с API

### Читай контракт:

Открой: `_docs/teamwork/API_CONTRACT.md`

Там все endpoints с примерами.

### Пример использования:

```javascript
// frontend/src/services/api.js
import axios from 'axios';

const API_BASE = 'http://localhost:8000';

export const analyzeStock = async (ticker) => {
  // Шаг 1: Получить данные
  const step1 = await axios.post(`${API_BASE}/analyze/step1?ticker=${ticker}`);
  
  // Шаг 2: Метрики
  const step2 = await axios.post(`${API_BASE}/analyze/step2?ticker=${ticker}`);
  
  // Шаг 3: AI анализ
  const step3 = await axios.post(`${API_BASE}/analyze/step3?ticker=${ticker}&ai_model=gemini`);
  
  return step3.data;
};
```

### Mock данные (если backend не запущен):

```javascript
// frontend/src/mocks/mockData.js
export const mockAnalysis = {
  status: "success",
  ticker: "SPY",
  stock_data: {
    price: 669.21,
    change: -0.12
  },
  ai_analysis: "## Анализ рынка\n\nРынок показывает..."
};

// Использование:
// import { mockAnalysis } from './mocks/mockData';
// const data = mockAnalysis; // Вместо API запроса
```

---

## 🐛 Частые проблемы

### 1. CORS ошибка

```
Access to fetch at 'http://localhost:8000' has been blocked by CORS
```

**Решение:** Попроси Левона добавить твой домен в `.env`:
```
ALLOWED_ORIGINS=http://localhost:3000,http://твой-домен.com
```

### 2. API не отвечает

**Проверь:**
```bash
curl http://localhost:8000/health
```

Если не работает - backend не запущен.

### 3. Конфликты при merge

```bash
git status  # Посмотреть конфликты
# Открой файлы, исправь конфликты
git add .
git commit -m "fix: resolved merge conflicts"
```

---

## 📞 Связь с Левоном

### Когда писать Левону:

1. **Нужны API ключи** для `.env`
2. **Нужен новый endpoint** в API
3. **Баг в backend**
4. **Готов Pull Request** - нужен review

### Когда НЕ писать:

1. UI баги - исправляй сам
2. Стили - твоя зона
3. Промпты - экспериментируй сам

---

## ✅ Чеклист перед коммитом

- [ ] Код работает (`npm start` без ошибок)
- [ ] Нет console.log (или закомментированы)
- [ ] Компоненты в правильных папках
- [ ] Понятное название коммита: `feat(ui): добавил...`
- [ ] Запушил в свою ветку (не в main!)

---

---

**Вопросы?** Пиши Левону!

**Документация:** `_docs/teamwork/forAndrey/`

**Последнее обновление:** 2025-01-09
