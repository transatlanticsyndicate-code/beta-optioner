# 🚀 Быстрая настройка за 15 минут

**Для тех кто хочет быстро начать работать**

---

## ⚡ Экспресс-проверка (5 минут)

### Шаг 1: Проверка ПО

Открыть PowerShell и выполнить:

```powershell
node --version
npm --version
python --version
git --version
```

**Все команды работают?**
- ✅ Да → Переход к Шагу 2
- ❌ Нет → Установить недостающее ПО (см. раздел "Установка ПО")

---

### Шаг 2: Проверка проекта

```powershell
cd C:\Users\Andres\Desktop\WINDSURF\OPTIONER\syn1

# Проверка backend
dir backend\venv
dir backend\.env

# Проверка frontend
dir frontend\node_modules
```

**Все папки существуют?**
- ✅ Да → Переход к Шагу 3
- ❌ Нет → Запустить установку (см. раздел "Установка окружения")

---

### Шаг 3: Запуск проекта

```powershell
# Из корня проекта
restart-local.bat
```

**Открылись 2 окна и серверы запустились?**
- ✅ Да → **Готово! Можно работать!** 🎉
- ❌ Нет → Читать детальную инструкцию ниже

---

## 📦 Установка ПО (если нужно)

### Node.js (если отсутствует)

1. Скачать: https://nodejs.org/ (версия LTS)
2. Установить с настройками по умолчанию
3. **Перезапустить терминал!**
4. Проверить: `node --version`

### Python (если отсутствует)

1. Скачать: https://www.python.org/downloads/ (версия 3.12)
2. ✅ **ВАЖНО:** Отметить "Add Python to PATH"
3. Установить
4. **Перезапустить терминал!**
5. Проверить: `python --version`

### Git (если отсутствует)

1. Скачать: https://git-scm.com/download/win
2. Установить с настройками по умолчанию
3. Проверить: `git --version`

---

## 🔧 Установка окружения (если нужно)

### Backend

```powershell
# Из корня проекта
setup-backend.bat
```

Этот скрипт:
- Создаст виртуальное окружение
- Установит все зависимости Python

**Ожидаемый результат:** "Backend setup complete!"

---

### Frontend

```powershell
cd frontend
npm install
cd ..
```

**Ожидаемый результат:** Папка `node_modules` создана и заполнена

---

### Проверка .env файла

```powershell
type backend\.env
```

**Если файл пустой или отсутствует:**
```powershell
cd backend
copy .env.example .env
cd ..
```

**Важно:** API ключи можно запросить у Левона позже, для начала работы с UI они не критичны.

---

## 🚀 Первый запуск

### Способ 1: Автоматический (рекомендуется)

```powershell
restart-local.bat
```

### Способ 2: Ручной (если bat-файл не работает)

**Окно 1 - Backend:**
```powershell
cd backend
venv\Scripts\activate
python -m uvicorn app.main:app --reload --port 8000
```

**Окно 2 - Frontend:**
```powershell
cd frontend
set PORT=3000
npm start
```

---

## ✅ Проверка работоспособности

### 1. Backend API

Открыть в браузере: http://localhost:8000/docs

**Должна открыться Swagger документация**

### 2. Frontend приложение

Открыть в браузере: http://localhost:3000

**Должно загрузиться React приложение**

### 3. Консоль браузера

Нажать F12 → вкладка Console

**Не должно быть красных ошибок (warnings можно игнорировать)**

---

## 🎯 Первые шаги в разработке

### 1. Создать рабочую ветку

```powershell
git checkout main
git pull origin main
git checkout -b feature/my-first-task
```

### 2. Внести изменения

Открыть любой файл в `frontend/src/` и изменить

Например: `frontend/src/App.js`

### 3. Проверить в браузере

Изменения должны примениться автоматически (hot reload)

### 4. Закоммитить

```powershell
git add .
git commit -m "feat(ui): мое первое изменение"
git push origin feature/my-first-task
```

### 5. Создать Pull Request

1. Открыть: https://github.com/levonmusoyan-cell/syn1
2. Нажать "Compare & pull request"
3. Описать изменения
4. Создать PR

---

## 🛠️ Windsurf команды

### Основные команды:

```
/andrey-test
```
Делает: Коммит → Пуш → Деплой на тест  
Когда: Нужно быстро проверить на тесте

```
/andrey-pull-v0
```
Делает: Скачивает код из v0.app  
Когда: После создания компонента в v0.app

```
/local-restart
```
Делает: Перезапускает серверы  
Когда: После изменения конфигурации

---

## 🐛 Частые проблемы

### Проблема: Порт 8000 занят

**Ошибка:**
```
ERROR: error while attempting to bind on address
```

**Решение:**
```powershell
netstat -ano | findstr :8000
taskkill /PID <номер_процесса> /F
```

---

### Проблема: Порт 3000 занят

**Ошибка:**
```
Something is already running on port 3000
```

**Решение:**
```powershell
netstat -ano | findstr :3000
taskkill /PID <номер_процесса> /F
```

---

### Проблема: CORS ошибки

**Ошибка в консоли браузера:**
```
Access to fetch blocked by CORS
```

**Решение:**
Проверить в `backend/.env`:
```env
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

---

### Проблема: npm install не работает

**Ошибка:**
```
npm ERR! peer dependencies
```

**Решение:**
```powershell
cd frontend
npm install --legacy-peer-deps
```

---

### Проблема: Python venv не активируется

**Ошибка:**
```
cannot be loaded because running scripts is disabled
```

**Решение:**
Запустить PowerShell от имени администратора:
```powershell
Set-ExecutionPolicy RemoteSigned
```

---

## 📚 Что читать дальше

После успешного запуска изучить:

1. **ANDREY_GUIDE.md** - полная инструкция для работы
2. **WORKFLOW_PROCESS.md** - процесс работы с задачами
3. **API_CONTRACT.md** - описание доступных API
4. **PROMPTS_GUIDE.md** - работа с AI промптами

---

## ✨ Чеклист готовности

- [ ] Node.js установлен
- [ ] Python установлен
- [ ] Git установлен и настроен
- [ ] Backend запускается
- [ ] Frontend запускается
- [ ] `restart-local.bat` работает
- [ ] Могу создать ветку
- [ ] Могу сделать коммит
- [ ] Могу отправить на GitHub
- [ ] Hot reload работает
- [ ] Нет CORS ошибок

**Все пункты выполнены?** → **Можно начинать работать! 🚀**

---

## 🆘 Нужна помощь?

### Самостоятельно можно решить:
- Установка ПО
- Проблемы с npm
- Git конфликты
- UI баги

### Обратиться к Левону:
- API ключи
- Backend ошибки
- Новые endpoints
- Проблемы с БД

---

## 🎯 Следующие задачи

1. ✅ Настроить окружение
2. ⬜ Изучить структуру проекта
3. ⬜ Попробовать создать компонент
4. ⬜ Протестировать v0.app workflow
5. ⬜ Начать работу над реальными задачами

---

**Время на настройку:** 15-30 минут  
**Сложность:** Легко  
**Результат:** Готовая среда для разработки

**Успехов! 🚀**
