# 📋 ТЗ: Настройка локальной разработки фронтэнда (Windows)

**Дата создания:** 17.10.2025  
**Исполнитель:** Андрей (Frontend разработчик)  
**Платформа:** Windows  
**Цель:** Полноценная локальная разработка UI с возможностью внесения правок и отправки в репозиторий

---

## 🎯 Главная цель

Настроить полноценный процесс локальной разработки фронтэнда на Windows компьютере:
- ✅ Запуск проекта локально
- ✅ Внесение правок в UI
- ✅ Тестирование изменений
- ✅ Отправка изменений в репозиторий

---

## 📊 Текущее состояние проекта

### Структура проекта:
```
syn1/
├── frontend/          # React приложение (твоя зона)
│   ├── src/
│   ├── package.json
│   └── node_modules/  # ✅ Уже установлены
├── backend/           # Python FastAPI (зона Левона)
│   ├── app/
│   ├── venv/          # ✅ Виртуальное окружение создано
│   ├── requirements.txt
│   └── .env           # ✅ Конфигурация есть
├── .windsurf/
│   └── workflows/     # Готовые команды
└── *.bat              # Скрипты для Windows
```

### Что уже есть:
- ✅ Backend виртуальное окружение создано (`backend/venv/`)
- ✅ Frontend зависимости установлены (`frontend/node_modules/`)
- ✅ Готовые bat-файлы для запуска
- ✅ Workflows для автоматизации
- ✅ Документация для Андрея

---

## 🔍 ЭТАП 1: Диагностика текущего состояния

### 1.1 Проверка установленного ПО

**Задача:** Проверить наличие необходимого ПО на компьютере

**Действия:**
```powershell
# Проверка Node.js
node --version
# Требуется: v18.x или выше

# Проверка npm
npm --version
# Требуется: v9.x или выше

# Проверка Python
python --version
# Требуется: Python 3.12+

# Проверка Git
git --version
# Требуется: любая версия
```

**Ожидаемый результат:**
- Node.js >= 18.0.0
- npm >= 9.0.0
- Python >= 3.12
- Git установлен

**Если что-то отсутствует → переход к ЭТАПУ 2**

---

### 1.2 Проверка состояния проекта

**Задача:** Убедиться, что проект корректно клонирован

**Действия:**
```powershell
# Перейти в папку проекта
cd C:\Users\Andres\Desktop\WINDSURF\OPTIONER\syn1

# Проверить статус Git
git status

# Проверить текущую ветку
git branch

# Проверить удаленный репозиторий
git remote -v
```

**Ожидаемый результат:**
- Репозиторий: `https://github.com/levonmusoyan-cell/syn1.git`
- Ветка: `main` или другая рабочая ветка
- Нет незакоммиченных изменений (или понятно какие есть)

---

### 1.3 Проверка Backend окружения

**Задача:** Проверить готовность backend к запуску

**Действия:**
```powershell
# Проверить наличие виртуального окружения
dir backend\venv

# Проверить наличие .env файла
dir backend\.env

# Попробовать активировать venv
cd backend
venv\Scripts\activate
python --version
deactivate
cd ..
```

**Ожидаемый результат:**
- ✅ Папка `backend\venv\` существует
- ✅ Файл `backend\.env` существует
- ✅ Виртуальное окружение активируется
- ✅ Python версия 3.12+

**Возможные проблемы:**
- ❌ venv не активируется → переустановка venv
- ❌ .env отсутствует → создать из .env.example
- ❌ Ошибки при активации → проверка прав доступа

---

### 1.4 Проверка Frontend окружения

**Задача:** Проверить готовность frontend к запуску

**Действия:**
```powershell
# Проверить node_modules
dir frontend\node_modules

# Проверить package.json
type frontend\package.json

# Проверить установленные пакеты
cd frontend
npm list --depth=0
cd ..
```

**Ожидаемый результат:**
- ✅ Папка `frontend\node_modules\` существует и не пустая
- ✅ `package.json` содержит все зависимости
- ✅ Нет критических ошибок в зависимостях

**Возможные проблемы:**
- ❌ node_modules пустая → `npm install`
- ❌ Ошибки зависимостей → `npm install --force`
- ❌ Конфликты версий → очистка и переустановка

---

## 🛠️ ЭТАП 2: Установка недостающего ПО

### 2.1 Установка Node.js (если отсутствует)

**Действия:**
1. Скачать Node.js LTS: https://nodejs.org/
2. Установить версию 18.x или 20.x (LTS)
3. Перезапустить терминал
4. Проверить: `node --version` и `npm --version`

**Критично:** После установки ОБЯЗАТЕЛЬНО перезапустить терминал!

---

### 2.2 Установка Python (если отсутствует)

**Действия:**
1. Скачать Python 3.12: https://www.python.org/downloads/
2. ✅ Отметить "Add Python to PATH" при установке
3. Установить
4. Проверить: `python --version`

**Критично:** Галочка "Add to PATH" обязательна!

---

### 2.3 Установка Git (если отсутствует)

**Действия:**
1. Скачать Git: https://git-scm.com/download/win
2. Установить с настройками по умолчанию
3. Проверить: `git --version`

---

## 🔧 ЭТАП 3: Настройка Backend

### 3.1 Проверка/Создание виртуального окружения

**Если venv уже существует:**
```powershell
cd backend
venv\Scripts\activate
pip list
deactivate
cd ..
```

**Если venv отсутствует или поврежден:**
```powershell
# Удалить старый venv (если есть)
rmdir /s /q backend\venv

# Запустить setup-backend.bat
setup-backend.bat
```

**Ожидаемый результат:**
- Виртуальное окружение создано
- Все пакеты из `requirements.txt` установлены
- Нет ошибок установки

---

### 3.2 Проверка .env файла

**Действия:**
```powershell
# Проверить наличие .env
type backend\.env
```

**Если .env отсутствует:**
```powershell
cd backend
copy .env.example .env
cd ..
```

**Важно:** Файл `.env` уже существует в проекте, но нужно убедиться что он содержит все необходимые ключи.

**Минимально необходимые переменные:**
```env
# API Keys (запросить у Левона если отсутствуют)
POLYGON_API_KEY=your_key_here
GEMINI_API_KEY=your_key_here

# Database (для локальной разработки можно оставить как есть)
DATABASE_URL=postgresql://...

# CORS (для локальной разработки)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

---

### 3.3 Тестовый запуск Backend

**Действия:**
```powershell
cd backend
venv\Scripts\activate
python -m uvicorn app.main:app --reload --port 8000
```

**Ожидаемый результат:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

**Проверка в браузере:**
- Открыть: http://localhost:8000/docs
- Должна открыться Swagger документация API

**Возможные ошибки:**

**Ошибка 1: Порт 8000 занят**
```
ERROR: [Errno 10048] error while attempting to bind on address
```
Решение:
```powershell
# Найти и убить процесс на порту 8000
netstat -ano | findstr :8000
taskkill /PID <номер_процесса> /F
```

**Ошибка 2: Отсутствуют API ключи**
```
ERROR: POLYGON_API_KEY not found
```
Решение: Запросить ключи у Левона и добавить в `.env`

**Ошибка 3: Ошибка подключения к БД**
```
ERROR: could not connect to database
```
Решение: Для локальной разработки frontend БД не критична, можно продолжать

---

## 🎨 ЭТАП 4: Настройка Frontend

### 4.1 Проверка зависимостей

**Действия:**
```powershell
cd frontend
npm list --depth=0
```

**Если есть ошибки или отсутствуют пакеты:**
```powershell
# Чистая переустановка
rmdir /s /q node_modules
del package-lock.json
npm install
```

**Ожидаемый результат:**
- Все пакеты из `package.json` установлены
- Нет критических ошибок (warnings можно игнорировать)

---

### 4.2 Тестовый запуск Frontend

**Действия:**
```powershell
cd frontend
set PORT=3000
npm start
```

**Ожидаемый результат:**
```
Compiled successfully!

You can now view syndicate-platform-frontend in the browser.

  Local:            http://localhost:3000
  On Your Network:  http://192.168.x.x:3000
```

**Проверка в браузере:**
- Автоматически откроется http://localhost:3000
- Приложение должно загрузиться без ошибок

**Возможные ошибки:**

**Ошибка 1: Порт 3000 занят**
```
Something is already running on port 3000
```
Решение:
```powershell
# Убить процесс на порту 3000
netstat -ano | findstr :3000
taskkill /PID <номер_процесса> /F
```

**Ошибка 2: Ошибки компиляции**
```
Failed to compile
```
Решение:
- Проверить логи ошибок
- Возможно конфликт версий Node.js
- Попробовать `npm install --legacy-peer-deps`

**Ошибка 3: CORS ошибки при запросах к API**
```
Access to fetch at 'http://localhost:8000' blocked by CORS
```
Решение: Проверить `ALLOWED_ORIGINS` в `backend/.env`

---

## 🚀 ЭТАП 5: Запуск полного стека

### 5.1 Использование готового bat-файла

**Самый простой способ:**
```powershell
# Из корня проекта
restart-local.bat
```

Этот скрипт:
1. Останавливает процессы на портах 8000 и 3000
2. Запускает Backend в отдельном окне
3. Запускает Frontend в отдельном окне

**Ожидаемый результат:**
- Открылось 2 окна терминала (Backend и Frontend)
- Backend запущен на http://localhost:8000
- Frontend запущен на http://localhost:3000
- Оба сервиса работают без ошибок

---

### 5.2 Проверка работоспособности

**Тест 1: Backend API**
```powershell
# В браузере открыть
http://localhost:8000/docs
```
Должна открыться Swagger документация

**Тест 2: Frontend приложение**
```powershell
# В браузере открыть
http://localhost:3000
```
Должно загрузиться React приложение

**Тест 3: Связь Frontend → Backend**
- В приложении попробовать выполнить любой запрос к API
- Проверить в консоли браузера (F12) отсутствие CORS ошибок
- Данные должны успешно загружаться

---

## 📝 ЭТАП 6: Настройка Git и рабочего процесса

### 6.1 Настройка Git (если еще не настроен)

**Действия:**
```powershell
# Настроить имя и email
git config --global user.name "Andrey"
git config --global user.email "your-email@example.com"

# Проверить настройки
git config --list
```

---

### 6.2 Создание рабочей ветки

**Процесс работы:**

1. **Всегда работать в отдельной ветке (НЕ в main!)**
```powershell
# Убедиться что на main
git checkout main

# Получить последние изменения
git pull origin main

# Создать новую ветку для задачи
git checkout -b feature/ui-improvements
```

2. **Внести изменения в код**
- Редактировать файлы в `frontend/src/`
- Сохранить изменения

3. **Проверить что изменилось**
```powershell
git status
git diff
```

4. **Закоммитить изменения**
```powershell
# Добавить файлы
git add .

# Создать коммит с понятным описанием
git commit -m "feat(ui): улучшил дизайн главной страницы"
```

5. **Отправить в GitHub**
```powershell
git push origin feature/ui-improvements
```

---

### 6.3 Использование Windsurf workflows

**Готовые команды для Андрея:**

**Команда 1: `/andrey-test`**
- Что делает: Коммит → Пуш → Деплой на тест сервер
- Когда использовать: Когда нужно быстро проверить изменения на тесте

**Команда 2: `/andrey-pull-v0`**
- Что делает: Скачивает код из v0.app (из GitHub ветки)
- Когда использовать: После создания компонента в v0.app

**Команда 3: `/local-restart`**
- Что делает: Перезапускает локальные серверы
- Когда использовать: После изменения конфигурации

---

## 🧪 ЭТАП 7: Тестирование рабочего процесса

### 7.1 Тест: Внесение простого изменения

**Задача:** Изменить текст на главной странице

**Шаги:**
1. Создать ветку:
```powershell
git checkout -b test/simple-change
```

2. Открыть файл (например):
```
frontend/src/App.js
```

3. Изменить любой текст (например, заголовок)

4. Сохранить файл

5. Проверить в браузере (http://localhost:3000)
   - Изменения должны примениться автоматически (hot reload)

6. Закоммитить:
```powershell
git add .
git commit -m "test: изменил заголовок"
git push origin test/simple-change
```

7. Проверить на GitHub:
   - Открыть https://github.com/levonmusoyan-cell/syn1
   - Должна появиться ветка `test/simple-change`

**Ожидаемый результат:**
- ✅ Изменения видны локально
- ✅ Коммит создан
- ✅ Ветка отправлена на GitHub
- ✅ Можно создать Pull Request

---

### 7.2 Тест: Работа с v0.app workflow

**Задача:** Скачать компонент из v0.app

**Предварительно:**
- Создать простой компонент в v0.app
- Нажать "Push to GitHub"
- v0.app создаст ветку (например, `v0/button-component`)

**Шаги:**
1. В Windsurf выполнить команду:
```
/andrey-pull-v0
```

2. Указать имя ветки:
```
v0/button-component
```

3. Код скачается в проект

4. Доработать компонент (подключить к API, стили и т.д.)

5. Протестировать локально

6. Использовать `/andrey-test` для деплоя на тест

**Ожидаемый результат:**
- ✅ Код из v0.app скачан
- ✅ Можно редактировать локально
- ✅ Изменения отправляются на тест сервер

---

## 📋 ЭТАП 8: Документирование проблем и решений

### 8.1 Создание лога проблем

**Во время настройки записывать:**

**Файл:** `LOCAL_SETUP_LOG.md`

```markdown
# Лог настройки локального окружения

## Дата: 17.10.2025

### Проблема 1: [Описание]
**Ошибка:**
```
[текст ошибки]
```

**Решение:**
[что сделал чтобы исправить]

**Статус:** ✅ Решено / ❌ Не решено

---

### Проблема 2: ...
```

---

### 8.2 Чеклист финальной проверки

**После завершения настройки проверить:**

- [ ] Node.js установлен и работает
- [ ] Python установлен и работает
- [ ] Git настроен (имя, email)
- [ ] Backend запускается без ошибок
- [ ] Frontend запускается без ошибок
- [ ] `restart-local.bat` работает корректно
- [ ] Можно создать ветку и сделать коммит
- [ ] Можно отправить изменения на GitHub
- [ ] Windsurf workflows работают
- [ ] Связь Frontend ↔ Backend работает
- [ ] Hot reload работает (изменения применяются автоматически)

---

## 🎯 Критерии успешного завершения

### ✅ Проект считается настроенным если:

1. **Запуск проекта:**
   - Backend запускается одной командой
   - Frontend запускается одной командой
   - Оба сервиса работают одновременно

2. **Разработка:**
   - Можно редактировать файлы в `frontend/src/`
   - Изменения видны в браузере автоматически
   - Нет критических ошибок в консоли

3. **Git workflow:**
   - Можно создать ветку
   - Можно сделать коммит
   - Можно отправить на GitHub
   - Можно создать Pull Request

4. **Интеграция:**
   - Frontend может делать запросы к Backend
   - Нет CORS ошибок
   - API возвращает данные

---

## 📞 Когда обращаться к Левону

### ✅ Можно решить самостоятельно:
- Проблемы с установкой Node.js/Python
- Ошибки npm install
- Проблемы с Git
- UI баги и стили
- Работа с промптами

### ❌ Нужна помощь Левона:
- API ключи отсутствуют в .env
- Backend не запускается (ошибки Python)
- Нужен новый endpoint в API
- Ошибки базы данных
- Проблемы с деплоем на продакшн

---

## 📚 Полезные ресурсы

### Документация проекта:
- `README.md` - общее описание
- `_docs/teamwork/forAndrey/ANDREY_GUIDE.md` - полная инструкция
- `_docs/teamwork/forAndrey/WORKFLOW_PROCESS.md` - процесс работы
- `_docs/teamwork/forAndrey/API_CONTRACT.md` - описание API

### Команды Windsurf:
- `/andrey-test` - быстрый деплой на тест
- `/andrey-pull-v0` - скачать из v0.app
- `/local-restart` - перезапуск серверов

### Bat-файлы:
- `restart-local.bat` - перезапуск всего стека
- `setup-backend.bat` - установка backend окружения

---

## 🔄 План действий (краткая версия)

1. **Проверка ПО** (10 мин)
   - Node.js, Python, Git

2. **Диагностика проекта** (15 мин)
   - Проверка backend/venv
   - Проверка frontend/node_modules
   - Проверка .env файлов

3. **Установка недостающего** (30 мин)
   - Установка ПО если нужно
   - Переустановка зависимостей если нужно

4. **Первый запуск** (20 мин)
   - Запуск backend
   - Запуск frontend
   - Проверка работоспособности

5. **Настройка Git** (10 мин)
   - Конфигурация
   - Создание тестовой ветки

6. **Тестирование workflow** (30 мин)
   - Внесение изменений
   - Коммит и пуш
   - Проверка на GitHub

7. **Документирование** (15 мин)
   - Записать все проблемы
   - Записать решения
   - Заполнить чеклист

**Общее время:** ~2-3 часа (с учетом возможных проблем)

---

## ✨ Следующие шаги после настройки

1. Изучить структуру проекта (`frontend/src/`)
2. Ознакомиться с API Contract
3. Попробовать создать простой компонент
4. Протестировать workflow с v0.app
5. Начать работу над реальными задачами

---

**Успехов в настройке! 🚀**

**Вопросы:** Пиши Левону или используй документацию в `_docs/teamwork/forAndrey/`
