# 🚀 Шпаргалка по командам

**Быстрый справочник всех команд для работы**

---

## ⚡ Запуск проекта

### Автоматический запуск (рекомендуется)
```powershell
# Из корня проекта
restart-local.bat
```

### Ручной запуск Backend
```powershell
cd backend
venv\Scripts\activate
python -m uvicorn app.main:app --reload --port 8000
```

### Ручной запуск Frontend
```powershell
cd frontend
set PORT=3000
npm start
```

---

## 🛑 Остановка серверов

### Остановить Backend (порт 8000)
```powershell
netstat -ano | findstr :8000
taskkill /PID <номер_процесса> /F
```

### Остановить Frontend (порт 3000)
```powershell
netstat -ano | findstr :3000
taskkill /PID <номер_процесса> /F
```

### Остановить оба сразу
```powershell
for /f "tokens=5" %a in ('netstat -aon ^| find ":8000" ^| find "LISTENING"') do taskkill /F /PID %a
for /f "tokens=5" %a in ('netstat -aon ^| find ":3000" ^| find "LISTENING"') do taskkill /F /PID %a
```

---

## 🔧 Установка и настройка

### Первая установка Backend
```powershell
setup-backend.bat
```

### Переустановка Backend зависимостей
```powershell
cd backend
venv\Scripts\activate
pip install -r requirements.txt
deactivate
cd ..
```

### Первая установка Frontend
```powershell
cd frontend
npm install
cd ..
```

### Переустановка Frontend зависимостей
```powershell
cd frontend
rmdir /s /q node_modules
del package-lock.json
npm install
cd ..
```

### Переустановка с legacy режимом (если ошибки)
```powershell
cd frontend
npm install --legacy-peer-deps
cd ..
```

---

## 🌿 Git команды

### Создание новой ветки
```powershell
# Переключиться на main
git checkout main

# Получить последние изменения
git pull origin main

# Создать новую ветку
git checkout -b feature/название-задачи
```

### Проверка статуса
```powershell
# Посмотреть что изменилось
git status

# Посмотреть детали изменений
git diff

# Посмотреть текущую ветку
git branch
```

### Коммит изменений
```powershell
# Добавить все файлы
git add .

# Или добавить конкретный файл
git add путь/к/файлу

# Создать коммит
git commit -m "feat(ui): описание изменений"

# Отправить на GitHub
git push origin название-ветки
```

### Типы коммитов
```powershell
git commit -m "feat(ui): добавил новый компонент"      # Новая функция
git commit -m "fix(ui): исправил баг с кнопкой"       # Исправление бага
git commit -m "style(ui): улучшил стили формы"        # Стили
git commit -m "refactor(ui): переписал компонент"     # Рефакторинг
git commit -m "docs: обновил README"                  # Документация
git commit -m "test: добавил тесты"                   # Тесты
```

### Отмена изменений
```powershell
# Отменить изменения в файле (до коммита)
git checkout -- путь/к/файлу

# Отменить все изменения (до коммита)
git reset --hard

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1
```

### Обновление ветки
```powershell
# Получить изменения из main
git checkout main
git pull origin main
git checkout твоя-ветка
git merge main
```

---

## 🤖 Windsurf Workflows

### Основные команды
```
/andrey-test
```
Делает: Коммит → Пуш → Деплой на тест сервер  
Использовать: Когда нужно быстро проверить на тесте

```
/andrey-pull-v0
```
Делает: Скачивает код из v0.app (из GitHub ветки)  
Использовать: После создания компонента в v0.app

```
/andrey-push-test
```
Делает: Отправляет изменения UI на тест сервер  
Использовать: Быстрый деплой UI изменений

### Локальная разработка
```
/local-start
```
Запускает backend и frontend серверы

```
/local-stop
```
Останавливает все серверы

```
/local-restart
```
Перезапускает серверы (Windows версия)

```
/local-test-api
```
Тестирует API endpoints

---

## 🔍 Проверка и диагностика

### Проверка установленного ПО
```powershell
node --version          # Должно быть v18.x+
npm --version           # Должно быть v9.x+
python --version        # Должно быть 3.12+
git --version           # Любая версия
```

### Проверка серверов
```powershell
# Backend
curl http://localhost:8000/health

# Frontend
curl http://localhost:3000

# API документация
start http://localhost:8000/docs
```

### Проверка портов
```powershell
# Посмотреть что занимает порт 8000
netstat -ano | findstr :8000

# Посмотреть что занимает порт 3000
netstat -ano | findstr :3000

# Посмотреть все занятые порты
netstat -ano | findstr LISTENING
```

### Проверка процессов
```powershell
# Все процессы Python
tasklist | findstr python

# Все процессы Node
tasklist | findstr node

# Убить процесс по PID
taskkill /PID <номер> /F
```

---

## 📦 npm команды

### Основные
```powershell
cd frontend

npm start              # Запуск dev сервера
npm run build          # Сборка для продакшн
npm test               # Запуск тестов
npm install            # Установка зависимостей
```

### Работа с пакетами
```powershell
# Установить новый пакет
npm install имя-пакета

# Установить dev зависимость
npm install --save-dev имя-пакета

# Удалить пакет
npm uninstall имя-пакета

# Обновить пакет
npm update имя-пакета

# Посмотреть установленные пакеты
npm list --depth=0
```

### Очистка
```powershell
# Очистить кэш npm
npm cache clean --force

# Удалить node_modules и переустановить
rmdir /s /q node_modules
del package-lock.json
npm install
```

---

## 🐍 Python команды

### Виртуальное окружение
```powershell
cd backend

# Активировать venv
venv\Scripts\activate

# Деактивировать venv
deactivate

# Создать новый venv
python -m venv venv
```

### Работа с пакетами
```powershell
# Установить зависимости
pip install -r requirements.txt

# Установить один пакет
pip install имя-пакета

# Обновить пакет
pip install --upgrade имя-пакета

# Посмотреть установленные пакеты
pip list

# Сохранить текущие пакеты
pip freeze > requirements.txt
```

---

## 🌐 Полезные URL

### Локальная разработка
```
http://localhost:8000          # Backend API
http://localhost:8000/docs     # Swagger документация
http://localhost:8000/health   # Health check
http://localhost:3000          # Frontend приложение
```

### Тестовый сервер
```
https://test.optioner.online   # Тестовое окружение
```

### GitHub
```
https://github.com/levonmusoyan-cell/syn1              # Репозиторий
https://github.com/levonmusoyan-cell/syn1/pulls        # Pull Requests
https://github.com/levonmusoyan-cell/syn1/branches     # Ветки
```

---

## 🔑 Переменные окружения

### Проверка .env
```powershell
type backend\.env
```

### Основные переменные
```env
# API ключи
POLYGON_API_KEY=your_key
GEMINI_API_KEY=your_key

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# База данных
DATABASE_URL=postgresql://...
```

---

## 🐛 Решение проблем

### Ошибка: Порт занят
```powershell
# Найти процесс
netstat -ano | findstr :8000

# Убить процесс
taskkill /PID <номер> /F
```

### Ошибка: venv не активируется
```powershell
# Запустить PowerShell от администратора
Set-ExecutionPolicy RemoteSigned
```

### Ошибка: npm peer dependencies
```powershell
npm install --legacy-peer-deps
```

### Ошибка: CORS
Добавить в `backend/.env`:
```env
ALLOWED_ORIGINS=http://localhost:3000
```

### Ошибка: Module not found
```powershell
# Frontend
cd frontend
npm install

# Backend
cd backend
venv\Scripts\activate
pip install -r requirements.txt
```

---

## 📝 Быстрые заметки

### Создать файл
```powershell
echo. > имя-файла.txt
```

### Посмотреть содержимое файла
```powershell
type имя-файла.txt
```

### Скопировать файл
```powershell
copy источник.txt назначение.txt
```

### Удалить файл
```powershell
del имя-файла.txt
```

### Создать папку
```powershell
mkdir имя-папки
```

### Удалить папку
```powershell
rmdir /s /q имя-папки
```

---

## 🎯 Типичные сценарии

### Сценарий 1: Начало работы над новой задачей
```powershell
# 1. Получить последние изменения
git checkout main
git pull origin main

# 2. Создать ветку
git checkout -b feature/моя-задача

# 3. Запустить проект
restart-local.bat

# 4. Начать работу
# Редактировать файлы в frontend/src/
```

### Сценарий 2: Сохранить и отправить изменения
```powershell
# 1. Проверить что изменилось
git status

# 2. Добавить файлы
git add .

# 3. Создать коммит
git commit -m "feat(ui): описание"

# 4. Отправить на GitHub
git push origin feature/моя-задача
```

### Сценарий 3: Быстрый тест на сервере
```
# В Windsurf выполнить
/andrey-test
```

### Сценарий 4: Работа с v0.app
```
# 1. Создать компонент в v0.app
# 2. Push to GitHub (v0.app создаст ветку)
# 3. В Windsurf выполнить
/andrey-pull-v0
# 4. Указать имя ветки (например: v0/button-component)
# 5. Доработать компонент
# 6. Использовать /andrey-test для деплоя
```

---

## 💡 Советы

### Горячие клавиши PowerShell
- `Ctrl + C` - Остановить процесс
- `Ctrl + L` - Очистить экран
- `Tab` - Автодополнение
- `↑` / `↓` - История команд

### Полезные алиасы (можно добавить в профиль)
```powershell
# Создать профиль PowerShell
notepad $PROFILE

# Добавить алиасы
function gst { git status }
function gco { git checkout $args }
function gcm { git commit -m $args }
function gp { git push $args }
```

---

**Сохрани эту шпаргалку - она пригодится каждый день! 📌**
