# 🔄 Полный процесс работы с тест-сервером

## 🎯 Архитектура

```
┌─────────────────────────────────────────────┐
│           GitHub Repository                  │
│                                              │
│  main ──────────────────────────────────────│
│    ↓                                         │
│  ├── feature/levon-backend-*                │
│  └── feature/andrey-ui-*                    │
│                                              │
└─────────────────────────────────────────────┘
         ↓                    ↓
         ↓                    ↓
┌────────────────┐   ┌────────────────┐
│  Test Server   │   │  Production    │
│                │   │                │
│ test.optioner  │   │ optioner.online│
│   .online      │   │                │
│                │   │                │
│ Docker:        │   │ Docker:        │
│ - Backend:8001 │   │ - Backend:8000 │
│ - Frontend:3001│   │ - Frontend:3000│
│ - DB (test)    │   │ - DB (prod)    │
│ - Redis (test) │   │ - Redis (prod) │
└────────────────┘   └────────────────┘
```

---

## 📋 Процесс: Левон работает с Backend

### Шаг 1: Создать ветку

```
Команда: /levon-create-branch
Ветка: feature/levon-backend-history-api
```

### Шаг 2: Работать

- Пишешь код в Windsurf
- Изменяешь файлы в `/backend/app/`

### Шаг 3: Коммит → Пуш → Деплой на тест (все в одном)

```
Команда: /levon-test
Сообщение: "feat(backend): добавил API для истории анализов"
```

**Что происходит:**
1. Коммитит изменения
2. Пушит на GitHub
3. Подключается к серверу
4. Скачивает изменения
5. Перезапускает Docker (backend + frontend)
6. Тест-сервер обновлен!


### Шаг 6: Тестировать

- Открываешь `https://test.optioner.online`
- Проверяешь новый API endpoint
- Тестируешь с разными данными
- Если нужно → исправляешь → повторяешь шаги 3-6

### Шаг 7: Создать PR

- Заходишь на GitHub
- Создаешь Pull Request
- Описываешь изменения

### Шаг 8: Смержить

- Проверяешь код
- Жмешь "Merge pull request"
- Изменения теперь в `main`

### Шаг 9: Деплой на production

```
Команда: /levon-deploy-prod
```

**Что происходит:**
1. Подключается к серверу
2. Переключается на `main`
3. Скачивает изменения
4. Перезапускает Docker production
5. Production обновлен!

### Шаг 10: Проверить

- Открываешь `https://optioner.online`
- Проверяешь что все работает

---

## 📋 Процесс: Андрей работает с Frontend/Промптами

### Шаг 1: Генерация в v0.app (если UI)

- Генерирует компонент в v0.app
- Жмет "Push to GitHub"
- v0.app создает ветку автоматически

### Шаг 2: Скачать в Windsurf

```
Команда: /andrey-create-branch
Или: "Скачай ветку v0/component-card из GitHub"
```

### Шаг 3: Доработать

- Подключает API
- Адаптирует стили
- Или редактирует промпты в `/backend/app/prompts/`

### Шаг 4: Коммит → Пуш → Деплой на тест (все в одном)

```
Команда: /andrey-test
Сообщение: "feat(ui): добавил карточку продукта"
```

**Что происходит:**
1. Коммитит изменения
2. Пушит на GitHub
3. Подключается к серверу (SSH ключ Андрея)
4. Скачивает изменения
5. Перезапускает Docker (backend + frontend)
6. Тест-сервер обновлен!

**Андрей деплоит САМ! Не зависит от тебя.**

### Шаг 7: Тестировать САМ

- Открывает `https://test.optioner.online`
- Проверяет UI
- Тестирует с SPY, AAPL, TSLA
- Проверяет промпты если менял
- Если нужно → исправляет → повторяет шаги 4-7

### Шаг 8: Создать PR

- Заходит на GitHub
- Создает Pull Request
- Пишет: "Протестировал на test-сервере, все ОК"

### Шаг 9: Ты проверяешь

- Открываешь PR на GitHub
- Смотришь изменения
- Проверяешь что не сломал backend
- Жмешь "Merge pull request"

### Шаг 10: Ты деплоишь на production

```
Команда: /levon-deploy-prod
```

### Шаг 11: Проверяете вместе

- Открываете `https://optioner.online`
- Проверяете что все работает

---

## 🔄 Параллельная работа

### Сценарий: Вы работаете одновременно

**Левон:**
- Ветка: `feature/levon-backend-api`
- Деплоит на тест: `/levon-deploy-test`
- Тестирует backend

**Андрей:**
- Ветка: `feature/andrey-ui-homepage`
- Деплоит на тест: `/andrey-deploy-test`
- Тестирует UI

**Проблема:** Последний деплой перезаписывает тест-сервер!

**Решение:**

1. **Договориться кто тестирует:**
   ```
   Левон: "Андрей, я сейчас тестирую на test-сервере backend"
   Андрей: "Ок, я подожду"
   ```

2. **Или тестировать по очереди:**
   ```
   Левон тестирует → Андрей тестирует → Левон тестирует
   ```

3. **Или создать 2 тест-сервера** (если часто конфликтуете):
   ```
   test-backend.optioner.online (для Левона)
   test-frontend.optioner.online (для Андрея)
   ```

---

## 🐛 Частые сценарии

### Сценарий 1: Андрей изменил промпт

1. **Андрей:**
   - Редактирует `/backend/app/prompts/options_analysis_prompt.md`
   - `/andrey-test` → коммит + пуш + деплой на тест
   - Тестирует на `test.optioner.online` с SPY, AAPL
   - Создает PR

2. **Ты:**
   - Проверяешь PR
   - Мержишь
   - Деплоишь: `/levon-deploy-prod`

### Сценарий 2: Ты изменил API

1. **Ты:**
   - Добавляешь новый endpoint
   - `/levon-test` → коммит + пуш + деплой на тест
   - Тестируешь на `test.optioner.online`

2. **Обновляешь API Contract:**
   - Редактируешь `_docs/teamwork/forAndrey/API_CONTRACT.md`
   - Добавляешь новый endpoint

3. **Пишешь Андрею:**
   ```
   "Андрей, добавил новый endpoint /api/history
   Смотри обновленный API_CONTRACT.md"
   ```

4. **Создаешь PR, мержишь, деплоишь**

### Сценарий 3: Андрей сломал что-то

1. **Андрей задеплоил → что-то сломалось**

2. **Откат на тест-сервере:**
   ```
   Команда: /levon-deploy-test
   Указать: main (откатываемся на рабочую версию)
   ```

3. **Андрей исправляет:**
   - Фиксит баг
   - `/andrey-test` → деплоит снова
   - Тестирует

---

## ✅ Чеклист перед production деплоем

- [ ] Протестировано на `test.optioner.online`
- [ ] Все работает корректно
- [ ] PR создан и смержен в `main`
- [ ] Обновлен `API_CONTRACT.md` (если менял API)
- [ ] Андрей в курсе изменений (если менял API)
- [ ] Готов к деплою

**Деплой:**
```
/levon-deploy-prod
```

---

## 🎯 Итого

### Твой workflow:

```
1. /levon-create-branch
2. Работаешь (тестируешь локально)
3. /levon-test → коммит + пуш + деплой на тест
4. Тестируешь на test.optioner.online
5. PR → Merge
6. /levon-deploy-prod
```

### Workflow Андрея:

```
1. v0.app → Push to GitHub (создает ветку v0/component)
2. /andrey-pull-v0 → скачивает код
3. Дорабатывает
4. /andrey-test → коммит + пуш + деплой на тест
5. Тестирует САМ на test.optioner.online
6. PR → ты мержишь
7. Ты деплоишь: /levon-deploy-prod
```

**Время от идеи до production:** 15-30 минут

---

**Создано:** 2025-01-09
