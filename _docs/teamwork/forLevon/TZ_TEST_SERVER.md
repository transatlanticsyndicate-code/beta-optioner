# 📋 ТЗ: Развертывание тестового сервера

**Дата создания:** 2025-10-10  
**Цель:** Настроить изолированный тестовый сервер для совместной работы с Андреем

---

## 🎯 Цель проекта

Развернуть тестовое окружение `test.optioner.online` на текущем VPS, чтобы Андрей мог:
- Самостоятельно деплоить свои изменения
- Тестировать UI и промпты без участия Левона
- Быстро итерировать (v0.app → тест → исправления → тест)

---

## 🏗️ Архитектура

```
VPS (89.117.52.143)
├── Production (optioner.online)
│   ├── Frontend: порт 3000 → /home/deploy/app/frontend/build
│   └── Backend: порт 8000 → /home/deploy/app/backend
│
└── Test (test.optioner.online)
    ├── Frontend: порт 3001 → /var/www/test/frontend/build
    └── Backend: порт 8001 → /var/www/test/backend
```

**Изоляция:**
- Разные порты
- Разные директории
- Разные процессы (PM2)
- Отдельная база данных (test_optioner)

---

## ✅ Что нужно сделать ТЕБЕ (Левон)

### 1. Подготовка сервера (10 мин)

**Задачи:**
- [ ] Создать пользователя `andrey` на сервере
- [ ] Создать директорию `/var/www/test`
- [ ] Настроить права доступа для Андрея
- [ ] Создать SSH ключ для Андрея

**Команды для выполнения:**
```bash
# Подключиться к серверу
ssh root@optioner.online

# Создать пользователя andrey
sudo adduser andrey
sudo usermod -aG sudo andrey

# Создать директорию для тестового окружения
sudo mkdir -p /var/www/test
sudo chown -R andrey:andrey /var/www/test

# Создать SSH ключ для Андрея
ssh-keygen -t rsa -b 4096 -C "andrey@optioner.online" -f /root/.ssh/andrey_key
# Добавить публичный ключ в authorized_keys для пользователя andrey
sudo mkdir -p /home/andrey/.ssh
sudo cp /root/.ssh/andrey_key.pub /home/andrey/.ssh/authorized_keys
sudo chown -R andrey:andrey /home/andrey/.ssh
sudo chmod 700 /home/andrey/.ssh
sudo chmod 600 /home/andrey/.ssh/authorized_keys
```

**Результат:**
- Приватный ключ `/root/.ssh/andrey_key` → отправить Андрею
- Андрей сможет подключаться: `ssh -i andrey_key andrey@optioner.online`

---

### 2. DNS настройка (5 мин)

**Задачи:**
- [ ] Добавить A-запись для поддомена `test.optioner.online`

**Где настраивать:**
- Панель управления доменом (где куплен optioner.online)
- Добавить запись: `test.optioner.online` → `89.117.52.143`

**Проверка:**
```bash
# Через 5-10 минут проверить
ping test.optioner.online
# Должен отвечать 89.117.52.143
```

---

### 3. Передать Андрею SSH ключ (5 мин)

**Задачи:**
- [ ] Скачать приватный ключ с сервера
- [ ] Отправить Андрею безопасным способом
- [ ] Дать инструкцию по добавлению в Windsurf

**Команды:**
```bash
# Скачать ключ с сервера
scp root@optioner.online:/root/.ssh/andrey_key ~/Downloads/andrey_key

# Отправить Андрею (Telegram/Email/Secure share)
```

**Инструкция для Андрея:**
```
1. Сохрани файл andrey_key в безопасное место
2. В Windsurf: Settings → SSH → Add Key
3. Путь к ключу: /path/to/andrey_key
4. Host: optioner.online
5. User: andrey
```

---

### 4. Проверка доступа (5 мин)

**Задачи:**
- [ ] Андрей подключился по SSH
- [ ] Андрей может писать в `/var/www/test`

**Проверка:**
```bash
# Андрей выполняет на своей машине
ssh -i andrey_key andrey@optioner.online

# На сервере проверяет права
cd /var/www/test
touch test_file.txt  # Должно работать без ошибок
rm test_file.txt
```

---

## 🤖 Что нужно сделать МНЕ (Cascade)

### 1. Nginx конфигурация для test.optioner.online (10 мин)

**Задачи:**
- [ ] Создать конфиг `/etc/nginx/sites-available/test.optioner.online`
- [ ] Настроить проксирование на порты 3001 (frontend) и 8001 (backend)
- [ ] Включить конфиг и перезапустить Nginx

**Файл конфигурации:**
```nginx
server {
    listen 80;
    server_name test.optioner.online;

    # Frontend (React build)
    location / {
        root /var/www/test/frontend/build;
        try_files $uri $uri/ /index.html;
    }

    # Backend API
    location /api/ {
        proxy_pass http://127.0.0.1:8001/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Увеличенные таймауты для AI запросов
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }
}
```

---

### 2. SSL сертификат (Let's Encrypt) (5 мин)

**Задачи:**
- [ ] Установить certbot (если нет)
- [ ] Получить SSL сертификат для test.optioner.online
- [ ] Настроить автообновление

**Команды:**
```bash
# Установить certbot
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx

# Получить сертификат
sudo certbot --nginx -d test.optioner.online

# Проверить автообновление
sudo certbot renew --dry-run
```

---

### 3. Тестовая база данных (5 мин)

**Задачи:**
- [ ] Создать БД `test_optioner`
- [ ] Создать пользователя для тестовой БД
- [ ] Скопировать схему из production

**Команды:**
```bash
# Подключиться к PostgreSQL
sudo -u postgres psql

# Создать БД и пользователя
CREATE DATABASE test_optioner;
CREATE USER test_user WITH PASSWORD 'test_password_123';
GRANT ALL PRIVILEGES ON DATABASE test_optioner TO test_user;

# Скопировать схему (без данных)
pg_dump -U deploy -s optioner | psql -U test_user test_optioner
```

---

### 4. PM2 конфигурация для тестового окружения (10 мин)

**Задачи:**
- [ ] Создать PM2 конфиг для тестового backend
- [ ] Настроить переменные окружения для теста
- [ ] Запустить тестовый backend на порту 8001

**Файл:** `/var/www/test/ecosystem.config.js`
```javascript
module.exports = {
  apps: [
    {
      name: 'optioner-backend-test',
      script: '/var/www/test/backend/venv/bin/uvicorn',
      args: 'main:app --host 0.0.0.0 --port 8001',
      cwd: '/var/www/test/backend',
      interpreter: 'none',
      env: {
        DATABASE_URL: 'postgresql://test_user:test_password_123@localhost/test_optioner',
        OPENAI_API_KEY: process.env.OPENAI_API_KEY,
        ENVIRONMENT: 'test'
      }
    }
  ]
};
```

**Команды:**
```bash
# Запустить тестовый backend
pm2 start /var/www/test/ecosystem.config.js
pm2 save
```

---

### 5. Скрипт деплоя для Андрея (15 мин)

**Задачи:**
- [ ] Создать скрипт `/var/www/test/deploy.sh`
- [ ] Скрипт должен: pull → install → build → restart
- [ ] Дать права на выполнение

**Файл:** `/var/www/test/deploy.sh`
```bash
#!/bin/bash
set -e

echo "🚀 Начинаем деплой на test.optioner.online..."

# Переходим в директорию проекта
cd /var/www/test

# Pull последних изменений
echo "📥 Скачиваем изменения из GitHub..."
git pull origin main

# Backend
echo "🔧 Обновляем backend..."
cd backend
source venv/bin/activate
pip install -r requirements.txt
pm2 restart optioner-backend-test

# Frontend
echo "🎨 Собираем frontend..."
cd ../frontend
npm install
npm run build

echo "✅ Деплой завершен!"
echo "🌐 Проверь: https://test.optioner.online"
```

**Команды:**
```bash
chmod +x /var/www/test/deploy.sh
```

---

### 6. Обновить Windsurf команду `/andrey-test` (5 мин)

**Задачи:**
- [ ] Проверить команду `/andrey-test`
- [ ] Убедиться, что она использует правильный SSH ключ
- [ ] Протестировать деплой

**Файл:** `.windsurf/workflows/andrey-test.md`
```markdown
---
description: Коммит → Пуш → Деплой на тест (для Андрея)
---

# Деплой на тестовый сервер

1. Коммит изменений
2. Пуш в GitHub
3. Деплой на test.optioner.online

## Шаги:

// turbo
1. git add .
// turbo
2. git commit -m "feat: обновление UI/промптов"
// turbo
3. git push origin main
// turbo
4. ssh -i ~/.ssh/andrey_key andrey@optioner.online '/var/www/test/deploy.sh'

✅ Готово! Проверь: https://test.optioner.online
```

---

## 📋 Чеклист выполнения

### Левон делает:
- [ ] Создал пользователя `andrey` на сервере
- [ ] Создал директорию `/var/www/test`
- [ ] Создал SSH ключ для Андрея
- [ ] Добавил DNS запись для `test.optioner.online`
- [ ] Отправил SSH ключ Андрею
- [ ] Андрей подключился по SSH

### Cascade делает:
- [ ] Создал Nginx конфиг для test.optioner.online
- [ ] Получил SSL сертификат
- [ ] Создал тестовую БД
- [ ] Настроил PM2 для тестового backend
- [ ] Создал скрипт деплоя `/var/www/test/deploy.sh`
- [ ] Обновил команду `/andrey-test`

### Проверка:
- [ ] `https://test.optioner.online` открывается
- [ ] Backend отвечает на `/api/health`
- [ ] Андрей может деплоить через `/andrey-test`
- [ ] Изменения на test не влияют на production

---

## ⏱️ Время выполнения

**Левон:** ~25 минут  
**Cascade:** ~50 минут  
**Итого:** ~1 час 15 минут

---

## 🎯 Результат

После выполнения:
- ✅ Андрей деплоит САМ через `/andrey-test`
- ✅ Андрей тестирует САМ на `test.optioner.online`
- ✅ Левон не участвует в тестировании
- ✅ Быстрые итерации (5-10 минут от изменения до теста)
- ✅ Production изолирован от тестов

---

## 🚀 Следующие шаги после настройки

1. **Первый тестовый деплой:**
   - Андрей запускает `/andrey-test`
   - Проверяем, что все работает

2. **Обучение Андрея:**
   - Показать процесс: v0.app → `/andrey-pull-v0` → `/andrey-test`
   - Дать попробовать самому

3. **Начало работы:**
   - Андрей начинает работать автономно
   - Левон фокусируется на backend

---

**Создано:** 2025-10-10  
**Статус:** Готово к выполнению
