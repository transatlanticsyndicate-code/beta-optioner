# StrikeScale: Инструкция по тестированию

**Дата:** 17.10.2025  
**Этап:** 2.6 - Финальное тестирование с реальными данными  
**Коммит:** `d6df44c`

---

## 🎯 Цель тестирования

Проверить что StrikeScale корректно отображает:
1. ✅ Флаги позиций на правильных страйках
2. ✅ Правильные цвета флагов (красный/зеленый)
3. ✅ Количество контрактов в кружках
4. ✅ Y-levels (флаги не накладываются)
5. ✅ Флаг тикера (текущая цена)
6. ✅ Столбики объемов (если есть OI в данных)

---

## 📋 Сценарии тестирования

### Сценарий 1: Базовый тест с одним опционом

**Шаги:**
1. Откройте http://localhost:3000/tools/options-calculator-v2
2. Выберите тикер **SPY** (в поиске тикера)
3. Дождитесь загрузки данных (появятся даты экспирации)
4. Добавьте опцион: нажмите **"Buy CALL"**
5. В таблице опционов:
   - Выберите дату экспирации (например ближайшую)
   - Выберите страйк (например близкий к текущей цене)
6. Прокрутите вниз до блока **"StrikeScale"**

**Ожидаемый результат:**
- ✅ На шкале появился **красный флаг сверху** на выбранном страйке
- ✅ Флаг с **кружком "1"** (если quantity = 1, кружка может не быть)
- ✅ Флаг тикера (серый) на текущей цене SPY

**Как проверить:**
- Откройте консоль браузера (F12)
- Найдите логи:
  ```
  📊 StrikeScale render: { ticker, currentPrice, ... }
  🔄 Преобразование positions → flags
  ✅ Флаги созданы: { верхних: ..., нижних: ... }
  ```

---

### Сценарий 2: Несколько опционов разных типов

**Шаги:**
1. Добавьте **Buy CALL** на страйк выше текущей цены (+10)
2. Добавьте **Sell CALL** на страйк выше текущей цены (+20)
3. Добавьте **Buy PUT** на страйк ниже текущей цены (-10)
4. Добавьте **Sell PUT** на страйк ниже текущей цены (-20)

**Ожидаемый результат:**
- ✅ **Buy CALL** → Красный флаг сверху
- ✅ **Sell CALL** → Зеленый флаг снизу
- ✅ **Buy PUT** → Зеленый флаг снизу
- ✅ **Sell PUT** → Красный флаг сверху
- ✅ Все флаги на правильных страйках
- ✅ Если флаги близко (< 15 страйков) → разные Y-levels

**Логика цветов (для справки):**
```
Верхние флаги (прибыль при росте):
- Красный: Buy CALL или Sell PUT

Нижние флаги (прибыль при падении):
- Зеленый: Buy PUT или Sell CALL
```

---

### Сценарий 3: Группировка позиций

**Шаги:**
1. Добавьте **Buy CALL** на страйк 400
2. Добавьте еще один **Buy CALL** на тот же страйк 400
3. В таблице измените quantity первого на 2

**Ожидаемый результат:**
- ✅ Один флаг на страйке 400
- ✅ Кружок с цифрой **"3"** (2 + 1)
- ✅ Нет дублирования флагов

**Проверка в консоли:**
```
📦 Группировка завершена: { было: 2, стало: 1 }
```

---

### Сценарий 4: Множество близких позиций (Y-levels)

**Шаги:**
1. Добавьте 5-7 **Buy CALL** на страйках: 400, 402, 404, 406, 408, 410, 412
2. Все должны быть видимыми (visible=true)

**Ожидаемый результат:**
- ✅ Все флаги видны
- ✅ Флаги на разных Y-levels (не накладываются)
- ✅ Максимум 4 уровня (0, 1, 2, 3)

**Проверка:**
- Визуально: флаги расположены "лесенкой"
- В консоли: `уровнейВерх: 7`

---

### Сценарий 5: Изменение количества

**Шаги:**
1. Добавьте **Buy CALL** на страйк 400
2. В таблице измените quantity на **5**
3. Посмотрите на флаг

**Ожидаемый результат:**
- ✅ Кружок с цифрой **"5"**
- ✅ Размер кружка не изменился (20x20px)

---

### Сценарий 6: Скрытие позиции

**Шаги:**
1. Добавьте **Buy CALL**
2. Нажмите кнопку скрыть (глазик) в таблице
3. Посмотрите на шкалу

**Ожидаемый результат:**
- ✅ Флаг исчез со шкалы
- ✅ При раскрытии - флаг появился снова

---

## 🐛 Известные ограничения

1. **Столбики объемов пока не работают**
   - Причина: нужны данные OI для всех страйков с рынка
   - Сейчас options содержит только добавленные пользователем опционы
   - Решение: в будущем загружать полную цепочку опционов

2. **Диапазон страйков фиксированный (5100-5310)**
   - Хардкод в коде
   - Для SPY (цена ~$400-500) будет работать плохо
   - Решение: сделать динамический диапазон в Этапе 3

3. **Формат даты**
   - StrikeScale ожидает DD.MM.YY
   - Проверить что дата правильно конвертируется

---

## ✅ Чек-лист проверки

### Базовые функции
- [ ] Флаги появляются при добавлении опционов
- [ ] Флаги исчезают при удалении опционов
- [ ] Правильные цвета (красный/зеленый)
- [ ] Правильное расположение (верх/низ)
- [ ] Кружки с количеством отображаются
- [ ] Флаг тикера отображается (серый)

### Группировка
- [ ] Группировка по одинаковым позициям работает
- [ ] Quantity суммируется правильно
- [ ] Нет дублирования флагов

### Y-levels
- [ ] Близкие флаги не накладываются
- [ ] Используется максимум 4 уровня
- [ ] "Лесенка" выглядит логично

### Интерактивность (пока нет)
- [ ] ~~Drag & Drop~~ (будет в Этапе 3)
- [ ] ~~Tooltip при наведении~~ (будет позже)

### Производительность
- [ ] Рендер занимает < 100ms (проверить в React DevTools)
- [ ] Нет лагов при добавлении/удалении позиций
- [ ] Консоль без ошибок

---

## 🔍 Отладка

### Если флаги не появляются:

1. **Проверьте консоль:**
   ```javascript
   📊 StrikeScale render: { 
     ticker: "SPY",
     currentPrice: 450.23,
     optionsCount: 3,  // Должно быть > 0
     positionsCount: 0
   }
   ```

2. **Проверьте структуру опциона:**
   ```javascript
   sampleOption: {
     id: "...",
     action: "Buy",      // Должно быть "Buy" или "Sell"
     type: "CALL",       // Должно быть "CALL" или "PUT"
     strike: 450,        // Должен быть числом
     date: "15.11.25",   // Должен быть строкой
     quantity: 1         // Может быть отрицательным
   }
   ```

3. **Проверьте фильтр:**
   - StrikeScale фильтрует только CALL и PUT
   - Базовые активы (акции) не отображаются

---

### Если цвета неправильные:

**Логика цветов:**
```javascript
isTopFlag(position) {
  return (type === 'PUT' && action === 'Sell') 
      || (type === 'CALL' && action === 'Buy');
}

calculateFlagColor(position) {
  if (isTopFlag(position)) {
    return 'rgb(255, 107, 107)'; // Красный
  } else {
    return 'rgb(76, 175, 80)';   // Зеленый
  }
}
```

**Примеры:**
- Buy CALL → isTopFlag=true → красный, сверху ✅
- Sell CALL → isTopFlag=false → зеленый, снизу ✅
- Buy PUT → isTopFlag=false → зеленый, снизу ✅
- Sell PUT → isTopFlag=true → красный, сверху ✅

---

## 📸 Скриншоты для проверки

После тестирования сделайте скриншоты:

1. **Пустая шкала** (без опционов)
2. **Один опцион** (Buy CALL)
3. **4 разных типа** (Buy/Sell CALL/PUT)
4. **Группировка** (несколько на одном страйке)
5. **Y-levels** (много близких позиций)

Сохраните в `_docs/calculator/screenshots/strikescale-testing/`

---

## 🎉 Критерии успешного завершения Этапа 2

**Этап 2 считается завершенным если:**

✅ Все базовые функции работают  
✅ Группировка работает корректно  
✅ Y-levels предотвращают наложение  
✅ Нет критичных багов  
✅ Производительность < 100ms  
✅ Консоль без ошибок  

**После успешного тестирования:**
1. Закоммитить: "Этап 2: Завершен - динамические данные работают"
2. Обновить ТЗ: отметить Этап 2 как завершенный
3. Перейти к Этапу 3: Интерактивность (Drag & Drop)

---

**Удачи в тестировании! 🚀**
