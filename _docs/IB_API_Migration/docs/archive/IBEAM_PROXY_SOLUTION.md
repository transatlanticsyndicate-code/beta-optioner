# 🔐 IBEAM Secure Proxy - Решение для удаленного доступа

**Дата:** 23 октября 2025  
**Статус:** ✅ Работает (но медленно)  
**Рекомендация:** Использовать только для production, для разработки - локальный IBEAM

---

## 🎯 Проблема

IBEAM в Docker контейнере принимает запросы **только изнутри контейнера**, а не с хоста:

```bash
# ❌ НЕ РАБОТАЕТ (с хоста)
curl https://localhost:5000/v1/api/iserver/auth/status
# Ответ: Access Denied

# ✅ РАБОТАЕТ (изнутри контейнера)
docker exec ibeam curl https://localhost:5000/v1/api/iserver/auth/status
# Ответ: {"authenticated": true, ...}
```

**Причина:** IB Gateway внутри контейнера слушает только на `127.0.0.1`, а не на `0.0.0.0`.

---

## 💡 Решение: Secure Proxy через Docker Exec

Создан прокси скрипт который:
1. Принимает HTTP запросы от клиентов
2. Проверяет API ключ
3. Выполняет запрос через `docker exec`
4. Возвращает результат клиенту

### Архитектура

```
Локальная машина (разработка)
    ↓ HTTPS + API Key
    ↓ https://test.optioner.online/api/ibeam-secure/...
    ↓
Production сервер (89.117.52.143)
    ↓ FastAPI Backend (прокси скрипт)
    ↓ Проверяет X-API-Key
    ↓ Выполняет: docker exec ibeam curl ...
    ↓
IBEAM Docker контейнер
    ↓ IB Gateway (localhost:5000)
    ↓ Возвращает данные
    ↓
FastAPI Backend
    ↓ Возвращает клиенту
    ↓
Локальная машина
```

---

## 📁 Файлы

### 1. Прокси скрипт
**Файл:** `backend/app/api/ibeam_secure_proxy.py`

```python
def _make_docker_request(path: str, method: str = "GET", params: dict = None, data: bytes = None):
    """Делает запрос к IBEAM через docker exec"""
    import subprocess
    
    # Определить контейнер
    ibeam_port = os.getenv("IBEAM_PORT", "5000")
    container_name = "ibeam" if ibeam_port == "5000" else "ibeam-test"
    
    # Построить URL
    url = f"https://localhost:5000/v1/{path}"
    if params:
        query = "&".join([f"{k}={v}" for k, v in params.items()])
        url = f"{url}?{query}"
    
    # Выполнить через docker exec
    cmd = ["docker", "exec", container_name, "curl", "-s", "-k", "-w", "\\n%{http_code}", "-X", method, url]
    
    result = subprocess.run(cmd, capture_output=True, text=True, timeout=30)
    # ... парсинг результата
```

### 2. Подключение в main.py
```python
from app.api import ibeam_secure_proxy

app.include_router(ibeam_secure_proxy.router)
```

### 3. Переменные окружения (.env)
```bash
IBEAM_PROXY_API_KEY=x9fK-yZbKAQE2xkBLkHdIkmY6pYi0tjVb28ZZnwnxSA
IBEAM_PORT=5000  # 5000 для production, 5001 для test
```

---

## 🚀 Использование

### Из локальной разработки

```python
import os
from app.services.ib_client import IBClient

# Настроить IBClient для использования прокси
os.environ["IB_API_URL"] = "https://test.optioner.online/api/ibeam-secure"
os.environ["IBEAM_PROXY_API_KEY"] = "x9fK-yZbKAQE2xkBLkHdIkmY6pYi0tjVb28ZZnwnxSA"

# Сбросить Singleton
IBClient._instance = None

# Использовать как обычно
client = IBClient()
price = client.get_stock_price("AAPL")
```

### Прямые запросы через curl

```bash
# Auth status
curl -H "X-API-Key: x9fK-yZbKAQE2xkBLkHdIkmY6pYi0tjVb28ZZnwnxSA" \
  https://test.optioner.online/api/ibeam-secure/portal/iserver/auth/status

# Search contract
curl -H "X-API-Key: x9fK-yZbKAQE2xkBLkHdIkmY6pYi0tjVb28ZZnwnxSA" \
  "https://test.optioner.online/api/ibeam-secure/api/iserver/secdef/search?symbol=AAPL"

# Health check
curl -H "X-API-Key: x9fK-yZbKAQE2xkBLkHdIkmY6pYi0tjVb28ZZnwnxSA" \
  https://test.optioner.online/api/ibeam-secure/health
```

---

## ✅ Преимущества

1. **Безопасность**
   - Защищен API ключом
   - Работает через HTTPS
   - Не нужен SSH туннель

2. **Простота**
   - Не нужно настраивать IBEAM
   - Работает "из коробки"
   - Единый endpoint для всех

3. **Надежность**
   - Использует существующий IBEAM
   - Не требует дополнительных портов
   - Работает через Docker

---

## ❌ Недостатки

### 1. Медленная производительность

**Проблема:** Каждый запрос = новый процесс `docker exec`

```bash
# Тест скорости
time curl -H "X-API-Key: ..." https://test.optioner.online/api/ibeam-secure/portal/iserver/auth/status
# Результат: ~1 секунда на запрос
```

**Почему медленно:**
- Docker exec создает новый процесс
- Нет переиспользования соединений
- Нет HTTP keep-alive
- Каждый curl запускается заново

**Влияние на IBClient:**
- Получение цены: ~2-3 секунды (2-3 запроса)
- Получение дат экспирации: ~3-5 секунд (3-4 запроса)
- Получение опционной цепочки: ~30-60 секунд (20-50 запросов)

### 2. Нет кэширования

Каждый запрос идет в IBEAM заново, даже если данные уже запрашивались.

### 3. Ограничения Docker

- Требует Docker на сервере
- Требует прав на `docker exec`
- Может быть проблемы с безопасностью

---

## 🎯 Рекомендации по использованию

### ✅ Когда использовать прокси:

1. **Production деплой**
   - Когда нужен удаленный доступ к IBEAM
   - Для мониторинга и отладки
   - Для редких запросов

2. **CI/CD тесты**
   - Интеграционные тесты
   - Проверка работоспособности

3. **Демонстрация**
   - Показать что IBEAM работает
   - Проверить подключение

### ❌ Когда НЕ использовать прокси:

1. **Локальная разработка**
   - Слишком медленно
   - Неудобно отлаживать
   - **Решение:** Локальный IBEAM через Docker Desktop

2. **Частые запросы**
   - Получение опционных цепочек
   - Real-time данные
   - **Решение:** Локальный IBEAM

3. **Production API**
   - Высокая нагрузка
   - Требуется скорость
   - **Решение:** Прямое подключение к IBEAM

---

## 🚀 Альтернативные решения

### Вариант 1: Локальный IBEAM (РЕКОМЕНДУЕТСЯ для разработки)

**Скорость:** 🚀🚀🚀 (в 10-20 раз быстрее)

```bash
# Запустить IBEAM локально
cd ~/ibeam-local
docker-compose up -d

# Использовать напрямую
export IB_API_URL="https://localhost:5000"
python backend/tests/test_ib_simple.py
```

**Преимущества:**
- ✅ Очень быстро (50-100ms на запрос)
- ✅ Нет зависимости от сервера
- ✅ Можно отлаживать
- ✅ Работает offline

**Недостатки:**
- ❌ Нужен Docker Desktop
- ❌ Требует 2FA каждый день
- ❌ Занимает ресурсы локально

### Вариант 2: Оптимизированный прокси

**Идеи для оптимизации:**
1. Использовать persistent connection вместо docker exec
2. Добавить Redis кэш для часто запрашиваемых данных
3. Батчинг запросов (объединять несколько в один)
4. WebSocket для real-time данных

### Вариант 3: Настроить IBEAM на 0.0.0.0

**Изменить конфигурацию IBEAM:**
```yaml
services:
  ibeam:
    environment:
      IBEAM_GATEWAY_BASE_URL: "0.0.0.0:5000"
```

**Проблема:** Может не работать из-за ограничений IB Gateway

---

## 📊 Сравнение решений

| Параметр | Прокси (docker exec) | Локальный IBEAM | Прямой доступ |
|----------|---------------------|-----------------|---------------|
| Скорость | 🐌 1000ms | 🚀 50-100ms | 🚀 50-100ms |
| Настройка | ✅ Простая | ⚠️ Средняя | ❌ Сложная |
| Безопасность | ✅ API Key | ⚠️ Локально | ❌ Открыт |
| Для разработки | ❌ Медленно | ✅ Отлично | ⚠️ Сложно |
| Для production | ✅ Подходит | ❌ Нет | ✅ Идеально |

---

## 🔧 Troubleshooting

### Проблема: "Invalid API key"

```bash
# Проверить API ключ в .env
grep IBEAM_PROXY_API_KEY /var/www/test/backend/.env

# Проверить что ключ совпадает
echo $IBEAM_PROXY_API_KEY
```

### Проблема: "Container not found"

```bash
# Проверить что контейнер запущен
docker ps | grep ibeam

# Проверить имя контейнера
docker ps --format "{{.Names}}"
```

### Проблема: Медленные запросы

**Решение:** Использовать локальный IBEAM для разработки

```bash
cd ~/ibeam-local
docker-compose up -d
export IB_API_URL="https://localhost:5000"
```

---

## 📝 История изменений

### 23 октября 2025 - v1.0
- ✅ Создан прокси скрипт с docker exec
- ✅ Добавлена авторизация по API ключу
- ✅ Протестировано на test сервере
- ⚠️ Обнаружена проблема с производительностью
- 📋 Рекомендовано использовать локальный IBEAM для разработки

---

## 🎯 Следующие шаги

1. ✅ Прокси работает (медленно)
2. 🔄 Настроить локальный IBEAM для разработки
3. 📋 Оптимизировать прокси для production (опционально)
4. 🚀 Интегрировать в основное приложение

---

**Подготовил:** Cascade AI  
**Дата:** 23 октября 2025  
**Версия:** 1.0
