# 🐳 Локальный IBEAM через Docker - Полное руководство

**Дата:** 23 октября 2025  
**Статус:** ✅ Протестировано и работает  
**Версия:** 1.0

---

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Установка и настройка](#установка-и-настройка)
3. [Проверка подключения](#проверка-подключения)
4. [API Endpoints](#api-endpoints)
5. [Тестовые скрипты](#тестовые-скрипты)
6. [Производительность](#производительность)
7. [Troubleshooting](#troubleshooting)
8. [Сравнение с альтернативами](#сравнение-с-альтернативами)

---

## 🚀 Быстрый старт

### Требования
- Docker Desktop установлен (версия 28.5.1+)
- macOS или Linux
- ~2GB свободного места
- Интернет для скачивания образа

### За 3 минуты

```bash
# 1. Перейти в директорию
cd ~/ibeam-local

# 2. Запустить контейнер
docker compose up -d

# 3. Дождаться авторизации (30-60 сек)
docker compose logs -f

# 4. Проверить что работает
curl -k https://localhost:5001/v1/portal/iserver/auth/status

# Должен вернуть: {"authenticated": true, ...}
```

---

## 📦 Установка и настройка

### Шаг 1: Создать директорию

```bash
mkdir -p ~/ibeam-local
cd ~/ibeam-local
```

### Шаг 2: Создать docker-compose.yml

```yaml
services:
  ibeam:
    image: voyz/ibeam:latest
    container_name: ibeam-local
    restart: unless-stopped
    
    environment:
      IBEAM_ACCOUNT: bogda6172
      IBEAM_PASSWORD: 19642014angel
      IBEAM_TRADING_MODE: paper
      
    ports:
      - '5001:5000'
    
    volumes:
      - ./outputs:/srv/ibeam/outputs
      - ./inputs:/srv/ibeam/inputs
```

### Шаг 3: Запустить контейнер

```bash
docker compose up -d
```

### Шаг 4: Проверить логи

```bash
docker compose logs --tail 50 -f
```

**Ожидаемый вывод:**
```
ibeam-local | 2025-10-23 17:09:59,474|I| AUTHENTICATED Status(running=True, session=True, ...)
ibeam-local | 2025-10-23 17:09:59,565|I| Gateway running and authenticated, session id: a0db0031ea48a68aa8facf0e064392cc
```

### Шаг 5: Остановить (когда нужно)

```bash
docker compose down
```

---

## 🔍 Проверка подключения

### Быстрая проверка

```bash
# Проверить что контейнер запущен
docker ps | grep ibeam-local

# Проверить логи
docker compose logs --tail 5

# Проверить авторизацию
curl -k https://localhost:5001/v1/portal/iserver/auth/status
```

### Полная проверка

```bash
# Запустить тестовый скрипт
cd /path/to/windsurf-project/backend
python tests/test_aapl_final.py
```

**Ожидаемый результат:**
```
✅ Authenticated: True
✅ Symbol: AAPL
✅ Company: APPLE INC
💰 Last Price: $260.45
📊 Bid: $260.43
📊 Ask: $260.47
✅ Найдено дат: 16
✅ Найдено страйков: 63
```

---

## 🔌 API Endpoints

### Базовый URL
```
https://localhost:5001/v1
```

### Основные endpoints

#### 1. Авторизация
```bash
GET /portal/iserver/auth/status

# Ответ:
{
  "authenticated": true,
  "connected": true,
  "serverInfo": {
    "serverName": "JifZ20059",
    "serverVersion": "Build 10.40.1b, Sep 24, 2025 12:16:06 PM"
  }
}
```

#### 2. Поиск контракта
```bash
GET /api/iserver/secdef/search?symbol=AAPL

# Ответ:
[
  {
    "conid": 265598,
    "symbol": "AAPL",
    "companyName": "APPLE INC",
    "sections": [
      {
        "secType": "OPT",
        "months": "OCT25;NOV25;DEC25;JAN26;..."
      }
    ]
  }
]
```

#### 3. Получить цену
```bash
GET /api/iserver/marketdata/snapshot?conids=265598&fields=31,84,86

# Поля:
# 31 = Last Price
# 84 = Bid
# 86 = Ask

# Ответ:
[
  {
    "conid": 265598,
    "31": "260.45",
    "84": "260.43",
    "86": "260.47"
  }
]
```

#### 4. Получить страйки
```bash
GET /api/iserver/secdef/strikes?conid=265598&sectype=OPT&month=NOV25

# Ответ:
{
  "call": [100.0, 105.0, 110.0, 115.0, ...],
  "put": [100.0, 105.0, 110.0, 115.0, ...]
}
```

#### 5. Получить информацию об опционе
```bash
GET /api/iserver/secdef/info?conid=265598&sectype=OPT&month=NOV25&strike=260.0&right=C

# Ответ:
[
  {
    "conid": 123456789,
    "symbol": "AAPL NOV25 260 C",
    "description": "APPLE INC NOV 2025 260 CALL"
  }
]
```

#### 6. Получить данные опциона
```bash
GET /api/iserver/marketdata/snapshot?conids=123456789&fields=31,84,86,100,101,104,106,107,108,109

# Поля:
# 31 = Last
# 84 = Bid
# 86 = Ask
# 100 = IV (Implied Volatility)
# 101 = Volume
# 104 = Delta
# 106 = Gamma
# 107 = Theta
# 108 = Vega
# 109 = Rho
```

---

## 🧪 Тестовые скрипты

### 1. Быстрая проверка скорости
**Файл:** `backend/tests/test_speed_comparison.py`

```bash
python backend/tests/test_speed_comparison.py
```

**Результат:**
```
Локальный IBEAM:  112ms
Прокси (docker):  475ms
Ускорение: в 4.2 раз!
```

### 2. Полный тест AAPL
**Файл:** `backend/tests/test_aapl_final.py`

```bash
python backend/tests/test_aapl_final.py
```

**Результат:**
```
🍎 ОСНОВНЫЕ ДАННЫЕ:
   Symbol: AAPL
   Company: APPLE INC
   ConID: 265598

💰 ЦЕНЫ:
   Last: $260.45
   Bid: $260.43
   Ask: $260.47
   Spread: $0.04

📅 ОПЦИОНЫ:
   Дат экспирации: 16
   Ближайшие: OCT25, NOV25, DEC25
   Страйков для NOV25: 63
```

### 3. Локальный IBEAM с IBClient
**Файл:** `backend/tests/test_local_ibeam.py`

```bash
python backend/tests/test_local_ibeam.py
```

**Использование IBClient:**
```python
import os
from app.services.ib_client import IBClient

# Настроить на локальный IBEAM
os.environ["IB_API_URL"] = "https://localhost:5001"
os.environ.pop("IBEAM_PROXY_API_KEY", None)  # Убрать API ключ

# Сбросить Singleton
IBClient._instance = None

# Использовать
client = IBClient()
price = client.get_stock_price("AAPL")
dates = client.get_expiration_dates("AAPL")
chain = client.get_options_chain("AAPL", "NOV25")
```

### 4. Прямые HTTP запросы
**Файл:** `backend/tests/test_aapl_final.py`

```bash
# Проверить авторизацию
curl -k https://localhost:5001/v1/portal/iserver/auth/status

# Получить цену AAPL
curl -k "https://localhost:5001/v1/api/iserver/marketdata/snapshot?conids=265598&fields=31,84,86"

# Получить даты экспирации
curl -k "https://localhost:5001/v1/api/iserver/secdef/search?symbol=AAPL"

# Получить страйки
curl -k "https://localhost:5001/v1/api/iserver/secdef/strikes?conid=265598&sectype=OPT&month=NOV25"
```

---

## ⚡ Производительность

### Измеренные результаты (23 октября 2025)

| Операция | Время | Примечание |
|----------|-------|-----------|
| Auth check | 98ms | Первый запрос |
| Поиск контракта | 796ms | Включает парсинг дат |
| Получение цены | 139ms | 3 поля (Last, Bid, Ask) |
| Даты экспирации | 183ms | Парсинг из поиска |
| Страйки | 495ms | 63 страйка для одной даты |
| **Итого** | **~1.7 сек** | Полный цикл |

### Сравнение с альтернативами

| Метод | Скорость | Преимущества | Недостатки |
|-------|----------|-------------|-----------|
| **Локальный IBEAM** | 🚀 100-200ms | Быстро, надежно, offline | Требует Docker |
| **Прокси (docker exec)** | 🐌 400-500ms | Работает везде | Медленно, зависит от сервера |
| **SSH туннель** | 🐌 1000ms+ | Безопасно | Очень медленно |
| **Прямой доступ** | 🚀 100-200ms | Быстро | Сложно настроить |

---

## 🔧 Troubleshooting

### Проблема: "Cannot connect to Docker daemon"

**Решение:**
```bash
# Запустить Docker Desktop
open -a Docker

# Или проверить статус
docker ps
```

### Проблема: "Port 5001 already in use"

**Решение:**
```bash
# Найти что занимает порт
lsof -i :5001

# Использовать другой порт в docker-compose.yml
ports:
  - '5002:5000'  # Изменить на 5002

# Обновить IBClient
export IB_API_URL="https://localhost:5002"
```

### Проблема: "Authentication failed"

**Решение:**
```bash
# Проверить логи
docker compose logs | grep -i "auth\|error"

# Перезапустить контейнер
docker compose restart

# Проверить credentials в docker-compose.yml
# IBEAM_ACCOUNT: bogda6172
# IBEAM_PASSWORD: 19642014angel
```

### Проблема: "Connection refused"

**Решение:**
```bash
# Проверить что контейнер запущен
docker ps | grep ibeam-local

# Проверить логи
docker compose logs --tail 20

# Дождаться "Gateway running and authenticated"
docker compose logs -f
```

### Проблема: "Цены N/A"

**Решение:**
```bash
# Рынок может быть закрыт (после 16:00 NY)
# Проверить через curl
curl -k "https://localhost:5001/v1/api/iserver/marketdata/snapshot?conids=265598&fields=31,84,86"

# Если цены есть через curl но не в Python - проблема в парсинге
# Проверить что используются правильные ключи: '31', '84', '86' (строки)
```

---

## 📊 Сравнение с альтернативами

### Вариант 1: Локальный IBEAM (РЕКОМЕНДУЕТСЯ) ✅

**Когда использовать:**
- Локальная разработка
- Быстрое тестирование
- Работа offline

**Преимущества:**
- ✅ Очень быстро (100-200ms)
- ✅ Не зависит от сервера
- ✅ Можно отлаживать
- ✅ Работает offline

**Недостатки:**
- ❌ Нужен Docker Desktop
- ❌ Требует 2FA каждый день
- ❌ Занимает ресурсы локально

**Установка:**
```bash
cd ~/ibeam-local
docker compose up -d
```

### Вариант 2: Прокси через docker exec

**Когда использовать:**
- Удаленный доступ из локальной разработки
- Редкие запросы
- Демонстрация

**Преимущества:**
- ✅ Защищен API ключом
- ✅ Работает везде
- ✅ Не нужна локальная настройка

**Недостатки:**
- ❌ Медленно (400-500ms)
- ❌ Зависит от сервера
- ❌ Требует интернета

**Использование:**
```python
os.environ["IB_API_URL"] = "https://test.optioner.online/api/ibeam-secure"
os.environ["IBEAM_PROXY_API_KEY"] = "x9fK-yZbKAQE2xkBLkHdIkmY6pYi0tjVb28ZZnwnxSA"
```

**Документация:** `_docs/architecture/IBEAM_PROXY_SOLUTION.md`

### Вариант 3: Прямой доступ к IBEAM на сервере

**Когда использовать:**
- Production
- High-performance приложения
- Когда нужна скорость

**Преимущества:**
- ✅ Очень быстро (100-200ms)
- ✅ Надежно
- ✅ Масштабируемо

**Недостатки:**
- ❌ Сложно настроить
- ❌ Требует доступа к серверу
- ❌ Проблемы с безопасностью

---

## 📝 Учетные данные

### IBEAM Account
```
Account: bogda6172
Password: 19642014angel
Mode: Paper Trading
```

### IB Account
```
Account ID: DU4883788
Name: BOHDAN KOPYTKO
Type: DEMO (Paper Trading)
Currency: USD
```

---

## 🔗 Ссылки

### Документация
- [IBEAM GitHub](https://github.com/Voyz/ibeam)
- [IB Client Portal API](https://www.interactivebrokers.com/campus/ibkr-api-page/cpapi-v1/)
- [IB API Forum](https://groups.io/g/twsapi)

### Локальные файлы
- Тесты: `backend/tests/test_*.py`
- IBClient: `backend/app/services/ib_client.py`
- Прокси: `backend/app/api/ibeam_secure_proxy.py`
- Конфиг: `~/ibeam-local/docker-compose.yml`

### Альтернативная документация
- Прокси решение: `_docs/architecture/IBEAM_PROXY_SOLUTION.md`
- IBEAM Setup: `__Levon_tasks/IB_API_Migration/docs/archive/IBEAM_SETUP.md`

---

## 🎯 Рекомендации

### Для разработки
1. ✅ Используй локальный IBEAM (порт 5001)
2. ✅ Запускай тесты перед каждым коммитом
3. ✅ Проверяй скорость через `test_speed_comparison.py`

### Для production
1. ✅ Используй прямой доступ к IBEAM на сервере
2. ✅ Или оптимизированный прокси
3. ✅ Добавь кэширование для часто запрашиваемых данных

### Для CI/CD
1. ✅ Используй прокси для интеграционных тестов
2. ✅ Или запускай локальный IBEAM в Docker контейнере
3. ✅ Проверяй что все endpoints работают

---

## 📊 Статистика

**Дата тестирования:** 23 октября 2025  
**Время:** 20:15 UTC+3  
**Рынок:** Закрыт (после 16:00 NY)  
**Данные:** Последние закрывающие цены  

### Результаты
- ✅ Авторизация: успешна
- ✅ Поиск контрактов: работает
- ✅ Получение цен: работает
- ✅ Даты экспирации: 16 дат
- ✅ Страйки: 63 страйка
- ✅ Скорость: 4.2x быстрее чем прокси

---

## 🚀 Следующие шаги

1. ✅ Локальный IBEAM настроен и работает
2. 🔄 Интегрировать в основное приложение
3. 📋 Добавить кэширование для оптимизации
4. 🧪 Написать unit-тесты с моками
5. 📊 Добавить мониторинг и логирование

---

**Подготовил:** Cascade AI  
**Дата:** 23 октября 2025  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
