# 📊 Поток данных в Options Flow AI Analyzer

## Схема потока данных

```
┌─────────────────┐
│   Polygon.io    │
│   API           │
└────────┬────────┘
         │
         ├─► get_stock_price(ticker)
         │   └─► Stock Data (9 полей)
         │
         └─► get_options_chain(ticker)
             └─► Options Data (50 контрактов × 15 полей)
                 │
                 ▼
         ┌───────────────────┐
         │   Calculations    │
         │   (calculations.py)│
         └────────┬──────────┘
                  │
                  ├─► calculate_max_pain()
                  ├─► calculate_put_call_ratio()
                  ├─► calculate_gamma_exposure()
                  └─► find_key_levels()
                      │
                      ▼
              ┌──────────────┐
              │   Metrics    │
              │  (сводные)   │
              └──────┬───────┘
                     │
                     ├─► Format для Gemini
                     │   (5 основных значений)
                     │
                     ▼
              ┌──────────────┐
              │  Gemini AI   │
              │  (анализ)    │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │   Response   │
              │   (JSON)     │
              └──────────────┘
```

---

## 1. Данные из Polygon.io

### 📈 Stock Data (get_stock_price)

**Endpoint:** `GET /v2/aggs/ticker/{ticker}/prev`

**Что получаем:**
```json
{
  "ticker": "SPY",
  "price": 669.21,           // Цена закрытия
  "open": 669.99,            // Цена открытия
  "high": 672.675,           // Максимум дня
  "low": 668.16,             // Минимум дня
  "change": -0.78,           // Изменение в $
  "change_percent": -0.12,   // Изменение в %
  "volume": 70494350.0,      // Объем торгов
  "timestamp": "2025-10-03T23:00:00"
}
```

**Используется:**
- ✅ `price` - для расчета метрик и сравнения с Max Pain
- ✅ `change`, `change_percent` - для отображения в UI
- ✅ `volume` - для контекста анализа
- ❌ Остальное - для справки

---

### 📊 Options Data (get_options_chain)

**Endpoint:** `GET /v3/reference/options/contracts`

**Что получаем:** 50 контрактов (ограничено для MVP)

**Структура каждого контракта:**
```json
{
  "ticker": "O:SPY251010C00450000",
  "underlying": "SPY",
  "expiration_date": "2025-10-10",
  "strike": 450.0,
  "option_type": "call",          // или "put"
  "open_interest": 0,             // Открытый интерес
  "volume": 0,                    // Объем торгов
  "bid": 0,                       // Цена покупки
  "ask": 0,                       // Цена продажи
  "last_price": 0,                // Последняя цена
  "implied_volatility": 0,        // Подразумеваемая волатильность
  "delta": 0,                     // Греки
  "gamma": 0,
  "theta": 0,
  "vega": 0
}
```

**Всего данных:** 50 контрактов × 15 полей = **750 значений**

**Используется:**
- ✅ `strike` - для всех расчетов
- ✅ `option_type` - для разделения на calls/puts
- ✅ `open_interest` - для Max Pain и ключевых уровней
- ✅ `volume` - для Put/Call Ratio
- ✅ `gamma` - для Gamma Exposure
- ❌ `bid`, `ask`, `last_price` - пока не используется
- ❌ `delta`, `theta`, `vega` - для будущих фич

---

## 2. Рассчитанные метрики

### 🧮 Что мы рассчитываем:

#### 2.1 Max Pain
```python
calculate_max_pain(options_data) -> float

Результат: 450.0
```
**Алгоритм:**
1. Для каждого страйка рассчитываем суммарную "боль" держателей опционов
2. Находим страйк с минимальной болью
3. Это и есть Max Pain

**Используется:** Open Interest всех контрактов

---

#### 2.2 Put/Call Ratio
```python
calculate_put_call_ratio(options_data) -> dict

Результат:
{
  "volume_ratio": 0.85,        // PUT volume / CALL volume
  "oi_ratio": 0.92,            // PUT OI / CALL OI
  "total_call_volume": 123456,
  "total_put_volume": 104838,
  "total_call_oi": 234567,
  "total_put_oi": 215881
}
```

**Используется:** Volume и Open Interest всех контрактов

---

#### 2.3 Gamma Exposure
```python
calculate_gamma_exposure(options_data, current_price) -> dict

Результат:
{
  "total_gamma": 1500000.0,
  "call_gamma": 900000.0,
  "put_gamma": 600000.0,
  "net_gamma": 300000.0      // Положительный = стабилизирующий
}
```

**Используется:** Gamma и Open Interest всех контрактов

---

#### 2.4 Key Levels
```python
find_key_levels(options_data, current_price) -> dict

Результат:
{
  "support_levels": [
    {"strike": 458, "oi": 15000, "strength": "strong"},
    {"strike": 456, "oi": 12000, "strength": "medium"}
  ],
  "resistance_levels": [
    {"strike": 462, "oi": 12000, "strength": "strong"},
    {"strike": 465, "oi": 8000, "strength": "medium"}
  ],
  "max_oi_strike": 460
}
```

**Алгоритм:**
1. Группируем OI по страйкам
2. Поддержка = PUT OI ниже текущей цены
3. Сопротивление = CALL OI выше текущей цены
4. Топ-5 самых значимых уровней

---

## 3. Что передается в Gemini

### 📝 Форматированный промпт

**Из 750 значений Polygon → 5 ключевых метрик:**

```
ОБРАЗОВАТЕЛЬНЫЙ АНАЛИЗ ОПЦИОННЫХ ДАННЫХ
(Только для обучения, не является инвестиционной рекомендацией)

Тикер: SPY
Текущая цена: 669.21
Max Pain уровень: 450.00
Put/Call Ratio: 0.85
Gamma Exposure: 300000
Ключевые страйки: 458 PUT, 456 PUT, 462 CALL, 465 CALL

Задача: Предоставь детальное объективное описание...
```

**Объем данных:**
- Исходные данные: ~750 значений (15 KB)
- Передаем в Gemini: ~5 значений (~200 байт)
- **Сжатие: 99.7%** 🎯

---

## 4. Ответ от Gemini

### 🤖 AI Analysis

**Что получаем:**
- Текст анализа: 800-1200 токенов (~3000-4500 символов)
- Структурированный по 5 разделам
- На русском языке

**Пример:**
```
ОБРАЗОВАТЕЛЬНЫЙ АНАЛИЗ ОПЦИОННЫХ ДАННЫХ ДЛЯ SPY

1. НАБЛЮДАЕМЫЕ ЦЕНОВЫЕ УРОВНИ
- Max Pain уровень: 450.00
- Расстояние от текущей цены: 219.21 (32.7%)
- Значимость: потенциальный магнит для цены...

2. ТЕКУЩИЙ РЫНОЧНЫЙ СЕНТИМЕНТ
- Put/Call Ratio: 0.85 - умеренно-бычий
- Преобладание CALL позиций...

... (еще 3 раздела)
```

---

## 5. Итоговый Response

### 📤 JSON Response для Frontend

```json
{
  "status": "success",
  "ticker": "SPY",
  
  "stock_data": {
    "price": 669.21,
    "change": -0.78,
    "change_percent": -0.12,
    "volume": 70494350.0,
    "timestamp": "2025-10-03T23:00:00"
  },
  
  "metrics": {
    "max_pain": 450.0,
    "put_call_ratio": {
      "volume_ratio": 0.85,
      "oi_ratio": 0.92,
      ...
    },
    "gamma_exposure": {
      "net_gamma": 300000.0,
      ...
    },
    "key_levels": {
      "support_levels": [...],
      "resistance_levels": [...],
      ...
    },
    "current_price": 669.21,
    "total_contracts": 50
  },
  
  "ai_analysis": "ОБРАЗОВАТЕЛЬНЫЙ АНАЛИЗ...",
  "ai_provider": "gemini"
}
```

**Размер ответа:** ~5-10 KB

---

## 📊 Сводная таблица

| Этап | Источник | Объем данных | Что содержит |
|------|----------|--------------|--------------|
| 1 | Polygon Stock API | 9 полей | Цена, объем, OHLC |
| 2 | Polygon Options API | 750 значений | 50 контрактов × 15 полей |
| 3 | Calculations | 20 значений | Max Pain, P/C Ratio, GEX, Levels |
| 4 | Gemini Prompt | 5 значений | Ключевые метрики |
| 5 | Gemini Response | 3000-4500 символов | AI анализ |
| 6 | Final JSON | ~5-10 KB | Все данные + анализ |

---

## 🔄 Оптимизация данных

### ❓ FAQ: Почему не передаем все 750 значений в Gemini?

#### Проблемы передачи всех данных:

**1. Лимиты токенов**
- Gemini имеет лимит на input: ~30,000 токенов
- 750 значений = ~5,000-7,000 токенов только на данные
- Остается мало места для промпта и инструкций

**2. Стоимость**
```
Gemini 2.5 Flash (бесплатный tier):
- 15 запросов/минуту
- 1500 запросов/день

Если передавать все данные:
- Input: ~7,000 токенов
- Output: ~1,500 токенов
- Итого: ~8,500 токенов на запрос

Если передавать сжатые:
- Input: ~500 токенов (промпт + 5 метрик)
- Output: ~1,500 токенов
- Итого: ~2,000 токенов на запрос

Экономия: 4.25x меньше токенов!
```

**3. Качество анализа**
- Gemini может "потеряться" в 750 значениях
- Сжатые метрики = более фокусированный анализ
- Max Pain, P/C Ratio - это уже готовые инсайты
- AI лучше работает с агрегированными данными

**4. Скорость**
- Меньше токенов = быстрее обработка
- ~3-4 секунды вместо ~10-15 секунд
- Лучший UX для пользователя

### ❓ FAQ: Почему ограничиваем до 50 контрактов?

#### Polygon возвращает 250 контрактов, но мы обрабатываем только 50. Почему?

**Проблема: API Rate Limits**

```python
# Текущий код:
for contract in contracts[:50]:  # Для каждого контракта
    contract_data = self._get_contract_details(contract["ticker"])
    # Делаем отдельный запрос к Polygon!
```

**Polygon.io бесплатный tier:**
- ⚠️ 5 запросов/минуту
- ⚠️ Если обрабатывать 250 контрактов = 250 запросов
- ⏰ Время: 250 / 5 = **50 минут!**

**С 50 контрактами:**
- ✅ 50 запросов
- ⏰ Время: 50 / 5 = **10 минут** (все еще много, но приемлемо для MVP)

**Релевантность данных:**
- Polygon возвращает ВСЕ страйки (от $100 до $900)
- Текущая цена SPY: $669
- Нам нужны только около текущей цены ($650-$690)
- 90% контрактов - нерелевантны (слишком далеко от цены)

**Решение для будущего:**
1. Фильтровать по цене (±$50 от текущей) → ~20-30 контрактов
2. Использовать batch API (если доступен)
3. Кэшировать результаты на 5 минут
4. Использовать платный tier Polygon (100 запросов/минуту)

### Что теряем при сжатии?

- ❌ Детали по каждому контракту
- ❌ Bid/Ask спреды
- ❌ Индивидуальные Greeks
- ❌ Точные объемы по страйкам
- ❌ Дальние OTM контракты

### Что сохраняем?

- ✅ Общую картину рынка
- ✅ Ключевые уровни
- ✅ Сентимент (P/C Ratio)
- ✅ Волатильность (Gamma)
- ✅ Max Pain
- ✅ Релевантные страйки около текущей цены

---

## 💡 Выводы

**Эффективность потока данных:**
- Polygon дает нам **750 значений**
- Мы рассчитываем **20 метрик**
- В Gemini передаем **5 ключевых**
- Получаем **детальный анализ**

**Преимущества:**
- 🚀 Быстро (минимум данных в AI)
- 💰 Дешево (мало токенов)
- 🎯 Точно (фокус на главном)
- 📊 Полно (все важное сохранено)

---

*Последнее обновление: 2025-01-04*
*Проект: Options Flow AI Analyzer*
