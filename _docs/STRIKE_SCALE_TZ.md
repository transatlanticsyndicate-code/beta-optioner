# Техническое задание: Компонент StrikeScale

**Дата создания:** 16.10.2025  
**Дата завершения Этапа 3:** 17.10.2025  
**Версия:** 1.3  
**Статус:** Этап 3 завершен ✅ | Этап 4 в планировании

---

## 1. Общее описание

**StrikeScale** — компонент визуализации опционных позиций трейдера на интерактивной шкале страйк-цен.

### Назначение
Компонент позволяет трейдеру:
- Видеть все открытые позиции по опционам на одной шкале
- Оценивать расположение позиций относительно текущей цены актива
- Анализировать рыночный интерес к разным страйкам через объемы
- Отслеживать даты экспирации опционов
- Быстро ориентироваться в своем портфеле

---

## 2. Визуальная структура

### 2.1. Общая компоновка
```
┌─────────────────────────────────────────────────────┐
│  [Фиолетовый бейдж: 30.01.20]                      │
│     ↓                                                │
│  [5150🔴2] [5170🔴] [SPX⚫] [5195🟢] [5220🟢5]     │ ← Верхние флаги
│  ║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║   │ ← Зеленые столбики (CALL)
│  5100  5110  5120  5130  5140  5150 ... 5310       │ ← Шкала цен
│  ║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║║   │ ← Красные столбики (PUT)
│  [5160🟢3] [5175🟢] [5200🔴] [5230🔴4]             │ ← Нижние флаги
│     ↓                                                │
│  [Голубой бейдж: 28.12.25]                         │
└─────────────────────────────────────────────────────┘
```

---

## 3. Компоненты и элементы

### 3.1. Флаги (Badges) — Позиции опционов

#### Верхние флаги (над шкалой)
**Расположение:** `top: 48-56px` (с учетом уровней наложения)

**Типы:**
1. **Красные флаги** `rgb(255, 107, 107)`
   - Проданные PUT опционы
   - Купленные CALL опционы (out-of-the-money)
   - Прибыль при росте цены

2. **Зеленые флаги** `rgb(76, 175, 80)`
   - Купленные CALL опционы
   - Прибыль при росте цены

3. **Серый флаг** `rgb(75, 85, 99)`
   - Текущая цена актива (тикер)
   - Всегда один, не перемещается
   - Метка: название тикера (например, "SPX")

#### Нижние флаги (под шкалой)
**Расположение:** `top: 134px+` (с учетом уровней наложения)

**Типы:**
1. **Зеленые флаги** `rgb(76, 175, 80)`
   - Купленные PUT опционы
   - Прибыль при падении цены

2. **Красные флаги** `rgb(255, 107, 107)`
   - Проданные CALL опционы
   - Прибыль при падении цены

#### Структура флага
```jsx
<div className="badge-container">
  {/* Бейдж с датой (опционально) */}
  <div className="date-badge" style={{backgroundColor: dateColor}}>
    30.01.20
  </div>
  
  {/* Основной флаг */}
  <div className="flag" style={{backgroundColor: flagColor}}>
    <span>5150</span>
    
    {/* Количество контрактов (опционально) */}
    <div className="count-circle">2</div>
    
    {/* Треугольник-указатель */}
    <div className="arrow" />
  </div>
</div>
```

**Свойства флага:**
- `price` — страйк-цена опциона
- `color` — цвет флага (красный/зеленый/серый)
- `count` — количество контрактов (опционально)
- `date` — дата экспирации (опционально)
- `dateColor` — цвет бейджа даты (опционально)
- `type` — тип флага: 'option' | 'ticker'
- `label` — текстовая метка (для тикера)

---

### 3.2. Черные кружки с цифрами

**Назначение:** Показывают количество контрактов в позиции

**Источник данных:** `position.quantity` из массива `positions`

**Структура данных (ЕДИНЫЙ ФОРМАТ):**
```javascript
// Пример позиции в positions
{
  id: "1",
  action: "Buy",        // "Buy" или "Sell" (uppercase)
  type: "CALL",         // "CALL" или "PUT" (uppercase)
  strike: 255,          // Страйк-цена
  date: "31.10.25",     // Дата экспирации (формат DD.MM.YY)
  quantity: 1,          // ← КОЛИЧЕСТВО КОНТРАКТОВ (всегда положительное)
  premium: 5.9,
  visible: true,        // Видимость в таблице
  // ...
}
```

**Логика отображения:**
- Если на одном страйке несколько позиций → суммируем `quantity`
- Пример: 
  - Позиция 1: strike=5150, quantity=2
  - Позиция 2: strike=5150, quantity=3
  - **Результат:** Флаг 5150 с кружком "5"

**Знак quantity (ИСПРАВЛЕНО):**
- `quantity` всегда **положительное число** (количество контрактов)
- Направление (Buy/Sell) определяется полем `action`
- В кружке показываем `quantity` как есть (без Math.abs)
- Если на одном страйке несколько позиций → суммируем `quantity`

**Стили:**
- Размер: `w-5 h-5` (20x20px)
- Фон: `bg-black`
- Текст: белый, `text-xs font-bold`
- Позиция: `absolute -right-[13px] top-1/2 -translate-y-1/2`
- Тень: `shadow-md`

**Отображается:** Только если `Math.abs(quantity) > 0`

---

### 3.3. Бейджи с датами экспирации

**Источник данных:** `position.date` из массива `positions`

**Структура данных:**
```javascript
// Пример позиции с датой
{
  id: "1",
  action: "Buy",
  type: "CALL",
  strike: 255,
  date: "31.10.25",  // ← ДАТА ЭКСПИРАЦИИ (формат DD.MM.YY)
  quantity: 1,
  // ...
}
```

**Откуда берется дата:**
1. **API Polygon** → `/api/polygon/ticker/${ticker}/expirations`
   - Возвращает список доступных дат экспирации для тикера
   - Пример: `["2025-10-15", "2025-10-16", "2025-10-31", ...]`

2. **Пользователь выбирает дату** в `ExpirationCalendar`
   - Дата сохраняется в `selectedExpirationDate`

3. **При добавлении опциона** дата записывается в `position.date`
   ```javascript
   const newPosition = {
     // ...
     date: selectedExpirationDate, // "31.10.25"
   };
   ```

4. **StrikeScale читает** `position.date` и отображает в бейдже

**Логика цвета даты:**
- **Верхние флаги:** Цвет задается динамически через `dateColor`
  - Можно использовать разные цвета для разных дат
  - Пример: фиолетовый `rgb(139, 92, 246)` для ближайшей экспирации
- **Нижние флаги:** Фиксированный голубой `#0891b2`

#### Для верхних флагов
**Позиция:** Над флагом  
**Стиль:** `bottom: calc(100% - 5px)`, смещение `left: calc(50% + 10px)`  
**Цвет:** Задается в `dateColor` (например, фиолетовый `rgb(139, 92, 246)`)

#### Для нижних флагов
**Позиция:** Под флагом  
**Стиль:** `top: calc(100% - 5px)`, смещение `left: calc(50% + 10px)`  
**Цвет:** Фиксированный голубой `#0891b2`

**Формат даты:** `DD.MM.YY` (например, "30.01.20")

**Стили текста:**
- Размер: `text-[10px]`
- Цвет: белый
- Вес: `font-medium`
- Отступы: `padding: 1px 3px`, `margin: 2px`

---

### 3.4. Столбики объемов (Volume Bars)

**Источник данных:** `options` массив из `OptionsCalculatorV2`

**Структура данных:**
```javascript
// Пример опциона с объемом
{
  id: "1",
  type: "CALL",      // или "PUT"
  strike: 255,       // Страйк-цена
  volume: 2164,      // ← ОБЪЕМ ТОРГОВ
  oi: 34514,         // Open Interest (открытый интерес)
  premium: 5.9,
  bid: 225.8,
  ask: 6.0,
  // ...
}
```

**Откуда берется volume:**
1. **API Polygon** → `/api/polygon/ticker/${ticker}/options/${date}/${strike}/${type}`
   - Возвращает детали опциона, включая `volume` и `open_interest`
   - Пример ответа:
     ```json
     {
       "volume": 2164,
       "open_interest": 34514,
       "bid": 225.8,
       "ask": 6.0,
       // ...
     }
     ```

2. **Загрузка происходит:**
   - При выборе тикера → загружаются все доступные опционы
   - При выборе даты экспирации → загружаются страйки
   - При выборе страйка → загружаются детали (volume, oi, bid, ask)

3. **Данные сохраняются** в `options` массив:
   ```javascript
   const [options, setOptions] = useState([]);
   ```

4. **StrikeScale получает** `options` через props и рисует столбики

**Альтернативный источник:** `open_interest` (OI)
- Можно использовать вместо `volume`
- OI показывает количество открытых контрактов (более стабильный показатель)
- Выбор между `volume` и `oi` — вопрос для обсуждения

#### Зеленые столбики (верхние)
**Назначение:** Объем торгов CALL опционов на каждом страйке

**Источник:** `options.filter(opt => opt.type === 'CALL')`

**Стили:**
- Цвет: `#B2FFAE` (светло-зеленый)
- Ширина: `w-[3px]`
- Высота: динамическая `0-30px`
- Отступ снизу: `mb-[3px]`

#### Красные столбики (нижние)
**Назначение:** Объем торгов PUT опционов на каждом страйке

**Источник:** `options.filter(opt => opt.type === 'PUT')`

**Стили:**
- Цвет: `#FFBCBC` (светло-красный)
- Ширина: `w-[3px]`
- Высота: динамическая `0-30px`
- Отступ сверху: `mt-[3px]`

**Логика высоты:**
- Высота пропорциональна объему торгов на этом страйке
- Минимум: `0px` (нет объема)
- Максимум: `30px` (максимальный объем)
- Расчет: `height = (volume / maxVolume) * 30`

**Алгоритм расчета:**
```javascript
// 1. Найти максимальный объем среди всех CALL
const maxCallVolume = Math.max(...options
  .filter(opt => opt.type === 'CALL')
  .map(opt => opt.volume || 0)
);

// 2. Для каждого страйка рассчитать высоту
const callHeight = (option.volume / maxCallVolume) * 30;

// Аналогично для PUT
```

---

### 3.5. Шкала цен (Price Scale)

**Диапазон:** 5100 - 5310 (211 точек)  
**Шаг:** 1 пункт  
**Расстояние между точками:** 6px

#### Метки цен
**Отображаются:** Каждые 10 пунктов (5100, 5110, 5120...)  
**Стили:**
- Размер: `text-xs`
- Цвет: `text-foreground`
- Позиция: центрирована по горизонтали и вертикали

#### Разделители (Tick Marks)
1. **Основные (каждые 10):** Черные, высота `10px`
2. **Промежуточные (каждые 5):** Черные, высота `10px`
3. **Остальные:** Серые, высота `5px`

**Стили:**
- Ширина: `w-px` (1px)
- Цвет основных: `bg-black`
- Цвет промежуточных: `bg-gray-400`

---

## 4. Интерактивность

### 4.1. Перетаскивание флагов (Drag & Drop)

**Функционал:**
- Флаги опционов можно перетаскивать по горизонтали
- Флаг тикера (SPX) перетаскивать нельзя
- При перетаскивании флаг "прилипает" к ближайшему страйку

**События:**
- `onMouseDown` — начало перетаскивания
- `onMouseMove` — движение флага
- `onMouseUp` — завершение перетаскивания

**Курсоры:**
- Флаги опционов: `cursor-pointer`
- Флаг тикера: `cursor-default`

---

#### 4.1.1. Влияние перетаскивания на систему

**Цепочка изменений при перетаскивании флага:**

```
User перетащил флаг 5150 → 5160
  ↓
1. StrikeScale (локальный state)
   - Визуально флаг двигается (optimistic update)
   - Пока НЕ обновляем positions
  ↓
2. onMouseUp → Определяем новый страйк (5160)
  ↓
3. StrikeScale вызывает callback:
   onPositionChange(positionId, newStrike)
  ↓
4. OptionsCalculatorV2 (родитель):
   setPositions(positions.map(pos => 
     pos.id === positionId 
       ? {...pos, strike: 5160}  // ← ИЗМЕНИЛИ СТРАЙК
       : pos
   ))
  ↓
5. React перерисовывает ВСЕ компоненты с новым positions
  ↓
6. ВЛИЯНИЕ НА ДРУГИЕ БЛОКИ:
```

**Блоки, которые обновятся:**

1. **StrikeScale** (сам компонент)
   - Флаг отрисован на новой позиции (5160)
   - Если на 5160 уже был флаг → пересчет уровней наложения

2. **PLChart** (График P&L)
   - ❗ **КРИТИЧНО:** Пересчет всего графика!
   - Новый страйк → новая кривая прибыли/убытка
   - Может измениться:
     - Максимальная прибыль
     - Максимальный убыток
     - Точка безубыточности (breakeven)
     - Форма кривой

3. **OptionsMetrics** (Метрики)
   - Пересчет Greeks:
     - Delta (изменение цены опциона)
     - Gamma (скорость изменения delta)
     - Theta (временной распад)
     - Vega (чувствительность к волатильности)
   - Пересчет рисков:
     - Max Profit / Max Loss
     - Probability of Profit
     - Expected Value

4. **OptionsTable** (Таблица опционов)
   - Обновление строки с измененной позицией
   - Новый страйк → новые bid/ask/premium
   - Может потребоваться загрузка данных для нового страйка

5. **BaseAssetPositions** (Позиции базового актива)
   - Обновление списка позиций
   - Новый страйк в отображении

6. **RiskCalculator** (Калькулятор рисков)
   - Пересчет сценариев:
     - "Что если цена вырастет на 10%?"
     - "Что если волатильность упадет?"
   - Новые рекомендации по хеджированию

---

#### 4.1.2. Производительность и оптимизация

**Проблема:** Перетаскивание вызывает пересчет ВСЕХ блоков!

**Решения:**

1. **Debounce обновления**
   ```javascript
   // Обновляем positions только после остановки перетаскивания
   // НЕ обновляем на каждом onMouseMove
   ```

2. **Optimistic UI**
   ```javascript
   // Визуально флаг двигается мгновенно (локальный state)
   // positions обновляется только onMouseUp
   ```

3. **Мемоизация расчетов**
   ```javascript
   // PLChart, Metrics используют useMemo
   // Пересчет только если изменился конкретный страйк
   ```

4. **Lazy loading данных**
   ```javascript
   // Если новый страйк не загружен → показываем loader
   // Загружаем данные асинхронно
   ```

---

#### 4.1.3. Валидация перетаскивания

**Ограничения:**

1. **Нельзя перетащить за границы шкалы**
   - Минимум: 5100
   - Максимум: 5310

2. **Нельзя перетащить на занятый страйк?**
   - Вопрос: Разрешить несколько позиций на одном страйке?
   - Если да → группировка (суммирование quantity)
   - Если нет → блокировка перетаскивания

3. **Проверка доступности страйка**
   - Новый страйк должен существовать в `options`
   - Если нет → загрузить данные или запретить

4. **Подтверждение изменения?**
   - Показать модалку: "Изменить страйк с 5150 на 5160?"
   - Или изменять сразу (быстрее, но рискованнее)

---

#### 4.1.4. Откат изменений (Undo)

**Функционал:** Возможность отменить перетаскивание

```javascript
// История изменений
const [positionsHistory, setPositionsHistory] = useState([]);

// При изменении
setPositionsHistory([...positionsHistory, oldPositions]);

// Undo
const undo = () => {
  const previous = positionsHistory.pop();
  setPositions(previous);
};
```

**UI:** Кнопка "Отменить" или Ctrl+Z

---

#### 4.1.5. Синхронизация с бэкендом

**Вопрос:** Сохранять ли изменения на сервере?

**Варианты:**

1. **Не сохранять** (текущий подход)
   - Позиции только в памяти
   - При обновлении страницы → все теряется

2. **Автосохранение**
   - После каждого перетаскивания → POST /api/positions
   - Риск: много запросов

3. **Ручное сохранение**
   - Кнопка "Сохранить портфель"
   - Пользователь контролирует

4. **LocalStorage**
   - Сохранение в браузере
   - Работает офлайн
   - Не синхронизируется между устройствами

### 4.2. Уровни наложения флагов (Y-levels)

**Проблема:** Флаги могут накладываться друг на друга

**Решение:** Система уровней по вертикали
- Уровень 0: `top: 56px` (верхние) / `top: 134px` (нижние)
- Уровень 1: `top: 56px - 38px = 18px` (верхние) / `top: 134px + 38px = 172px` (нижние)
- Уровень 2: `top: 56px - 76px = -20px` (верхние) / `top: 134px + 76px = 210px` (нижние)

**Логика:**
- При добавлении флага проверяется наложение с существующими
- Если есть наложение, флаг поднимается на уровень выше
- Максимум 3-4 уровня

### 4.3. Горизонтальный скролл

**Контейнер:**
- `overflow-x-auto` — горизонтальная прокрутка
- `hide-scrollbar` — скрытие полосы прокрутки
- Курсор: `cursor-grab` / `cursor-grabbing`

**Перетаскивание скролла:**
- Зажать мышь → двигать → отпустить
- Инерция прокрутки (опционально)

---

## 5. Источники данных и поток данных

### 5.0. Диаграмма потока данных

```
┌─────────────────────────────────────────────────────────────────┐
│                    OptionsCalculatorV2 (Parent)                  │
│                                                                   │
│  State:                                                           │
│  • positions: Position[] ← ЕДИНСТВЕННЫЙ ИСТОЧНИК ИСТИНЫ          │
│  • options: Option[]                                              │
│  • currentPrice: number                                           │
│                                                                   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ Props ↓
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ↓                   ↓                   ↓
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ OptionsTable │    │ OptionsBoard │    │ StrikeScale  │
│              │    │              │    │              │
│ Действие:    │    │ Действие:    │    │ Действие:    │
│ Клик на      │    │ Клик на      │    │ Drag & Drop  │
│ опцион       │    │ опцион       │    │ флага        │
└──────┬───────┘    └──────┬───────┘    └──────┬───────┘
       │                   │                   │
       │ Callback ↑        │ Callback ↑        │ Callback ↑
       │                   │                   │
       └───────────────────┴───────────────────┘
                            │
                            ↓
                    setPositions([...])
                            │
                            ↓
                    positions обновлен
                            │
                            ↓
              ┌─────────────┴─────────────┐
              │                           │
              ↓                           ↓
       StrikeScale                 PLChart, Metrics
       перерисовывает              обновляют расчеты
       флаги
```

### 5.0.1. Поток данных: Добавление опциона

**Сценарий 1: Из таблицы опционов**
```
1. User → Клик на опцион в OptionsTable
2. OptionsTable → Вызывает callback onAddOption(option)
3. OptionsCalculatorV2 → setPositions([...positions, newPosition])
4. React → Перерисовка всех компонентов с новым positions
5. StrikeScale → Получает обновленный positions через props
6. StrikeScale → Рассчитывает флаги из positions
7. StrikeScale → Рендерит новый Badge на шкале
```

**Сценарий 2: Из доски опционов**
```
1. User → Клик на ячейку в OptionsBoard
2. OptionsBoard → Вызывает callback onAddOption(option)
3. OptionsCalculatorV2 → setPositions([...positions, newPosition])
4. React → Перерисовка
5. StrikeScale → Рендерит новый Badge
```

### 5.0.2. Поток данных: Перетаскивание флага

**Сценарий: Drag & Drop флага**
```
1. User → Зажимает флаг мышью (onMouseDown)
2. StrikeScale → Сохраняет начальную позицию
3. User → Двигает мышь (onMouseMove)
4. StrikeScale → Обновляет визуальную позицию флага (локальный state)
5. User → Отпускает мышь (onMouseUp)
6. StrikeScale → Определяет новый страйк (прилипание к ближайшему)
7. StrikeScale → Вызывает callback onPositionChange(positionId, newStrike)
8. OptionsCalculatorV2 → setPositions(positions.map(pos => 
                            pos.id === positionId 
                              ? {...pos, strike: newStrike} 
                              : pos))
9. React → Перерисовка
10. StrikeScale → Флаг отрисован на новой позиции
11. PLChart, Metrics → Пересчитывают данные с новым страйком
```

### 5.0.3. Текущая реализация (Этап 1)

**Проблема:** Сейчас StrikeScale использует **хардкод** демо-данных:
```javascript
// В StrikeScale.jsx
const flags = [
  { id: 1, price: 5150, color: 'rgb(255, 107, 107)', count: 2, ... },
  { id: 2, price: 5170, color: 'rgb(255, 107, 107)', ... },
  // ... хардкод
];
```

**Что нужно сделать (Этап 2):**
```javascript
// Преобразовать positions в flags
const flags = useMemo(() => {
  return positions
    .filter(pos => pos.type === 'option')
    .map(pos => ({
      id: pos.id,
      price: pos.strike,
      color: calculateFlagColor(pos),
      count: pos.quantity,
      date: pos.expirationDate,
      dateColor: calculateDateColor(pos),
      type: 'option'
    }));
}, [positions]);
```

### 5.1. Входные props

```typescript
interface StrikeScaleProps {
  // Список всех доступных опционов
  options: Option[];
  
  // Текущая цена актива
  currentPrice: number;
  
  // Открытые позиции трейдера
  positions: Position[];
  
  // Название тикера
  ticker?: string;
  
  // Callback при изменении позиции
  onPositionChange?: (position: Position) => void;
}

interface Option {
  id: string;               // Уникальный ID
  strike: number;           // Страйк-цена
  type: 'CALL' | 'PUT';     // Тип опциона (uppercase)
  volume?: number;          // Объем торгов (опционально)
  oi?: number;              // Open Interest (опционально)
  date: string;             // Дата экспирации (формат DD.MM.YY)
  premium?: number;         // Цена опциона
  bid?: number;             // Bid цена
  ask?: number;             // Ask цена
}

interface Position {
  id: string;
  strike: number;
  type: 'CALL' | 'PUT';     // Uppercase для совместимости
  action: 'Buy' | 'Sell';   // Uppercase для совместимости
  quantity: number;         // Количество контрактов (положительное)
  date: string;             // Формат DD.MM.YY (например "31.10.25")
  premium?: number;         // Премия опциона (опционально)
  visible?: boolean;        // Видимость в таблице
}
```

### 5.2. Расчет данных для флагов

**Алгоритм:**
1. Из `positions` создаем флаги:
   - Для каждой позиции определяем цвет (красный/зеленый)
   - Определяем расположение (верх/низ)
   - Группируем по страйкам (суммируем quantity)

2. Добавляем флаг текущей цены:
   - Страйк = `currentPrice`
   - Цвет = серый
   - Метка = `ticker`

3. Рассчитываем уровни наложения (Y-levels)

**Правила цвета (ИСПРАВЛЕНО 17.10.2025):**

**Логика разделена на ДВА параметра:**

1. **Цвет флага** = по действию (соответствует таблице опционов)
   - 🟢 **Зеленый** `rgb(76, 175, 80)` = `action === 'Buy'` (любой опцион)
   - 🔴 **Красный** `rgb(255, 107, 107)` = `action === 'Sell'` (любой опцион)
   - ⚫ **Серый** `rgb(75, 85, 99)` = только флаг тикера

2. **Позиция на шкале** = по направлению прибыли
   - **Верх** (прибыль при росте цены): `Sell PUT` или `Buy CALL`
   - **Низ** (прибыль при падении цены): `Buy PUT` или `Sell CALL`

**Примеры:**
- **Buy CALL** 5150 → 🟢 Зеленый флаг сверху (Buy=зеленый, прибыль при росте)
- **Sell CALL** 5150 → 🔴 Красный флаг снизу (Sell=красный, прибыль при падении)
- **Buy PUT** 5150 → 🟢 Зеленый флаг снизу (Buy=зеленый, прибыль при падении)
- **Sell PUT** 5150 → 🔴 Красный флаг сверху (Sell=красный, прибыль при росте)

### 5.3. Расчет высоты столбиков

**Источник данных:** `options[].volume` или `options[].openInterest`

**Алгоритм:**
1. Найти максимальный объем: `maxVolume = Math.max(...volumes)`
2. Для каждого страйка:
   - CALL объем → зеленый столбик
   - PUT объем → красный столбик
3. Нормализация: `height = (volume / maxVolume) * 30`

---

## 6. Адаптивность и производительность

### 6.1. Оптимизация рендеринга

**Проблема:** 211 столбиков × 2 = 422 DOM элемента

**Решения:**
1. Виртуализация (показывать только видимые элементы)
2. `React.memo` для столбиков
3. `useMemo` для расчета высот
4. Дебаунс при перетаскивании

### 6.2. Адаптивность

**Desktop (>1024px):**
- Полная ширина, все 211 точек
- Горизонтальный скролл

**Tablet (768-1024px):**
- Уменьшенные отступы
- Скрытие некоторых меток

**Mobile (<768px):**
- Компонент скрыт или упрощен
- Альтернативный вид (список позиций)

---

## 7. Состояния компонента

### 7.1. Пустое состояние
**Условие:** `positions.length === 0`

**Отображение:**
- Только шкала цен и столбики объемов
- Флаг текущей цены (тикер)
- Плейсхолдер: "Добавьте опционы для отображения позиций"

### 7.2. Состояние загрузки
**Условие:** Данные опционов загружаются

**Отображение:**
- Скелетон шкалы
- Анимация загрузки

### 7.3. Состояние с данными
**Условие:** `positions.length > 0`

**Отображение:**
- Все флаги позиций
- Столбики объемов
- Интерактивность включена

### 7.4. Состояние блокировки
**Условие:** `!selectedTicker`

**Отображение:**
- Overlay с блюром `bg-white/50 backdrop-blur-sm`
- Компонент неактивен
- Подсказка: "Выберите тикер"

---

## 8. Технические требования

### 8.1. Технологии
- React 18+
- TypeScript
- Tailwind CSS
- Framer Motion (для анимаций, опционально)

### 8.2. Зависимости
- Нет внешних библиотек для графики
- Чистый CSS + HTML

### 8.3. Производительность
- Рендер < 100ms
- Плавное перетаскивание (60 FPS)
- Lazy loading для данных объемов

### 8.4. Доступность (A11y)
- Keyboard navigation (Tab, Arrow keys)
- ARIA labels для флагов
- Screen reader support
- Focus indicators

---

## 9. Этапы разработки

### Этап 1: Статическая версия ✅
**Коммит начала:** `25343c8` (Полная копия StrikeScale из кода Андрея)  
**Коммит окончания:** `766ad87` (Создана выжимка взаимодействий)

- [x] Копирование HTML/CSS кода Андрея
- [x] Интеграция в проект
- [x] Демо-данные (хардкод)

### Этап 2: Динамические данные ✅
**Коммит начала:** `1117520` (Исправлено ТЗ: правила цветов, формат данных)  
**Коммит окончания:** `8b3ad4a` (Документация масштабирования, Этап 2 завершен)

**Прогресс:** 6/6 шагов завершено ✅ (ЭТАП ЗАВЕРШЕН!)

- [x] **2.1:** Функции расчета (`isTopFlag`, `calculateFlagColor`, `calculateDateColor`) - коммит `0ab85e1` ✅
- [x] **2.2:** Подключение реальных `positions` → преобразование в flags с useMemo - коммит `24e24e3` ✅
- [x] **2.3:** Расчет высоты столбиков из `options` (Open Interest с нормализацией) - коммит `9a262ec` ✅
- [x] **2.4:** Группировка позиций по страйкам + расчет Y-levels (< 15 страйков → уровень выше) - коммит `3654aaa` ✅
- [x] **2.5:** Удалены тесты и демо-данные, код очищен - коммит `026cf30` ✅
- [x] **2.6:** Подготовка к тестированию (ticker, Math.abs, логирование) - коммит `d6df44c` ✅

**📋 Инструкция:** См. `_docs/STRIKE_SCALE_TESTING_GUIDE.md`
- [x] **Индикаторы скрытых флагов** - коммит `f28b9e5` + `70f6301` ✅
  - Синие кнопки `← N` и `N →` по краям шкалы
  - Показывают количество скрытых флагов за границами
  - При клике - автоскролл к краям шкалы
  - Обновляются динамически при скролле и после автоцентрирования

**📋 Запланированные улучшения (в разработке):**
- [ ] **Масштабирование шкалы (Zoom)** - запланировано для Этапа 3 ⏳
  - **Описание:** Панель управления масштабом `[−] [100%] [+]` над шкалой
  - **Диапазон:** 50%, 75%, 100%, 125%, 150%, 200%
  - **Управление:**
    - Кнопки: `[−]` уменьшить, `[+]` увеличить, клик на `[100%]` = сброс
    - Ctrl+Scroll: увеличение/уменьшение колесом мыши
  - **Эффект:** Изменяет ширину страйка с `6px` на `3px-12px`, позволяет видеть больше/меньше страйков
  - **Преимущества:** Решает проблему длинных диапазонов, удобный детальный просмотр
  - **Техническое решение:**
    - State: `const [scale, setScale] = useState(100)`
    - Ширина: `const strikeWidth = 6 * (scale / 100)`
    - Пересчет всех позиций флагов, меток и индикаторов

**❌ Неудачные решения (удалено):**
- ~~**Кастомные tooltips**~~ - коммиты `733ca26`, `70f6301`, удалено в `ae2305d` ❌
  - Показывали информацию при наведении: Strike, Quantity, Дата
  - **Проблема:** Перегружали интерфейс, дублировали видимую информацию (бейджи с датами)
  - **Решение:** Удалено. Вся информация видна на флагах и бейджах

### Этап 3: Интерактивность ✅ (ЗАВЕРШЕН)
**Коммит начала:** `e546e11` (Этап 2 завершен, начало Этапа 3)  
**Коммит окончания:** `38c2056` (все задачи выполнены, запушено на GitHub)

**Прогресс:** 7/7 задач ✅ (100%)

**🎯 Текущий статус (17.10.2025):**
- ✅ Drag & Drop с умным прилипанием реализован (`4512131`)
- 🔥 Прилипание к страйкам с высоким OI (>50,000)
- 🧲 Прилипание к круглым числам (5100, 5150 для SPX)
- 🎯 Автоскролл при перетаскивании к краям
- 🗓️ Фильтр по датам экспирации с цветовым кодированием (`a1ba72a`)
- 📋 Опционально: Визуализация магнитных точек пунктирными линиями

- [x] **3.1: Drag & Drop флагов** - коммит `9e375c8` ✅
  - Флаги можно перетаскивать мышью (кроме флага тикера)
  - Визуальная обратная связь: курсор `grab` → `grabbing`, полупрозрачность 0.7
  - Обработчики: `onMouseDown`, `onMouseMove`, `onMouseUp`
  - State: `draggingFlag`, `dragStartX`, `dragOffset`
- [x] **3.2: Привязка к страйкам (Smart Snapping)** - коммит `106421f` ✅
  - Расчет ближайшего страйка: `Math.round((position - 1.5) / 6)`
  - Ограничение диапазона: `clampedIndex = Math.max(0, Math.min(count-1, index))`
  - Добавлено поле `optionIds` в флаги для обновления групп опционов
  - Callback `onPositionUpdate(optionId, { strike })` для обновления опционов
- [x] **3.3: Интеграция с родительским компонентом** - коммит `a61a327` ✅
  - Создана функция `handleStrikeUpdate(optionId, updates)` в OptionsCalculatorV2
  - Callback передается через prop `onPositionUpdate` в StrikeScale
  - Опционы реально обновляются в state при перетаскивании
  - Логирование обновлений в консоль: `📍 Strike updated via Drag & Drop`
  - **РЕЗУЛЬТАТ:** Drag & Drop полностью работает! Можно менять страйки перетаскиванием флагов!
- [x] **3.4: Исправление работы с демо-данными** - коммит `a478392` ✅
  - Добавлен Fallback 2 для построения диапазона из страйков опционов
  - StrikeScale работает даже без currentPrice и strikesByDate
  - Используется для тестирования с демо-данными
- [x] **3.5: Автоскролл при перетаскивании** - коммит `4512131` ✅
  - Автоматическая прокрутка при приближении к краям контейнера
  - Порог активации: 50px от края
  - Скорость скролла: 10px за движение мыши
  - Работает для левого и правого края
- [x] **3.6: Умное прилипание (Smart Snapping)** - коммит `4111fa4` ✅
  - **Приоритет 0 (КРИТИЧНО):** Прилипание к реальным страйкам из опционов
    - Гарантирует что флаг всегда на существующем страйке (230, 240, 245...)
    - Радиус: ±5 шагов
    - Логирование: `📍 Прилипание к реальному страйку`
  - **Приоритет 1:** Прилипание к страйкам с высоким OI (>50,000)
    - Радиус поиска: ±3 шага страйка
    - Логирование: `🔥 Прилипание к страйку с высоким OI`
  - **Приоритет 2:** Прилипание к круглым числам
    - SPX: кратные 50 (5100, 5150, 5200...)
    - SPY: кратные 10 (240, 250, 260...)
    - Радиус прилипания: ±2 шага страйка
    - Логирование: `🧲 Прилипание к круглому числу`
  - ⏳ TODO: Визуализация магнитных точек пунктирными линиями
- [x] **Система уровней наложения** - уже работает через Y-levels ✅
- [x] **3.7: Группировка по датам экспирации** - коммит `a1ba72a` ✅
  - Переключатель дат над шкалой (кнопки)
  - Кнопка "Все даты" для отображения всех опционов
  - Цветовое кодирование кнопок по срокам:
    - 🔴 Красный: < 7 дней до экспирации
    - 🟣 Фиолетовый: 7-30 дней до экспирации
    - 🔵 Синий: > 30 дней до экспирации
  - Счетчик опционов на каждой кнопке: "31.10.25 (2)"
  - Автоматический расчет уникальных дат из опционов
  - Фильтрация флагов и столбиков OI по выбранной дате

---

## 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ ЭТАПА 3

### Подготовка:
1. Откройте `/tools/options-calculator-v2`
2. Страница загрузится с демо-данными:
   - **6 опционов** со страйками 230-260 (близко к цене 245)
   - **3 разные даты**: 25.10.25 (2 опциона), 15.11.25 (2 опциона), 20.12.25 (2 опциона)
   - **Все с высоким OI** (>50,000) для тестирования прилипания

### Тест 1: Фильтр по датам экспирации
**Что проверяем:** Группировка опционов по датам

**Шаги:**
1. Над шкалой страйков увидите кнопки дат:
   - `Все даты (6)` - синяя кнопка (активна по умолчанию)
   - `25.10.25 (2)` - 🔴 красная (< 7 дней)
   - `15.11.25 (2)` - 🟣 фиолетовая (7-30 дней)  
   - `20.12.25 (2)` - 🔵 синяя (> 30 дней)
2. Кликните на кнопку `25.10.25` (красная - срочная экспирация!)
3. **Ожидаемый результат:**
   - Кнопка станет красной и активной
   - На шкале останутся только 2 флага (страйки 250 и 240)
   - Остальные 4 флага исчезнут
4. Кликните `15.11.25` (фиолетовая)
5. **Ожидаемый результат:**
   - На шкале 2 других флага (страйки 260 и 230)
6. Кликните `Все даты`
7. **Ожидаемый результат:**
   - Все 6 флагов вернутся на шкалу

**Цветовое кодирование:**
- 🔴 **Красный** - экспирация < 7 дней (срочные опционы!)
- 🟣 **Фиолетовый** - экспирация 7-30 дней (среднесрочные)
- 🔵 **Синий** - экспирация > 30 дней (долгосрочные)

### Тест 2: Drag & Drop с фильтром
**Что проверяем:** Drag & Drop работает при включенном фильтре

**Шаги:**
1. Выберите дату `25.10.25` (останутся 2 флага: 250 и 240)
2. Найдите зеленый флаг CALL на страйке 250
3. Зажмите флаг мышью и перетащите влево на ~5px
4. Отпустите
5. **Ожидаемый результат:**
   - Флаг двигается плавно с полупрозрачностью
   - При отпускании прилипает к ближайшему страйку (249 или 250)
   - В консоли: `🎯 Привязка к страйку` и `📍 Strike updated`
   - В таблице опционов страйк изменился!

### Тест 3: Прилипание к реальным страйкам
**Что проверяем:** Флаг ВСЕГДА попадает на существующий страйк

**Шаги:**
1. Убедитесь что `Все даты (6)` выбрано
2. Найдите флаг на страйке 250
3. Перетащите на ~3px влево (между 245 и 250)
4. Отпустите
5. **Ожидаемый результат:**
   - В консоли: `📍 Прилипание к реальному страйку: { вычисленный: 247, реальный: 245 }`
   - Флаг прилип к 245 (ближайший реальный страйк)
   - **НЕ** остался на 247, 248, 249 (которых нет!)

### Тест 4: Умное прилипание к круглым числам
**Что проверяем:** Прилипание к 250, 260 (дополнительно к реальным)

**Шаги:**
1. На реальных данных (SPY/SPX с биржи)
2. Перетащите флаг к круглому числу 
3. **Ожидаемый результат:**
   - В консоли: `🧲 Прилипание к круглому числу`

### Тест 5: Прилипание к страйкам с высоким OI
**Что проверяем:** Приоритетное прилипание к популярным страйкам

**Шаги (на демо-данных):**
1. Убедитесь что выбрано `Все даты (6)`
2. Найдите зеленый флаг CALL на страйке 255 (дата 20.12.25)
3. Перетащите его влево к страйку 245 (где Sell CALL с OI=424514)
4. **Ожидаемый результат:**
   - В консоли: `🔥 Прилипание к страйку с высоким OI: { старый: 253, новый: 245, OI: 424514 }`
   - Флаг переместится на страйк 245 (с максимальным OI в радиусе)

**Альтернативно (на реальных данных):**
1. Выберите тикер **SPX** или **SPY**
2. Дождитесь загрузки опционов с биржи
3. Перетащите флаг к страйку с OI > 50,000
4. Увидите прилипание к популярному страйку

### Тест 6: Автоскролл при перетаскивании
**Что проверяем:** Автоматическая прокрутка к краям

**Шаги:**
1. Зажмите флаг и тяните его к правому краю окна
2. Когда курсор окажется в пределах 50px от края
3. **Ожидаемый результат:**
   - Шкала автоматически прокручивается вправо
   - Флаг продолжает двигаться
4. То же самое проверьте для левого края

### Тест 7: Индикаторы скрытых флагов с фильтром
**Что проверяем:** Индикаторы учитывают фильтр

**Шаги:**
1. Выберите конкретную дату
2. Посмотрите на индикаторы `← N` и `N →`
3. **Ожидаемый результат:**
   - Индикаторы показывают только флаги отфильтрованной даты
   - При смене фильтра счетчики меняются

---

### ✅ Критерии успеха:
- [x] Фильтр по датам работает (флаги появляются/исчезают)
- [x] Цветовое кодирование кнопок корректное (красный/фиолетовый/синий)
- [x] Drag & Drop работает с любым фильтром
- [x] **Прилипание к реальным страйкам** (флаг ВСЕГДА на существующем страйке!)
- [x] Прилипание к круглым числам срабатывает
- [x] Прилипание к высокому OI работает
- [x] Автоскролл работает к обоим краям
- [x] Индикаторы скрытых флагов учитывают фильтр
- [x] Нет ошибок в консоли

---

### Этап 4: Оптимизация и расширенная визуализация ✅ (ЗАВЕРШЕН)
**Коммит начала:** `38c2056` (Этап 3 завершен)  
**Коммит окончания:** `25d446a` (все задачи выполнены)

**Прогресс:** 5/5 задач ✅ (100%) - ЭТАП ЗАВЕРШЕН!

- [x] **4.1: Маркеры "Hot Strike" 🔥** - коммит `78a874a` ✅
  - Автоматический расчет популярных страйков (OI > 70% от макс и > 50k)
  - Визуальный маркер 🔥 рядом с ценой на шкале
  - Подсказка при наведении: "Популярный страйк (высокий OI)"
  - Работает с фильтром по датам
  - Логирование в консоль: `🔥 Hot Strikes (популярные)`
- [x] **4.2: Маркеры "High Volume" ⚡** - коммит `690c8ed` ✅
  - Автоматический расчет страйков с большим объемом (Volume > 80% от макс и > 1k)
  - Визуальный маркер ⚡ рядом с ценой
  - Подсказка: "Высокий объем торгов"
  - Может показываться вместе с 🔥
  - Логирование: `⚡ High Volume Strikes`
- [x] **4.3: Маркеры "Pin Risk" 🎯** - коммит `690c8ed` ✅
  - Автоматический расчет риска пиннинга (OI > 100k в радиусе ±5% от цены)
  - Визуальный маркер 🎯 рядом с ценой
  - Подсказка: "Риск пиннинга (близко к цене)"
  - Показывает где цена может "застрять"
  - Логирование: `🎯 Pin Risk Strikes`
  - **Карта тестирования:** `STRIKE_SCALE_STAGE4_TESTING.md` (обновлена)
- [x] **4.4: Мемоизация расчетов** - коммит `a5e2e62` ✅
  - Добавлены `useCallback` для функций расчета (isTopFlag, calculateFlagColor, calculateDateColor)
  - Все тяжелые вычисления уже были с `useMemo` (флаги, столбики, маркеры)
  - Оптимизация производительности - меньше пересчетов
  - Компонент готов для работы с большими объемами данных
- [x] **4.5: Полная карта Open Interest рынка** - коммит `25d446a` ✅
  - Чекбокс "Показать полную карту Open Interest рынка" над шкалой
  - **2 режима работы:**
    - Обычный: Показываем столбики OI только для опционов в таблице (быстро)
    - Полная карта: Загружаем ВСЕ опционы с биржи и показываем OI по всем страйкам
  - Автоматическая загрузка при включении чекбокса
  - API запрос к `/api/polygon/ticker/{ticker}/options?expiration_date={date}`
  - Группировка OI по страйкам: CALL (зеленые) и PUT (красные)
  - Маркеры 🔥⚡🎯 работают по полной картине!
  - Логирование: `📈 Используем ПОЛНУЮ карту OI рынка`

### Этап 5: Полировка и продвинутые функции ⏳
**Коммит начала:** `_____` (будет после Этапа 4)  
**Коммит окончания:** `_____` (будет после завершения)

- [ ] Анимации переходов
- [ ] Адаптивность
- [ ] Доступность (A11y)
- [ ] **Мини-график P&L прямо на шкале**
  - Тонкая кривая P&L поверх столбиков объемов
  - Показывает форму стратегии без переключения на отдельный график
  - Опциональное отображение (вкл/выкл)
  - Синхронизация с основным графиком P&L

---

## 10. Открытые вопросы (ДЛЯ ОБСУЖДЕНИЯ)

### ❗ КРИТИЧНЫЕ (Решить перед началом разработки)

#### 1. **Диапазон страйков: фиксированный или динамический?**
**Текущее:** Хардкод 5100-5310 (211 точек)

**Варианты:**
- A) Оставить фиксированный 5100-5310
- B) Динамический на основе `currentPrice ± 10%`
- C) Динамический на основе доступных страйков из `options`

**Ваше решение:** _____

---

#### 2. **Конвертация формата даты**
**Проблема:** API Polygon возвращает `"2025-10-31"`, а StrikeScale использует `"31.10.25"`

**Варианты:**
- A) Конвертация в `OptionsCalculatorV2` перед передачей в `positions`
- B) Конвертация внутри `StrikeScale`
- C) Хранить оба формата: `date` (DD.MM.YY) и `dateISO` (YYYY-MM-DD)

**Ваше решение:** _____

---

#### 3. **Источник данных volume: volume или OI?**
**Варианты:**
- A) Использовать `volume` (объем торгов за день)
- B) Использовать `openInterest` (открытый интерес)
- C) Переключатель между volume и OI

**Ваше решение:** _____

---

#### 4. **Callback onPositionChange: формат параметров**
**Проблема:** Противоречие в ТЗ

**Варианты:**
- A) `onPositionChange(positionId: string, newStrike: number)`
- B) `onPositionChange(updatedPosition: Position)`
- C) `onPositionChange(positionId: string, changes: Partial<Position>)`

**Ваше решение:** _____

---

### ⚠️ ВАЖНЫЕ (Можно решить на этапе 2-3)

#### 5. **Логика перетаскивания**
- Разрешить перетаскивание между верхом и низом? (Buy CALL → Sell CALL)
- Что происходит при перетаскивании за границы шкалы?
- Блокировать перетаскивание на занятый страйк или разрешить?

**Ваше решение:** _____

---

#### 6. **Множественные позиции на одном страйке**
**Вопрос:** Как отображать?
- A) Суммировать quantity и показать один флаг
- B) Показать несколько флагов на разных Y-уровнях
- C) Группировать по датам экспирации

**Ваше решение:** _____

---

#### 7. **Умное прилипание (Smart Snapping): приоритеты**
**Вопрос:** Что важнее при выборе магнитной точки?

**Варианты приоритетов:**
- A) Ближайший страйк (расстояние < 3) → Круглое число → Высокий OI → Breakeven
- B) Высокий OI → Круглое число → Breakeven → Ближайший страйк
- C) Круглое число → Высокий OI → Breakeven → Ближайший страйк

**Ваше решение:** _____

---

#### 8. **Фильтрация по датам экспирации**
**Вопрос:** Нужен ли фильтр?
- A) Да, переключатель дат над шкалой (Все / 31.10.25 / 15.11.25)
- B) Нет, показывать все позиции всегда
- C) Цветовое кодирование по датам (без фильтра)

**Ваше решение:** _____

---

### 🟡 ДОПОЛНИТЕЛЬНЫЕ (Можно решить на этапе 4-5)

#### 9. **Интеграция с другими блоками: приоритеты обновлений**
**Вопрос:** Какие компоненты обновлять синхронно, какие асинхронно?

**Предложение:**
- Синхронно: StrikeScale, BaseAssetPositions (~60ms)
- Асинхронно: PLChart, OptionsMetrics, RiskCalculator (~800ms)

**Ваше решение:** _____

---

#### 10. **Обработка ошибок API**
- Показывать сообщение об ошибке?
- Fallback на демо-данные?
- Показывать пустую шкалу?

**Ваше решение:** _____

---

#### 11. **Мобильная версия**
- Упрощенный вид (список позиций)?
- Полная функциональность с touch events?
- Скрыть компонент на мобильных?

**Ваше решение:** _____

---

#### 12. **Синхронизация с бэкендом**
**Вопрос:** Сохранять ли изменения на сервере?
- A) Не сохранять (только в памяти)
- B) Автосохранение после каждого изменения
- C) Ручное сохранение (кнопка "Сохранить портфель")
- D) LocalStorage (работает офлайн)

**Ваше решение:** _____

---

## 11. Примечания

- Текущая версия использует демо-данные (хардкод)
- Высоты столбиков генерируются случайно
- Флаги не перетаскиваются (статичные)
- Нет реальной связи с данными опционов

---

**Следующий шаг:** Обсудить открытые вопросы и приоритизировать этапы разработки.
