# 🌐 Chrome MCP - Установка и использование

**Дата:** 12 октября 2025  
**Статус:** Инструкция по настройке

---

## 📖 Что такое Chrome MCP?

**Chrome MCP** - это MCP сервер, который позволяет AI ассистенту управлять браузером Chrome:
- 🔍 Открывать страницы
- 📸 Делать скриншоты
- 🖱️ Кликать по элементам
- ⌨️ Вводить текст
- 📄 Читать контент страниц

---

## 🚀 Установка

### Шаг 1: Установить Chrome MCP сервер

```bash
# Через npm (рекомендуется)
npm install -g @modelcontextprotocol/server-puppeteer

# Или через npx (без установки)
# npx @modelcontextprotocol/server-puppeteer
```

### Шаг 2: Настроить в Windsurf

1. **Открой настройки Windsurf:**
   - `Cmd + ,` (Mac) или `Ctrl + ,` (Windows/Linux)
   - Или: `Windsurf` → `Settings`

2. **Найди раздел MCP:**
   - Поиск: "MCP" или "Model Context Protocol"
   - Раздел: `Extensions` → `MCP Servers`

3. **Добавь Chrome MCP:**
   ```json
   {
     "mcpServers": {
       "chrome": {
         "command": "npx",
         "args": [
           "-y",
           "@modelcontextprotocol/server-puppeteer"
         ]
       }
     }
   }
   ```

4. **Перезапусти Windsurf:**
   - `Cmd + Q` → Открыть снова
   - Или: `Windsurf` → `Restart`

### Шаг 3: Проверка установки

После перезапуска Windsurf, Chrome MCP должен быть доступен. Проверь в логах:
- `View` → `Output` → Выбери `MCP Servers`

---

## 🎯 Примеры использования

### Пример 1: Открыть сайт и сделать скриншот

**Запрос к AI:**
```
Открой https://optioner.online и сделай скриншот главной страницы
```

**Что произойдет:**
1. Chrome MCP откроет браузер
2. Перейдет на сайт
3. Сделает скриншот
4. Сохранит в файл

---

### Пример 2: Протестировать форму анализа

**Запрос к AI:**
```
Открой https://optioner.online, введи тикер SPY в форму, 
нажми кнопку анализа и сделай скриншот результата
```

**Что произойдет:**
1. Откроет сайт
2. Найдет поле ввода тикера
3. Введет "SPY"
4. Кликнет кнопку "Analyze"
5. Подождет загрузки
6. Сделает скриншот результата

---

### Пример 3: Проверить responsive дизайн

**Запрос к AI:**
```
Открой https://optioner.online в разных разрешениях 
(desktop, tablet, mobile) и сделай скриншоты
```

**Что произойдет:**
1. Откроет сайт в desktop разрешении (1920x1080)
2. Скриншот
3. Изменит размер на tablet (768x1024)
4. Скриншот
5. Изменит на mobile (375x667)
6. Скриншот

---

### Пример 4: Парсинг данных

**Запрос к AI:**
```
Открой https://optioner.online/analysis/123 
и извлеки данные анализа (ticker, price, metrics)
```

**Что произойдет:**
1. Откроет страницу анализа
2. Извлечет HTML контент
3. Распарсит данные
4. Вернет структурированный JSON

---

## 🔧 Доступные команды Chrome MCP

### Навигация
- `navigate(url)` - открыть URL
- `goBack()` - назад
- `goForward()` - вперед
- `reload()` - перезагрузить

### Взаимодействие
- `click(selector)` - кликнуть по элементу
- `type(selector, text)` - ввести текст
- `select(selector, value)` - выбрать в dropdown
- `hover(selector)` - навести курсор

### Получение данных
- `screenshot(filename)` - скриншот
- `getContent()` - получить HTML
- `getText(selector)` - получить текст элемента
- `getAttribute(selector, attr)` - получить атрибут

### Ожидание
- `wait(ms)` - подождать миллисекунды
- `waitForSelector(selector)` - ждать появления элемента
- `waitForNavigation()` - ждать загрузки страницы

---

## 🎨 Практические сценарии для проекта

### 1. E2E тестирование Options Analyzer

**Цель:** Проверить весь flow анализа

```javascript
// Открыть сайт
navigate("https://optioner.online")

// Ввести тикер
click("#ticker-input")
type("#ticker-input", "SPY")

// Выбрать AI модель
click("#ai-model-select")
select("#ai-model-select", "gemini")

// Запустить анализ
click("#analyze-button")

// Ждать результата (до 10 минут для AI)
waitForSelector("#analysis-result", { timeout: 600000 })

// Проверить что результат есть
const result = getText("#analysis-result")
screenshot("analysis-complete.png")

// Проверить что есть метрики
const metrics = getText("#metrics-section")
screenshot("metrics.png")
```

---

### 2. Мониторинг production UI

**Цель:** Проверить что сайт работает

```javascript
// Открыть production
navigate("https://optioner.online")

// Проверить что загрузился
waitForSelector("body")
screenshot("homepage-check.png")

// Проверить health endpoint
navigate("https://optioner.online/api/health")
const health = getContent()
// Должен быть: {"status": "healthy"}

// Если ошибка - отправить алерт в Slack
if (!health.includes("healthy")) {
  // Отправить уведомление
  sendSlackAlert("🚨 Production site is down!")
}
```

---

### 3. Генерация скриншотов для документации

**Цель:** Автоматические скриншоты для README

```javascript
// Главная страница
navigate("https://optioner.online")
screenshot("docs/screenshots/homepage.png")

// Форма анализа
click("#ticker-input")
type("#ticker-input", "SPY")
screenshot("docs/screenshots/ticker-input.png")

// Результат анализа (с тестовыми данными)
navigate("https://optioner.online/analysis/test-id")
waitForSelector("#analysis-result")
screenshot("docs/screenshots/analysis-result.png")

// Options Calculator
navigate("https://optioner.online/tools/options-calculator")
screenshot("docs/screenshots/calculator.png")
```

---

### 4. Тестирование responsive дизайна

**Цель:** Проверить на разных устройствах

```javascript
const devices = [
  { name: "Desktop", width: 1920, height: 1080 },
  { name: "Laptop", width: 1366, height: 768 },
  { name: "Tablet", width: 768, height: 1024 },
  { name: "Mobile", width: 375, height: 667 }
]

for (const device of devices) {
  // Установить размер
  setViewport(device.width, device.height)
  
  // Открыть сайт
  navigate("https://optioner.online")
  
  // Скриншот
  screenshot(`responsive/${device.name.toLowerCase()}.png`)
}
```

---

## 🐛 Troubleshooting

### Проблема: Chrome MCP не запускается

**Решение:**
```bash
# Проверь что установлен
which npx

# Переустанови
npm install -g @modelcontextprotocol/server-puppeteer

# Проверь версию Node.js (нужна 18+)
node --version
```

---

### Проблема: Скриншоты не сохраняются

**Решение:**
```bash
# Проверь права на запись
ls -la screenshots/

# Создай папку если нет
mkdir -p screenshots
chmod 755 screenshots
```

---

### Проблема: Timeout при загрузке страницы

**Решение:**
```javascript
// Увеличь timeout
waitForSelector("#element", { timeout: 60000 }) // 60 секунд

// Или используй waitForNavigation
navigate("https://optioner.online")
waitForNavigation({ timeout: 30000 })
```

---

## 📊 Метрики использования

После внедрения Chrome MCP отслеживай:

1. **Автоматизация тестов:**
   - Количество E2E тестов
   - Время выполнения
   - Процент успешных тестов

2. **Мониторинг:**
   - Uptime production сайта
   - Время отклика страниц
   - Количество ошибок UI

3. **Документация:**
   - Количество автоматических скриншотов
   - Актуальность документации
   - Время на обновление docs

---

## 🔗 Полезные ссылки

- [Chrome MCP GitHub](https://github.com/modelcontextprotocol/servers/tree/main/src/puppeteer)
- [Puppeteer API Docs](https://pptr.dev/)
- [MCP Protocol Docs](https://modelcontextprotocol.io/)
- [Windsurf MCP Guide](https://docs.codeium.com/windsurf/mcp)

---

## 🎯 Следующие шаги

1. ✅ Установить Chrome MCP
2. ✅ Протестировать на локальном сайте
3. 🟡 Создать E2E тесты для основных flows
4. 🟡 Настроить автоматические скриншоты
5. 🟡 Интегрировать в CI/CD

---

## 💡 Советы

1. **Используй headless режим** для CI/CD
2. **Сохраняй скриншоты** в отдельную папку
3. **Добавь retry логику** для нестабильных тестов
4. **Логируй все действия** для отладки
5. **Используй селекторы** по data-testid, а не по классам

---

*Последнее обновление: 12 октября 2025*
*Ответственный: Levon*
