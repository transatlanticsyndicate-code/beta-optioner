# 🤖 Руководство по написанию промптов для Gemini AI

## Проблема: Safety Filters

Google Gemini имеет строгие **safety policies**, которые блокируют контент, связанный с:
- Финансовыми советами и рекомендациями
- Потенциальными рисками и убытками
- Манипуляцией рынком
- "Опасным" контентом

**Finish Reason 2 = SAFETY** - означает, что промпт заблокирован.

---

## ✅ Что РАБОТАЕТ

### 1. Обязательный дисклеймер

**ВСЕГДА начинай промпт с:**

```
ОБРАЗОВАТЕЛЬНЫЙ АНАЛИЗ ОПЦИОННЫХ ДАННЫХ
(Только для обучения, не является инвестиционной рекомендацией)
```

Это сигнализирует Gemini, что контент образовательный, а не торговый совет.

---

### 2. Безопасные формулировки

#### ✅ ИСПОЛЬЗУЙ:

| Вместо (блокируется) | Используй (проходит) |
|---------------------|---------------------|
| "Риски" | "Зоны внимания", "Особенности" |
| "Убытки" | "Позиции", "Концентрации" |
| "Опасность" | "Значимость", "Важность" |
| "Stop-loss кластеры" | "Концентрации позиций" |
| "Рекомендуем покупать" | "Наблюдается активность" |
| "Прогноз цены" | "Возможное поведение" |
| "Инвестиционная стратегия" | "Техническое описание" |

---

### 3. Фокус на описании, а не действиях

#### ❌ НЕ ПИШИ:
```
"Рекомендуем продать путы на уровне $450"
"Высокий риск падения цены"
"Следует закрыть позиции"
```

#### ✅ ПИШИ:
```
"Наблюдается концентрация PUT позиций на уровне $450"
"Значительное отклонение от Max Pain"
"Участники рынка демонстрируют активность"
```

---

### 4. Структура промпта

**Безопасная структура:**

```
[ДИСКЛЕЙМЕР]

Тикер: {symbol}
Данные: {data}

Задача: Предоставь объективное ОПИСАНИЕ текущей ситуации.

Структура ответа:
1. НАБЛЮДАЕМЫЕ уровни
2. ТЕКУЩИЙ сентимент
3. НЕОБЫЧНАЯ активность
4. ЗОНЫ внимания
5. СТРУКТУРНЫЕ особенности

Правила:
- Используй только фактические данные
- НЕ давай инвестиционных рекомендаций
- НЕ делай прогнозов цены

Начинай анализ:
```

---

## ❌ Что БЛОКИРУЕТСЯ

### 1. Слова-триггеры

**Избегай этих слов:**
- Риск, риски, рисковый
- Убыток, убытки, потери
- Опасность, опасный
- Рекомендация, рекомендуем
- Покупать, продавать, закрывать
- Прогноз, предсказание
- Стратегия, тактика
- Stop-loss, take-profit
- Манипуляция
- Гарантия, обещание

---

### 2. Конкретные примеры в промпте

**❌ НЕ включай примеры с реальными ценами:**

```
=== ПРИМЕР ОТВЕТА ===
• $458 - Поддержка (PUT OI 15,000)
• $460 - Max Pain
• Риск падения к $450
```

Gemini может воспринять это как реальный торговый совет.

**✅ Вместо этого:**

```
Формат ответа: структурированный текст с конкретными числами из данных.
```

---

### 3. Прямые торговые действия

**❌ НЕ пиши:**
- "Следует открыть позицию"
- "Рекомендуем хеджировать"
- "Выгодно купить коллы"
- "Закройте путы"

**✅ Пиши:**
- "Наблюдается активность в коллах"
- "Участники рынка открывают позиции"
- "Распределение показывает концентрацию"

---

## 🔧 Настройки модели

### Safety Settings

В коде обязательно используй:

```python
from google.generativeai.types import HarmCategory, HarmBlockThreshold

model = genai.GenerativeModel(
    'gemini-2.5-flash',
    safety_settings={
        HarmCategory.HARM_CATEGORY_HARASSMENT: HarmBlockThreshold.BLOCK_NONE,
        HarmCategory.HARM_CATEGORY_HATE_SPEECH: HarmBlockThreshold.BLOCK_NONE,
        HarmCategory.HARM_CATEGORY_SEXUALLY_EXPLICIT: HarmBlockThreshold.BLOCK_NONE,
        HarmCategory.HARM_CATEGORY_DANGEROUS_CONTENT: HarmBlockThreshold.BLOCK_NONE,
    }
)
```

**Важно:** Настройки должны быть в `GenerativeModel()`, а НЕ в `generate_content()`.

---

## 📝 Шаблон рабочего промпта

```
ОБРАЗОВАТЕЛЬНЫЙ АНАЛИЗ ОПЦИОННЫХ ДАННЫХ
(Только для обучения, не является инвестиционной рекомендацией)

Тикер: {symbol}
Текущая цена: {current_price}
Max Pain уровень: {max_pain}
Put/Call Ratio: {put_call_ratio}

Задача: Предоставь детальное объективное описание текущей ситуации на опционном рынке.

Структура ответа:

1. НАБЛЮДАЕМЫЕ ЦЕНОВЫЕ УРОВНИ
- Определи значимые уровни на основе Open Interest
- Объясни почему каждый уровень важен
- Укажи расстояние от текущей цены

2. ТЕКУЩИЙ РЫНОЧНЫЙ СЕНТИМЕНТ
- Интерпретируй Put/Call Ratio
- Проанализируй распределение позиций
- Определи преобладающий сентимент

3. НЕОБЫЧНАЯ АКТИВНОСТЬ
- Отметь аномалии в объемах
- Укажи необычные изменения Open Interest

4. ЗОНЫ ВНИМАНИЯ
- Укажи уровни с низкой ликвидностью
- Определи зоны повышенной волатильности

5. СТРУКТУРНЫЕ ОСОБЕННОСТИ
- Опиши форму кривой волатильности
- Сделай выводы о позициях маркет-мейкеров

Правила:
- Используй только фактические данные
- Указывай конкретные числа
- Пиши на русском языке
- НЕ давай инвестиционных рекомендаций
- НЕ делай прогнозов цены

Начинай анализ:
```

---

## 🧪 Тестирование промпта

### Быстрый тест:

```python
import google.generativeai as genai
from google.generativeai.types import HarmCategory, HarmBlockThreshold

genai.configure(api_key='YOUR_API_KEY')

model = genai.GenerativeModel(
    'gemini-2.5-flash',
    safety_settings={
        HarmCategory.HARM_CATEGORY_HARASSMENT: HarmBlockThreshold.BLOCK_NONE,
        HarmCategory.HARM_CATEGORY_HATE_SPEECH: HarmBlockThreshold.BLOCK_NONE,
        HarmCategory.HARM_CATEGORY_SEXUALLY_EXPLICIT: HarmBlockThreshold.BLOCK_NONE,
        HarmCategory.HARM_CATEGORY_DANGEROUS_CONTENT: HarmBlockThreshold.BLOCK_NONE,
    }
)

prompt = "Твой промпт здесь"

response = model.generate_content(prompt)

# Проверка
print(f"Finish reason: {response.candidates[0].finish_reason}")
# 1 = STOP (успех)
# 2 = SAFETY (заблокирован)

if response.candidates[0].finish_reason == 1:
    print("✅ Промпт прошел!")
    print(response.text)
else:
    print("❌ Промпт заблокирован")
    print(f"Safety ratings: {response.candidates[0].safety_ratings}")
```

---

## 📊 Примеры из проекта

### ❌ Не работало (блокировалось):

```
Проанализируй опционы SPY:
- Цена: $669
- Max Pain: $450

Дай анализ рисков и рекомендации по торговле.

1. Ключевые уровни
2. Риски
3. Рекомендации
```

**Проблемы:**
- Нет дисклеймера
- Слово "риски"
- Просьба дать "рекомендации"

---

### ✅ Работает:

```
ОБРАЗОВАТЕЛЬНЫЙ АНАЛИЗ ОПЦИОННЫХ ДАННЫХ
(Только для обучения, не является инвестиционной рекомендацией)

Тикер: SPY
Текущая цена: 669.21
Max Pain: 450.00
Put/Call Ratio: 0.85

Задача: Предоставь объективное описание текущей ситуации.

Структура:
1. НАБЛЮДАЕМЫЕ УРОВНИ
2. ТЕКУЩИЙ СЕНТИМЕНТ
3. ЗОНЫ ВНИМАНИЯ

Правила:
- Только фактические данные
- НЕ давай рекомендаций

Начинай анализ:
```

**Почему работает:**
- ✅ Есть дисклеймер
- ✅ "Зоны внимания" вместо "риски"
- ✅ "Описание" вместо "рекомендации"
- ✅ Четкие правила

---

## 🎯 Чек-лист перед отправкой промпта

- [ ] Есть дисклеймер в начале
- [ ] Нет слов-триггеров (риски, убытки, опасность)
- [ ] Нет прямых торговых рекомендаций
- [ ] Фокус на "описании", а не "действиях"
- [ ] Нет конкретных примеров с ценами в промпте
- [ ] Есть четкое правило "НЕ давай рекомендаций"
- [ ] Safety settings настроены в коде
- [ ] Промпт протестирован отдельно

---

## 🔍 Отладка

### Если промпт блокируется:

1. **Проверь finish_reason:**
   ```python
   print(response.candidates[0].finish_reason)
   # 2 = SAFETY
   ```

2. **Посмотри safety_ratings:**
   ```python
   print(response.candidates[0].safety_ratings)
   ```

3. **Упрости промпт:**
   - Убери половину текста
   - Тестируй по частям
   - Найди проблемное слово

4. **Замени слова-триггеры:**
   - Ctrl+F "риск" → "зона внимания"
   - Ctrl+F "убыток" → "позиция"
   - Ctrl+F "рекомендация" → "описание"

---

## 📚 Полезные ссылки

- [Gemini API Safety Settings](https://ai.google.dev/docs/safety_setting_gemini)
- [Gemini Models](https://ai.google.dev/models/gemini)
- [Best Practices](https://ai.google.dev/docs/prompt_best_practices)

---

## 💡 Итоги
**Ключевые принципы:**

1. **Дисклеймер обязателен** - всегда в начале
2. **Описывай, не советуй** - факты, а не рекомендации
3. **Избегай триггеров** - "зоны" вместо "рисков"
4. **Тестируй отдельно** - перед интеграцией
5. **Safety settings** - настраивай в коде

**Помни:** Gemini очень чувствителен к финансовому контенту. Лучше быть излишне осторожным в формулировках, чем получить блокировку.

---

*Последнее обновление: 2025-01-04*
*Проект: Options Flow AI Analyzer*

---

## 🔬 Найденные особенности при работе с Gemini (обновлено 2025-01-04)

### ✅ Что РАБОТАЕТ:

1. **Визуальные разделители**
   ```
   ═══════════════════════════════════════════════════════════════
   ```
   - Gemini хорошо воспринимает визуальное структурирование
   - Помогает четко отделить секции промпта

2. **Явные инструкции с символами**
   ```
   ✓ Делай это
   ✗ НЕ делай это
   ```
   - Gemini лучше следует инструкциям с визуальными маркерами
   - Снижает вероятность нарушения правил

3. **Детальная структура с подпунктами**
   ```
   ## 1. РАЗДЕЛ
   
   Инструкция:
   - Пункт 1
   - Пункт 2
     * Подпункт 2.1
     * Подпункт 2.2
   ```
   - Чем детальнее структура, тем точнее ответ
   - Gemini хорошо следует иерархии

4. **Явное указание ограничений данных**
   ```
   ВАЖНО: Если Open Interest = 0 или данные ограничены, 
   ОБЯЗАТЕЛЬНО укажи это в начале анализа
   ```
   - Gemini честно признает ограничения если попросить
   - Снижает риск галлюцинаций

5. **Конкретные числовые диапазоны**
   ```
   * 0.7 и ниже = бычий
   * 0.8-1.2 = нейтральный
   * 1.3 и выше = медвежий
   ```
   - Gemini точнее интерпретирует с конкретными порогами
   - Избегает субъективных оценок

### ❌ Что НЕ РАБОТАЕТ (вызывает блокировки):

1. **Слова-триггеры в промпте**
   - ❌ "риск", "опасность", "убыток"
   - ✅ "зона внимания", "особенность", "характеристика"

2. **Прямые торговые рекомендации в инструкциях**
   - ❌ "Порекомендуй стратегию"
   - ✅ "Опиши наблюдаемую структуру"

3. **Категоричные утверждения**
   - ❌ "Цена обязательно пойдет вверх"
   - ✅ "Данные указывают на..."

4. **Сложные вложенные условия**
   - Gemini может запутаться в многоуровневых if-then
   - Лучше разбить на простые последовательные инструкции

### 🎯 Оптимальная длина промпта:

- **Минимум:** 200 токенов - слишком мало контекста
- **Оптимум:** 400-800 токенов - баланс детальности и ясности
- **Максимум:** 1500 токенов - начинает терять фокус

### �� Оптимальная длина ответа:

- **Короткий:** 200-400 токенов - для быстрых инсайтов
- **Средний:** 800-1200 токенов - детальный анализ (рекомендуется)
- **Длинный:** 1500+ токенов - может терять качество к концу

### 🔄 Температура и креативность:

Для финансового анализа:
- **Temperature: 0.3** - оптимально (факты, минимум креатива)
- **Top P: 0.8** - хороший баланс
- **Top K: 40** - достаточное разнообразие

### 💾 Хранение промптов:

**✅ Лучшая практика:**
```python
# Промпт в отдельном файле
prompt = load_from_file("prompts/options_analysis_prompt.txt")

# Заполнение переменных
formatted = prompt.format(
    ticker=ticker,
    current_price=f"${price:.2f}",
    ...
)

# Отправка в Gemini
response = model.generate_content(formatted)
```

**Преимущества:**
- ✅ Легко редактировать без изменения кода
- ✅ Версионность в git
- ✅ A/B тестирование разных версий
- ✅ Быстрая итерация

### 🛡️ Safety Settings:

Для финансового контента:
```python
safety_settings = [
    {
        "category": "HARM_CATEGORY_DANGEROUS_CONTENT",
        "threshold": "BLOCK_ONLY_HIGH"
    },
    {
        "category": "HARM_CATEGORY_HARASSMENT",
        "threshold": "BLOCK_ONLY_HIGH"
    }
]
```

**Важно:** Не ставить `BLOCK_NONE` - может привести к нестабильной работе.

### 📝 Markdown форматирование:

Gemini отлично работает с Markdown:
- ✅ `## Заголовки` - четкая структура
- ✅ `**Жирный текст**` - выделение ключевых моментов
- ✅ `* Списки` - перечисления
- ✅ `> Цитаты` - важные замечания
- ❌ Таблицы - иногда ломает форматирование

### 🔍 Обработка ошибок:

```python
try:
    return response.text
except ValueError:
    # Gemini заблокировал ответ
    if response.candidates:
        finish_reason = response.candidates[0].finish_reason
        # 1 = STOP (норма)
        # 2 = SAFETY (блокировка)
        # 3 = MAX_TOKENS (обрезан)
```

### 🎓 Обучение на примерах:

Gemini хорошо работает с few-shot learning:
```
Пример хорошего анализа:
[пример]

Теперь проанализируй эти данные:
[данные]
```

Но для нашего случая достаточно детальных инструкций.

---

## 📈 Метрики качества промпта:

**Хороший промпт:**
- ✅ Gemini генерирует 800-1200 токенов
- ✅ Нет блокировок (finish_reason = 1)
- ✅ Следует всем 5 разделам структуры
- ✅ Использует Markdown правильно
- ✅ Указывает ограничения данных

**Плохой промпт:**
- ❌ Gemini генерирует < 400 токенов
- ❌ Блокировки (finish_reason = 2)
- ❌ Игнорирует структуру
- ❌ Дает торговые рекомендации
- ❌ Выдумывает данные

---

## 🚀 Итоговые рекомендации:

1. **Используй файловое хранение промптов** - легко итерировать
2. **Добавляй визуальные разделители** - улучшает структуру
3. **Будь максимально конкретным** - числа, диапазоны, примеры
4. **Явно запрещай нежелательное** - с символами ✗
5. **Тестируй на разных тикерах** - SPY, AAPL, TSLA
6. **Мониторь finish_reason** - отлавливай блокировки
7. **Итерируй постепенно** - маленькие изменения, тестирование

**Помни:** Gemini - мощный инструмент, но требует аккуратности в формулировках для финансового контента.

