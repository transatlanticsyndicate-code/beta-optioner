# 🚀 НАЧНИ ОТСЮДА - Всего 3 команды!

**Время: 15-20 минут**

---

## Команда 1: Скопируй скрипт на сервер

```bash
cd /Users/levon/Downloads/SYNDICATE/CascadeProjects/windsurf-project
scp -i key_ scripts/setup_server.sh root@optioner.online:/tmp/
```

Введи пароль от SSH ключа (если попросит)

---

## Команда 2: Подключись и запусти скрипт

```bash
ssh -i key_ root@optioner.online "chmod +x /tmp/setup_server.sh && /tmp/setup_server.sh"
```

**Скрипт автоматически сделает всё:**
- Создаст директории
- Клонирует проект
- Создаст тестовую БД
- Настроит backend и frontend
- Настроит Nginx
- Запустит PM2

Подожди 10-15 минут пока выполнится.

---

## Команда 3: Добавь API ключ и получи SSL

```bash
ssh -i key_ root@optioner.online
```

Когда подключишься, выполни:

```bash
# 1. Скопируй OPENAI_API_KEY из production
grep OPENAI_API_KEY /home/deploy/app/backend/.env

# 2. Добавь его в тестовый .env
nano /var/www/test/backend/.env
# Найди строку OPENAI_API_KEY и вставь свой ключ
# Ctrl+O (сохранить), Enter, Ctrl+X (выйти)

# 3. Перезапусти backend
pm2 restart optioner-backend-test

# 4. Получи SSL сертификат
sudo certbot --nginx -d test.optioner.online
# Ответь: email, Y (agree), 2 (redirect to HTTPS)

# 5. Проверь работу
curl https://test.optioner.online/api/health
pm2 list

# 6. Выйди
exit
```

---

## ✅ Готово!

Открой в браузере: **https://test.optioner.online**

Попробуй запрос: SPY, AAPL, TSLA

---

## 🔧 Если нужна помощь

Напиши мне: "Cascade, помоги" + опиши что не работает

---

**Всё! Больше ничего не нужно делать.**
