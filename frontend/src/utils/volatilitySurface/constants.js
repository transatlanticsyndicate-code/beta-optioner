/**
 * Константы конфигурации для Volatility Surface
 * ЗАЧЕМ: Централизованное управление параметрами расчета волатильности
 * Затрагивает: все расчеты IV
 */

// Конфигурация поведения IV Surface
// ЗАЧЕМ: Настройка параметров интерполяции и прогнозирования
export const IV_SURFACE_CONFIG = {
  // Максимальный коэффициент роста IV при приближении к экспирации
  MAX_IV_GROWTH_RATIO: 1.5,
  
  // Минимальное количество дней для расчёта (избегаем деления на 0)
  MIN_DAYS_FOR_CALCULATION: 1,
  
  // IV по умолчанию если нет данных
  DEFAULT_IV: 0.25
};

// IV по умолчанию в процентах
// ЗАЧЕМ: Fallback значение если у опциона нет данных из API
export const DEFAULT_IV_PERCENT = 25;
