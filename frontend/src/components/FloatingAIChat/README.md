# Плавающий AI-чат (Floating AI Chat)

## Описание

Глобальный AI-ассистент на базе Gemini, доступный на всех страницах приложения.

## Основные возможности

### 🎯 Функциональность
- **Глобальный доступ** - работает на всех страницах
- **Сохранение истории** - диалоги сохраняются между сессиями
- **Контекст страницы** - чат знает, на какой странице вы находитесь
- **Ресайз** - можно изменять размер окна
- **Перетаскивание** - можно перемещать по экрану
- **Минимизация** - можно свернуть в компактный вид
- **Горячие клавиши** - Cmd+K (Mac) или Ctrl+K (Windows) для открытия/закрытия

### 💾 Сохранение данных
Все данные сохраняются в localStorage:
- `floatingAIChatMessages` - история сообщений
- `floatingAIChatOpen` - состояние открытия/закрытия
- `floatingAIChatPosition` - позиция окна на экране
- `floatingAIChatSize` - размер окна

### 🎨 UI/UX особенности
- **Плавающая кнопка** - красивая кнопка с градиентом и анимацией
- **Счетчик непрочитанных** - показывает количество новых сообщений
- **Быстрые вопросы** - предустановленные вопросы для быстрого доступа
- **Анимации** - плавные переходы и эффекты
- **Темная тема** - в стиле остального интерфейса

## Использование

### Открытие чата
1. Нажмите на плавающую кнопку в правом нижнем углу
2. Или используйте горячую клавишу: **Cmd+K** (Mac) / **Ctrl+K** (Windows)

### Управление окном
- **Перетаскивание**: Захватите заголовок окна (с иконкой GripVertical)
- **Изменение размера**: Потяните за края или углы окна
- **Минимизация**: Нажмите кнопку минимизации в заголовке
- **Закрытие**: Нажмите X или используйте Cmd+K

### Работа с чатом
1. Введите вопрос в поле ввода
2. Нажмите Enter или кнопку отправки
3. Используйте быстрые вопросы для частых запросов
4. Очистите историю через кнопку Sparkles в заголовке

## Интеграция

### Добавление в приложение
Компонент уже интегрирован в `App.js` и доступен глобально:

```javascript
import FloatingAIChat from './components/FloatingAIChat/FloatingAIChat';

function App() {
  return (
    <>
      {/* Ваши роуты */}
      <FloatingAIChat />
    </>
  );
}
```

### API эндпоинт
Чат использует эндпоинт `/api/ai/chat` для общения с Gemini:

```javascript
POST /api/ai/chat
{
  "message": "Пользовательский вопрос",
  "context": "Контекст текущей страницы",
  "history": [...] // Последние 10 сообщений
}
```

## Настройка

### Изменение начальной позиции
По умолчанию окно появляется в правом нижнем углу. Можно изменить в коде:

```javascript
const [position, setPosition] = useState(() => {
  return { x: window.innerWidth - 420, y: window.innerHeight - 620 };
});
```

### Изменение размера по умолчанию
```javascript
const [size, setSize] = useState(() => {
  return { width: 400, height: 600 };
});
```

### Настройка быстрых вопросов
Отредактируйте массив в компоненте:

```javascript
{[
  'Объясни мои риски',
  'Что такое Delta?',
  'Как улучшить портфель?'
].map((question, index) => (
  // ...
))}
```

## Технические детали

### Зависимости
- `react-rnd` - для ресайза и перетаскивания
- `lucide-react` - иконки
- `axios` - HTTP запросы

### Структура компонента
```
FloatingAIChat/
├── FloatingAIChat.js  - Основной компонент
└── README.md          - Документация
```

### Состояния компонента
1. **Закрыт** - показывается только плавающая кнопка
2. **Минимизирован** - компактная плашка с названием
3. **Открыт** - полное окно чата

## Будущие улучшения

### Планируемые фичи
- [ ] История сессий (переключение между разными диалогами)
- [ ] Экспорт истории чата
- [ ] Голосовой ввод
- [ ] Прикрепление файлов/скриншотов
- [ ] Темы оформления (светлая/темная)
- [ ] Настройки через UI (размер шрифта, звуки и т.д.)
- [ ] Интеграция с данными калькулятора (автоматический анализ позиций)

## Troubleshooting

### Чат не открывается
- Проверьте консоль браузера на ошибки
- Убедитесь, что `react-rnd` установлен: `npm install react-rnd`

### Сообщения не отправляются
- Проверьте доступность API: `/api/ai/chat`
- Проверьте настройки Gemini API в backend

### Позиция не сохраняется
- Проверьте localStorage в DevTools
- Убедитесь, что localStorage не заблокирован

### Окно вышло за пределы экрана
Очистите localStorage:
```javascript
localStorage.removeItem('floatingAIChatPosition');
```
Или в консоли браузера:
```javascript
localStorage.clear();
```
