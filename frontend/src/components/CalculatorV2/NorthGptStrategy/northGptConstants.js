/**
 * Константы стратегии «Север GPT».
 */

export const NORTH_GPT_KIND = 'gpt';

// Промпт по умолчанию (когда библиотека пуста). Служит и рабочим промптом,
// и образцом-шаблоном для пользователя.
export const DEFAULT_NORTH_GPT_PROMPT = `Ты — опытный опционный стратег. Подбери защитную конструкцию из покупки опционов Call и/или Put к длинной позиции по базовому активу.

Цели:
- на верхней цели — максимальный P&L опционов;
- на нижней цели — P&L всей позиции близок к нулю;
- удерживайся в коридоре маржина (margin ± marginTolerance).

Используй только страйки из присланной цепочки и только в заданных диапазонах. Для блока «актив + опционы» сам выбери разумное количество акций. Кратко поясни логику выбора.`;
