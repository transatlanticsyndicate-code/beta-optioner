/**
 * Константы стратегии «Север GPT».
 */

export const NORTH_GPT_KIND = 'gpt';

// Промпт по умолчанию (когда библиотека пуста). Служит и рабочим промптом,
// и образцом использования плейсхолдеров — вместо них при подборе
// подставляются реальные числа из параметров.
export const DEFAULT_NORTH_GPT_PROMPT = `Вход в сделку {вход}.
Цель по верху {цель_верх} — на этой цене результат должен быть максимально позитивным.
Выход по низу {цель_низ} (стоп-лосс) — на этой цене результат ВСЕЙ позиции близок к нулю, в пределах ± {допуск_низ}.
Маржин держи в коридоре {маржин} ± {допуск_маржин}.
Горизонт оценки — {дней}-й день.

Подбери оптимальную опционную конструкцию под эти условия из доступных страйков.`;
