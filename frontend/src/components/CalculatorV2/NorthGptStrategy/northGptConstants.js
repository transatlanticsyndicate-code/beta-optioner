/**
 * Константы стратегии «Север GPT».
 */

export const NORTH_GPT_KIND = 'gpt';

// Промпт по умолчанию (когда библиотека пуста). Служит и рабочим промптом,
// и образцом использования плейсхолдеров — вместо них при подборе
// подставляются реальные числа из параметров.
export const DEFAULT_NORTH_GPT_PROMPT = `Вход в сделку {вход}
Цель {цель_верх}, кроем все опционы
Выход {цель_низ}, кроем все опционы (стоп-лосс)
Маржин на опционы {маржин}, +/- {допуск_маржин}
Результат при верхнем диапазоне должен быть максимально позитивным
Результат при нижнем диапазоне должен быть ближе к 0, с диапазоном +/- {допуск_низ}

Сложи оптимальную опционную конструкцию со всеми условиями из списка и доступными страйками. Рассчитывай прогнозный результат на {дней}-й день.`;
