# Отладка функционала "Рейтинг криптовалют"

## Проблема: Не видно статусов операций

### Шаг 1: Откройте консоль браузера

1. Откройте страницу http://localhost:3000/tools/crypto-rating
2. Нажмите **F12** или **Ctrl+Shift+I** (открыть DevTools)
3. Перейдите на вкладку **Console**

### Шаг 2: Заполните форму и нажмите "Запустить мониторинг"

Вы должны увидеть в консоли:

```
🚀 Начало создания задачи...
Параметры: {dayOfWeek: "monday", time: "10:00", intervalValue: "24", intervalUnit: "hours"}
📡 Отправка запроса на: http://localhost:8000/api/crypto-rating/schedule
📥 Ответ получен: 200
✅ Задача создана: {id: 1, day_of_week: "monday", ...}
🔄 Обновление информации об активной задаче...
📡 Загрузка активной задачи...
📥 Ответ: 200
✅ Активная задача загружена: {id: 1, ...}
✅ Завершено
```

### Возможные проблемы и решения

#### ❌ Ошибка: "Failed to fetch" или "Network error"

**Причина:** Backend не запущен или недоступен

**Решение:**
1. Проверьте, что backend запущен:
   ```powershell
   Get-Process | Where-Object {$_.ProcessName -like "*uvicorn*"}
   ```
2. Если не запущен, запустите:
   ```powershell
   cd backend
   .\venv\Scripts\Activate.ps1
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

#### ❌ Ошибка: "404 Not Found" на `/api/crypto-rating/active-task`

**Причина:** Backend запущен со старой версией кода (без нового эндпоинта)

**Решение:**
1. Перезапустите backend (см. выше)
2. Убедитесь, что в файле `backend/app/routers/crypto_rating.py` есть эндпоинт:
   ```python
   @router.get("/active-task", response_model=TaskInfo)
   ```

#### ❌ Ошибка: "500 Internal Server Error"

**Причина:** Ошибка на backend

**Решение:**
1. Откройте окно PowerShell с backend
2. Посмотрите логи ошибок
3. Проверьте, что миграция БД выполнена:
   ```powershell
   python run_crypto_migration.py
   ```

#### ℹ️ Сообщение: "Активной задачи нет"

**Причина:** Задача еще не создана или была удалена

**Решение:**
1. Это нормально при первом запуске
2. Создайте задачу через форму
3. После создания информационный блок появится автоматически

### Шаг 3: Проверка БД

Проверьте, что задача создана в БД:

```powershell
cd backend
python check_active_task.py
```

Должно показать:
```
📊 Всего активных задач: 1

ID: 1
День недели: monday
Время: 10:00
Интервал: 24 hours
...
```

### Шаг 4: Проверка отображения

После успешного создания задачи:

1. **Должно появиться зеленое сообщение:**
   ```
   ✅ Мониторинг успешно запущен! Первый снимок будет создан в указанное время.
   ```

2. **Должен появиться синий блок справа от кнопки:**
   ```
   🕐 Активный мониторинг
   Понедельник в 10:00 (New York), интервал: 24 часа
   Следующий снимок: [дата]
   ───────────────────────────
   Статус операций:
   ● Ожидается первый снимок: [дата]
   ```

### Шаг 5: Если ничего не помогло

1. **Очистите кэш браузера:**
   - Ctrl+Shift+Delete
   - Очистить кэш
   - Перезагрузить страницу (Ctrl+F5)

2. **Проверьте вкладку Network в DevTools:**
   - Откройте вкладку Network
   - Нажмите "Запустить мониторинг"
   - Найдите запрос к `/api/crypto-rating/schedule`
   - Посмотрите статус и ответ

3. **Проверьте версию файла:**
   ```powershell
   # Должна быть строка с логированием
   Select-String -Path "frontend/src/pages/CryptoRating.jsx" -Pattern "console.log.*Начало создания задачи"
   ```

## Контрольный список

- [ ] Backend запущен (uvicorn)
- [ ] Frontend запущен (npm start)
- [ ] Миграция БД выполнена
- [ ] Консоль браузера открыта (F12)
- [ ] Форма заполнена (день, время, интервал)
- [ ] Нажата кнопка "Запустить мониторинг"
- [ ] В консоли видны логи с эмодзи (🚀, 📡, ✅)
- [ ] Появилось зеленое сообщение об успехе
- [ ] Появился синий блок с информацией о задаче

---

**Если все пункты выполнены, но блок не появляется:**
Пришлите скриншот консоли браузера и логов backend.
