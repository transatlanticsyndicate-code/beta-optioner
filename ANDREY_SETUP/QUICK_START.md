# ⚡ Быстрый старт для Андрея

## За 3 минуты до первого подключения

### 1️⃣ Проверь SSH ключ
```bash
ls -la ~/.ssh/id_optioner
```

Если ключа нет — попроси у Левона и сохрани:
```bash
nano ~/.ssh/id_optioner
# Вставить содержимое, Ctrl+X, Y, Enter
chmod 600 ~/.ssh/id_optioner
```

### 2️⃣ Открой SSH туннель
```bash
cd __Levon_tasks/IB/workingTWS/ANDREY_SETUP
./connect_to_prod_tws_andrey.sh
```

Должен увидеть:
```
✅ Туннель успешно открыт (PID: XXXXX)
✅ TWS API доступен на localhost:4002
```

### 3️⃣ Протестируй подключение
В другом терминале:
```bash
cd __Levon_tasks/IB/workingTWS/ANDREY_SETUP
python test_tws_connection_andrey.py
```

Если всё работает:
```
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Подключение работает корректно.
```

---

## 🎯 Теперь ты готов!

Можешь:
- ✅ Разрабатывать скрипты с `ib_insync`
- ✅ Работать в Jupyter notebooks
- ✅ Тестировать опционные стратегии

Помни:
- ❌ Не размещай ордера
- ❌ Не используй чужой clientId
- ✅ Закрывай соединение после работы (`ib.disconnect()`)

---

## 📖 Если что-то не работает

Читай `SETUP_FOR_ANDREY.md` — там полная инструкция и troubleshooting.

Если всё ещё не работает — пиши в чат! 🚀
