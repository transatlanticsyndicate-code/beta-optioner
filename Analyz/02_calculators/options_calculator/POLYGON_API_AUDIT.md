# Аудит API интеграции Polygon/Massiv в калькуляторе опционов

**Дата аудита:** 2025-11-18  
**Проведено:** ИИ-аналитик  
**Статус:** ✅ Завершено

---

## 🎯 Обзор интеграции

Калькулятор опционов интегрирован с **Polygon.io API** через промежуточный сервис **Massive**. API предоставляет реальные рыночные данные: цены активов, опционные цепочки, даты экспирации, греки и исторические данные.

**Бэкенд:** Python FastAPI с сервисами `PolygonClient` и `OptionsService`  
**Фронтенд:** React с кэшированием и последовательной загрузкой данных

---

## 📡 Полный список API эндпоинтов

### 🔹 1. `/api/polygon/ticker/{ticker}`
**Назначение:** Получение текущей цены актива  
**Вызывается:** `OptionsCalculatorBasic.jsx:316` при выборе тикера  
**Частота:** Один раз при выборе нового тикера  

**Параметры:**
- `ticker` - тикер актива (AAPL, SPY, TSLA и т.д.)

**Ответ:**
```json
{
  "status": "success",
  "ticker": "AAPL",
  "price": 245.27,
  "bid": 245.25,
  "ask": 245.30,
  "last": 245.27,
  "change": -0.80,
  "change_percent": -0.32
}
```

**Использование в коде:**
- Устанавливает `currentPrice` и `priceChange`
- Отображается в правом верхнем углу поля поиска тикера

---

### 🔹 2. `/api/polygon/ticker/{ticker}/expirations`
**Назначение:** Получение дат экспирации опционов  
**Вызывается:** `OptionsCalculatorBasic.jsx:248` после получения цены  
**Частота:** Один раз при выборе нового тикера  
**Кэширование:** TTL 5-1440 минут (настраивается)  

**Параметры:**
- `ticker` - тикер актива

**Ответ:**
```json
{
  "status": "success",
  "ticker": "AAPL",
  "dates": ["2025-11-15", "2025-11-22", "2025-11-29", ...]
}
```

**Использование в коде:**
- Заполняет календарь дат экспирации
- Группируется по месяцам для UI
- Кэшируется в `localStorage` под ключом `expirations_${ticker}`

---

### 🔹 3. `/api/polygon/ticker/{ticker}/options`
**Назначение:** Получение полной опционной цепочки на дату  
**Вызывается:** `OptionsBoard.jsx:93` при выборе даты в календаре  
**Частота:** При каждом выборе новой даты экспирации  

**Параметры:**
- `ticker` - тикер актива
- `expiration_date` - дата в формате YYYY-MM-DD

**Ответ:**
```json
{
  "status": "success",
  "ticker": "AAPL",
  "options": [
    {
      "type": "CALL",
      "strike": 240,
      "bid": 12.50,
      "ask": 12.80,
      "last": 12.65,
      "volume": 1250,
      "open_interest": 5000,
      "delta": 0.65,
      "gamma": 0.03,
      "theta": -0.15,
      "vega": 0.25,
      "implied_volatility": 0.22
    }
  ]
}
```

**Использование в коде:**
- Заполняет доску опционов (OptionsBoard)
- Используется для фильтрации и поиска страйков
- Данные фильтруются по volume, OI, moneyness

---

### 🔹 4. `/api/polygon/ticker/{ticker}/option-details`
**Назначение:** Детальная информация конкретного опциона  
**Вызывается:** `OptionsCalculatorBasic.jsx:187` при добавлении опциона  
**Частота:** При каждом добавлении нового опциона в калькулятор  

**Параметры:**
- `ticker` - тикер актива
- `expiration_date` - дата экспирации (YYYY-MM-DD)
- `strike` - страйк цена (число)
- `option_type` - CALL или PUT

**Ответ:**
```json
{
  "status": "success",
  "ticker": "AAPL",
  "details": {
    "premium": 5.25,
    "bid": 5.20,
    "ask": 5.30,
    "volume": 850,
    "open_interest": 3200,
    "delta": 0.35,
    "gamma": 0.05,
    "theta": -0.08,
    "vega": 0.18,
    "implied_volatility": 0.28
  }
}
```

**Использование в коде:**
- Обновляет данные опциона в состоянии калькулятора
- Заполняет премию, bid/ask, volume, OI, греки
- Используется для расчетов P&L

---

### 🔹 5. `/api/polygon/ticker/{ticker}/bars`
**Назначение:** Исторические бары для графика  
**Вызывается:** `AdvancedChart.jsx:52` при загрузке TradingView графика  
**Частота:** При открытии графика для каждого тикера  

**Параметры:**
- `ticker` - тикер актива
- `from` - дата начала (YYYY-MM-DD)
- `to` - дата окончания (YYYY-MM-DD)

**Ответ:**
```json
[
  {
    "time": 1735689600,
    "open": 245.10,
    "high": 246.50,
    "low": 244.80,
    "close": 245.27
  }
]
```

**Использование в коде:**
- Создает свечной график в компоненте AdvancedChart
- Конвертируется в формат Lightweight Charts

---

### 🔹 6. `/api/polygon/ticker/{ticker}/details`
**Назначение:** Информация о компании  
**Вызывается:** `TickerSearch.jsx:133` при выборе тикера  
**Частота:** Один раз при выборе нового тикера  
**Примечание:** ⚠️ Эндпоинт не реализован в бэкенде, запрос идет напрямую к Polygon API

---

## 🔄 Последовательность загрузки данных

### Шаг 1: Выбор тикера
```mermaid
graph TD
    A[Пользователь вводит тикер] --> B[TickerSearch.jsx]
    B --> C[/api/polygon/ticker/{ticker}]
    C --> D[Получение цены актива]
    D --> E[/api/polygon/ticker/{ticker}/expirations]
    E --> F[Загрузка дат экспирации + кэширование]
```

### Шаг 2: Выбор даты экспирации
```mermaid
graph TD
    G[Пользователь выбирает дату] --> H[OptionsBoard.jsx]
    H --> I[/api/polygon/ticker/{ticker}/options?expiration_date={date}]
    I --> J[Загрузка опционной цепочки]
    J --> K[Заполнение доски опционов]
```

### Шаг 3: Добавление опциона в калькулятор
```mermaid
graph TD
    L[Пользователь добавляет опцион] --> M[OptionsCalculatorBasic.jsx]
    M --> N[Автоподбор страйка из доступных]
    N --> O[/api/polygon/ticker/{ticker}/option-details]
    O --> P[Загрузка премии, bid/ask, греков]
    P --> Q[Обновление состояния калькулятора]
```

---

## 📊 Данные, получаемые от Polygon/Massiv

### Базовые данные актива:
- **Цена:** last, bid, ask, change, change_percent
- **Таймстамп:** время последнего обновления

### Опционные данные:
- **Цены:** premium, bid, ask, last
- **Объемы:** volume (торги за день), open_interest (открытые позиции)
- **Греки:** delta, gamma, theta, vega
- **Волатильность:** implied_volatility (IV)

### Даты экспирации:
- **Формат:** YYYY-MM-DD
- **Фильтр:** только будущие даты
- **Группировка:** по месяцам для UI

### Исторические данные:
- **OHLC:** open, high, low, close
- **Таймфреймы:** от 1 минута до 1 дня
- **Периоды:** от 1 дня до 1 года

---

## 🗂️ Компоненты, использующие API данные

### `OptionsCalculatorBasic.jsx` - Основной калькулятор
- ✅ Получение цены актива
- ✅ Загрузка дат экспирации
- ✅ Детали опционов при добавлении
- ✅ Кэширование дат экспирации
- ✅ Сохранение состояния в localStorage

### `OptionsBoard.jsx` - Доска опционов
- ✅ Полная опционная цепочка по дате
- ✅ Фильтрация по volume, OI, moneyness
- ✅ Пресеты фильтров (trading, hedging, etc.)

### `StrikeScale.jsx` - Шкала страйков
- ✅ Магнитное прилипание к доступным страйкам
- ✅ Визуализация объемов и OI на шкале

### `TickerSearch.jsx` - Поиск тикера
- ✅ Цена актива при выборе
- ✅ Название компании (прямой запрос к Polygon)
- ✅ История поиска в localStorage

### `AdvancedChart.jsx` - График актива
- ✅ Исторические бары для свечного графика
- ✅ Разные таймфреймы и периоды

---

## ⚙️ Настройки и конфигурация

### Кэширование:
- **Даты экспирации:** TTL 5-1440 минут (настраивается в CalculatorSettings)
- **Хранилище:** localStorage с префиксом `expirations_${ticker}`
- **CacheManager:** утилита для TTL-кэширования

### Таймауты:
- **Основные запросы:** 10 секунд
- **Информация о компании:** 3 секунды
- **Fallback:** демо-данные при ошибках

### Лимиты:
- **Опционы:** до 1000 контрактов на страницу
- **Пагинация:** до 5 страниц (5000 контрактов максимум)
- **История тикеров:** 10 последних в localStorage

---

## 🔧 Техническая реализация

### Backend (Python FastAPI):
```python
# routers/polygon.py
@router.get("/ticker/{ticker}")
async def get_ticker_price(ticker: str):
    price_data = polygon_client.get_stock_price(ticker.upper())
    return {"status": "success", "price": price_data.get("price"), ...}

@router.get("/ticker/{ticker}/options")
async def get_ticker_options(ticker: str, expiration_date: str = None):
    options = options_service.get_options_chain(ticker.upper(), expiration_date)
    return {"status": "success", "options": options}
```

### Frontend (React):
```javascript
// Загрузка с обработкой ошибок
const loadExpirationDates = async (ticker) => {
  const cacheKey = `expirations_${ticker}`;
  const cached = cacheManager.get(cacheKey, cacheTTLMinutes);
  
  if (cached) {
    setExpirationDates(cached);
    return;
  }
  
  const response = await fetch(`/api/polygon/ticker/${ticker}/expirations`);
  const data = await response.json();
  cacheManager.set(cacheKey, data.dates);
};
```

---

## 🚨 Выявленные проблемы и рекомендации

### ⚠️ Проблема 1: Прямой запрос к Polygon API
**Файл:** `TickerSearch.jsx:133`  
**Проблема:** Эндпоинт `/api/polygon/ticker/{ticker}/details` не реализован на бэкенде  
**Риск:** Прямое использование API ключа на фронтенде  
**Рекомендация:** Реализовать эндпоинт на бэкенде

### ⚠️ Проблема 2: Отсутствие обработки rate limits
**Проблема:** Нет защиты от превышения лимитов Polygon API  
**Риск:** Блокировка API при интенсивном использовании  
**Рекомендация:** Добавить retry logic и rate limiting

### ⚠️ Проблема 3: Синхронная загрузка данных
**Файл:** `OptionsCalculatorBasic.jsx:312-338`  
**Проблема:** Последовательная загрузка (цена → даты → опционы)  
**Риск:** Длительное время загрузки  
**Рекомендация:** Параллельная загрузка где возможно

---

## ✅ Соответствие требованиям

- [x] **Последовательность загрузки:** цена → даты → опционы → детали
- [x] **Реальные данные:** Все эндпоинты возвращают актуальные рыночные данные
- [x] **Кэширование:** Даты экспирации кэшируются с TTL
- [x] **Обработка ошибок:** Fallback на демо-данные при сбоях API
- [x] **UI/UX:** Плавная загрузка с индикаторами прогресса
- [x] **Производительность:** Лимиты на объем данных, фильтрация на клиенте

---

## 📋 Заключение

API интеграция Polygon/Massiv в калькуляторе опционов **полностью функциональна** и обеспечивает получение всех необходимых рыночных данных. Архитектура хорошо структурирована с разделением ответственности между сервисами, кэшированием и graceful degradation при ошибках.

**Рекомендуется:** Реализовать недостающий эндпоинт для информации о компании и добавить rate limiting для предотвращения блокировок API.
