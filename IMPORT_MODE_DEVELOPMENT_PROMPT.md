# Промпт для разработки калькулятора опционов с интеграцией Chrome Extension

## Контекст проекта

Разрабатывается **калькулятор опционов на фьючерсы** для веб-приложения. Калькулятор работает в связке с Chrome-расширением **TradingView Parser v9**, которое парсит опционы с TradingView и передает их в калькулятор через localStorage.

**Ключевой принцип:** Калькулятор НЕ имеет собственных источников данных. Он только отображает и рассчитывает метрики для опционов, переданных расширением.

---

## Интеграция с расширением TradingView Parser v9

Расширение `twparser_v9` добавляет кнопку **"📱 Открыть калькулятор"** на страницы опционов TradingView. При клике открывается калькулятор с параметрами в URL.

### Как работает расширение:

1. Пользователь открывает страницу опционов на TradingView
2. Расширение добавляет кнопку "📱 Открыть калькулятор"
3. При клике расширение:
   - Парсит данные опционов со страницы
   - Сохраняет их в `localStorage.calculatorState`
   - Открывает калькулятор с параметрами в URL: `?contract=КОД&price=ЦЕНА`
4. Калькулятор читает параметры из URL и localStorage
5. При изменении данных на TradingView расширение обновляет localStorage
6. Калькулятор автоматически обновляется через `storage` event

---

## "Волшебная кнопка" — обновление данных с фильтрами

### Как это работает

1. Пользователь нажимает **"Волшебная кнопка"** в калькуляторе
2. Калькулятор записывает команду с фильтрами в `localStorage` страницы TradingView
3. Расширение читает команду, собирает данные с указанными фильтрами
4. Расширение записывает результат обратно в `localStorage`
5. Калькулятор отслеживает прогресс и получает обновленные данные

### Формат команды (калькулятор → расширение)

**Ключ:** `tvc_refresh_command`

```javascript
localStorage.setItem('tvc_refresh_command', JSON.stringify({
  // Диапазон экспираций (дни от сегодня)
  daysFrom: 1,      // минимум дней до экспирации
  daysTo: 60,       // максимум дней до экспирации
  
  // Диапазон страйков (% от текущей цены)
  strikeFrom: -20,  // -20% = ниже цены на 20%
  strikeTo: 20,     // +20% = выше цены на 20%
  
  // Служебные поля
  timestamp: Date.now(),
  processed: false   // ВАЖНО: должно быть false
}));
```

### Примеры сценариев фильтрации

| Сценарий | daysFrom | daysTo | strikeFrom | strikeTo |
|----------|----------|--------|------------|----------|
| Ближние экспирации, широкий диапазон | 1 | 30 | -20 | 20 |
| Дальние экспирации | 60 | 180 | -15 | 15 |
| Только OTM колы (+5% и выше) | 1 | 60 | 5 | 30 |
| Только ITM путы (-30% и ниже) | 1 | 60 | -50 | -5 |
| ATM опционы (±5%) | 1 | 30 | -5 | 5 |

### Формат результата (расширение → калькулятор)

**Ключ:** `tvc_refresh_result`

```javascript
const result = JSON.parse(localStorage.getItem('tvc_refresh_result'));

// Структура:
{
  status: 'collecting' | 'complete' | 'error',
  progress: 0-100,              // процент выполнения
  message: 'Текст статуса',     // описание текущего действия
  timestamp: 1234567890,
  
  // При status === 'complete':
  data: {
    ticker: 'ESH2026',
    expirations: 5,             // количество экспираций
    options: 150,               // количество опционов
    duration: 45                // время сбора в секундах
  }
}
```

### Полные данные опционов

**Ключ:** `tvc_full_chain` (в chrome.storage)

```javascript
{
  ticker: 'ESH2026',
  collectedAt: '2026-01-19T12:00:00.000Z',
  underlyingPrice: 6910.75,
  filters: { 
    daysFrom: 1, 
    daysTo: 60, 
    minStrike: 5528.60,  // рассчитано от underlyingPrice
    maxStrike: 8292.90 
  },
  expirations: [
    {
      date: 'Jan 24, 2026',
      dateCode: '20260124',
      options: [
        { 
          strike: 6900, 
          callBid: 45.50, 
          callAsk: 46.00, 
          callIv: 0.18, 
          callDelta: 0.52,
          putBid: 34.75, 
          putAsk: 35.25, 
          putIv: 0.17, 
          putDelta: -0.48,
          // ... другие греки
        }
      ]
    }
  ]
}
```

### Пример реализации в калькуляторе

```javascript
// При нажатии "Волшебная кнопка"
function onMagicButtonClick(daysFrom, daysTo, strikeFrom, strikeTo) {
  // Записываем команду
  localStorage.setItem('tvc_refresh_command', JSON.stringify({
    daysFrom,
    daysTo,
    strikeFrom,
    strikeTo,
    timestamp: Date.now(),
    processed: false
  }));
  
  // Отслеживаем результат
  const interval = setInterval(() => {
    const result = JSON.parse(localStorage.getItem('tvc_refresh_result') || '{}');
    
    if (result.status === 'collecting') {
      updateProgressBar(result.progress, result.message);
    }
    
    if (result.status === 'complete') {
      clearInterval(interval);
      showSuccess(`Собрано ${result.data.options} опционов за ${result.data.duration}с`);
      // Данные автоматически загрузятся через storage event
    }
    
    if (result.status === 'error') {
      clearInterval(interval);
      showError(result.message);
    }
  }, 500);
}
```

---

## Архитектура системы

```
┌─────────────────────┐         ┌──────────────────┐         ┌─────────────────────┐
│  TradingView Page   │         │ Chrome Extension │         │   Calculator App    │
│                     │         │                  │         │                     │
│  User clicks +C/+P  │────────▶│  Parse option    │────────▶│  Read localStorage  │
│                     │         │  Save to         │         │  Display options    │
│                     │         │  localStorage    │         │  Calculate metrics  │
│                     │         │  Open calculator │         │  Subscribe to       │
│                     │         │                  │         │  storage events     │
└─────────────────────┘         └──────────────────┘         └─────────────────────┘
                                                                       │
                                                                       │ storage event
                                                                       ▼
                                                              ┌─────────────────────┐
                                                              │  Auto-update table  │
                                                              │  without reload     │
                                                              └─────────────────────┘
```

---

## Техническое задание

### 1. Формат данных в localStorage

Расширение записывает данные в `localStorage.calculatorState`:

```javascript
{
  "selectedTicker": "CME_MINI:ESH2026",      // TradingView символ контракта
  "selectedExpirationDate": "2026-01-16",    // ISO формат даты экспирации
  "underlyingPrice": 5985.25,                // Цена базового актива
  "options": [                                // Массив опционов
    {
      "id": "1768468327884",                 // Уникальный ID (timestamp)
      "action": "Buy",                       // "Buy" или "Sell"
      "type": "CALL",                        // "CALL" или "PUT"
      "strike": 6985,                        // Страйк цена
      "date": "2026-01-16",                  // Дата экспирации (ISO)
      "quantity": 1,                         // Количество контрактов
      "premium": 16.125,                     // Премия опциона
      "bid": 16,                             // Bid цена
      "ask": 16.25,                          // Ask цена
      "volume": 0,                           // Объем торгов
      "oi": 0,                               // Open Interest
      "visible": true,                       // Видимость на графике
      "ticker": "ESH6",                      // Underlying тикер
      "lastUpdated": "2026-01-15T09:12:07.884Z", // Timestamp обновления
      "delta": 0.5,                          // Греки
      "gamma": 0.01,
      "theta": -0.05,
      "vega": 0.1,
      "impliedVolatility": 0.25
    }
  ]
}
```

### 2. URL для открытия калькулятора

```
http://localhost:3000/tools/universal-calculator?contract=ESH26&price=6910.75
```

**Параметры URL:**

| Параметр | Тип | Описание | Пример |
|----------|-----|----------|--------|
| `contract` | string | Код контракта/тикер | `TSLA`, `ESH26`, `GCG26` |
| `price` | number | Текущая цена underlying | `437.5`, `6910.75` |

**Примеры:**
- Акции: `?contract=TSLA&price=437.5`
- Фьючерсы: `?contract=ESH26&price=6910.75` (год сокращается: ESH2026 → ESH26)

---

## Требования к реализации

### Хук `useExtensionData`

**Файл:** `/frontend/src/hooks/useExtensionData.js`

**Задачи:**
1. Парсить URL параметры `?contract=` и `?price=` для получения кода контракта и цены
2. Читать данные из `localStorage.calculatorState` при инициализации
3. Подписаться на `storage` event для автоматического обновления при изменениях
4. Возвращать:
   - `contractCode` — код контракта из URL (`?contract=`)
   - `urlPrice` — цена из URL (`?price=`), приоритет над localStorage
   - `underlyingPrice` — цена базового актива (из URL или localStorage)
   - `options` — массив опционов
   - `ticker` — тикер контракта
   - `expirationDate` — дата экспирации
   - `refreshFromStorage()` — функция ручного обновления

**Критично:**
- `storage` event срабатывает только при изменении localStorage из **другого контекста** (расширение)
- Это именно то, что нужно — расширение пишет, калькулятор автоматически обновляется

**Пример кода:**
```javascript
// Парсим URL параметры
const urlParams = new URLSearchParams(window.location.search);
const contractCode = urlParams.get('contract');
const urlPrice = urlParams.get('price');

// Читаем localStorage
const saved = localStorage.getItem('calculatorState');
const state = saved ? JSON.parse(saved) : {};

// Цена: приоритет URL > localStorage
const underlyingPrice = urlPrice 
  ? parseFloat(urlPrice) 
  : (state.underlyingPrice || 0);

// Подписка на storage event
useEffect(() => {
  const handleStorageChange = (event) => {
    if (event.key !== 'calculatorState') return;
    const newState = JSON.parse(event.newValue);
    updateStateFromStorage(newState);
  };
  
  window.addEventListener('storage', handleStorageChange);
  return () => window.removeEventListener('storage', handleStorageChange);
}, [updateStateFromStorage]);
```

---

### Главный компонент калькулятора

**Файл:** `/frontend/src/pages/OptionsCalculatorBasic.jsx`

**Задачи:**

#### 1. Инициализация из localStorage
```javascript
const {
  contractCode,
  underlyingPrice,
  options: extensionOptions,
  ticker,
  expirationDate
} = useExtensionData();

useEffect(() => {
  if (isInitialized) return;
  
  // Устанавливаем данные из расширения
  if (ticker) setSelectedTicker(ticker);
  if (underlyingPrice > 0) setCurrentPrice(underlyingPrice);
  if (expirationDate) setSelectedExpirationDate(expirationDate);
  if (extensionOptions && extensionOptions.length > 0) setOptions(extensionOptions);
  
  setIsInitialized(true);
}, [isInitialized, ticker, underlyingPrice, expirationDate, extensionOptions]);
```

#### 2. Синхронизация при обновлении из расширения
```javascript
useEffect(() => {
  if (!isInitialized) return;
  
  // Обновляем опционы при изменении (storage event)
  if (extensionOptions && extensionOptions.length > 0) {
    setOptions(extensionOptions);
  }
  
  // Обновляем цену
  if (underlyingPrice > 0) {
    setCurrentPrice(underlyingPrice);
  }
}, [isInitialized, extensionOptions, underlyingPrice]);
```


#### 4. UI: Хедер с контрактом и ценой
```javascript
{contractCode && (
  <div className="mb-6">
    <div className="inline-flex items-center gap-4 p-3 border border-purple-500 rounded-lg bg-purple-50">
      <div className="flex items-center gap-2">
        <span className="text-sm font-medium text-purple-700">📡 TradingView</span>
      </div>
      <div className="flex items-center gap-2">
        <span className="text-sm text-muted-foreground">Contract:</span>
        <span className="text-lg font-bold">{contractCode}</span>
      </div>
      <div className="flex items-center gap-2">
        <span className="text-sm text-muted-foreground">Price:</span>
        <span className="text-lg font-bold">${underlyingPrice.toFixed(2)}</span>
      </div>
    </div>
  </div>
)}
```

#### 5. Селектор тикера и календарь
Все данные (тикер, дата экспирации) приходят от расширения через URL и localStorage. Команда калькулятора самостоятельно определит необходимость использования компонентов `NewTikerFinder` и `ExpirationCalendar`.

---

### Таблица опционов

**Файлы:** 
- `/frontend/src/components/CalculatorV2/OptionsTable.jsx`
- `/frontend/src/components/CalculatorV2/OptionsTable/index.jsx`

**Задачи:**

Таблица опционов отображает данные, полученные от расширения. Команда калькулятора самостоятельно определит, какие элементы UI нужно оставить или убрать в соответствии с логикой работы только с расширением.

---

### Внешние источники данных НЕ используются

Калькулятор НЕ делает запросы к API и НЕ использует WebSocket.

#### 1. API запросы НЕ нужны

**В `OptionsCalculatorBasic.jsx`:**

```javascript
// Функции loadStrikesForDate, loadOptionDetails, loadExpirationDates
// Команда калькулятора самостоятельно определит необходимость их использования:

const loadStrikesForDate = async (ticker, date) => {
  console.log('📡 Загрузка страйков отключена — данные от расширения');
  return [];
};

const loadOptionDetails = async (optionId, ticker, date, strike, optionType) => {
  console.log('📡 Загрузка деталей отключена — данные от расширения');
  return null;
};

const loadExpirationDates = async (ticker, instrumentType = 'stock') => {
  console.log('📡 Загрузка дат отключена — данные от расширения');
  return;
};
```

#### 2. WebSocket НЕ используется

**Файл:** `/frontend/src/hooks/useMarketStatus.js`

```javascript
const useMarketStatus = () => {
  useEffect(() => {
    // WebSocket подключения НЕ создаются
    // Калькулятор работает только с данными от расширения
    setLoading(false);
    return;
  }, []);
};
```

#### 3. Sequential Data Loader НЕ используется

**Файл:** `/frontend/src/hooks/useSequentialDataLoader.js`

```javascript
export const useSequentialDataLoader = () => {
  useEffect(() => {
    if (loadedRef.current) return;
    loadedRef.current = true;
    
    // Загрузка пропускается — данные от расширения
    console.log('📡 [Sequential] Загрузка отключена');
    globalCache.isReady = true;
    setLoadingStep('done');
    setIsLoading(false);
  }, []);
};
```

---

## Логика работы

### Сценарий: Пользователь работает с калькулятором

```
1. Расширение:
   - Пользователь на TradingView выбирает опционы
   - Расширение парсит данные
   - Сохраняет в localStorage.calculatorState
   - Открывает калькулятор: http://localhost:3000/tools/universal-calculator?contract=ESH26&price=6910.75

2. Калькулятор инициализируется:
   - useExtensionData читает данные из localStorage
   - Устанавливает: тикер, цену, дату экспирации, опционы
   - API запросы и WebSocket НЕ используются

3. UI отображается:
   - Хедер: "📡 TradingView | ESH26 | $5985.25"
   - Таблица опционов — отображается
   - Кнопки управления — НЕ отображаются (только Reset и DevMode)
   - График P&L — рассчитывается и отображается

4. Пользователь добавляет новый опцион на TradingView:
   - Расширение обновляет localStorage.calculatorState
   - storage event срабатывает в калькуляторе
   - useExtensionData обновляет options
   - useEffect синхронизирует setOptions(extensionOptions)
   - Таблица автоматически обновляется БЕЗ перезагрузки страницы

5. Пользователь изменяет цену базового актива:
   - Расширение обновляет underlyingPrice в localStorage
   - storage event срабатывает
   - Хедер автоматически обновляет отображаемую цену
   - График P&L пересчитывается
```

---

## API для расширения

### Метод 1: Первичная запись данных

```javascript
// В Chrome Extension (content script)
const calculatorState = {
  selectedTicker: "CME_MINI:ESH2026",
  selectedExpirationDate: "2026-01-16",
  underlyingPrice: 5985.25,
  options: [
    {
      id: Date.now().toString(),
      action: "Buy",
      type: "CALL",
      strike: 6985,
      date: "2026-01-16",
      quantity: 1,
      premium: 16.125,
      bid: 16,
      ask: 16.25,
      volume: 0,
      oi: 0,
      visible: true,
      ticker: "ESH6",
      lastUpdated: new Date().toISOString(),
      delta: 0.5,
      gamma: 0.01,
      theta: -0.05,
      vega: 0.1,
      impliedVolatility: 0.25
    }
  ]
};

// ВАЖНО: Сначала запись, ПОТОМ открытие калькулятора
localStorage.setItem('calculatorState', JSON.stringify(calculatorState));

// Открыть калькулятор
const url = `http://localhost:3000/tools/universal-calculator?contract=ESH26&price=6910.75`;
chrome.tabs.create({ url });
```

### Метод 2: Добавление опциона в уже открытый калькулятор

```javascript
// Читаем текущее состояние
const saved = localStorage.getItem('calculatorState');
const state = saved ? JSON.parse(saved) : { options: [] };

// Добавляем новый опцион
const newOption = {
  id: Date.now().toString(),
  action: "Sell",
  type: "PUT",
  strike: 5900,
  date: "2026-01-16",
  quantity: 1,
  premium: 12.5,
  bid: 12.25,
  ask: 12.75,
  volume: 0,
  oi: 0,
  visible: true,
  ticker: "ESH6",
  lastUpdated: new Date().toISOString(),
  delta: -0.3,
  gamma: 0.01,
  theta: -0.04,
  vega: 0.08,
  impliedVolatility: 0.22
};

state.options.push(newOption);

// Сохраняем обратно
localStorage.setItem('calculatorState', JSON.stringify(state));

// storage event автоматически обновит таблицу в калькуляторе
```

### Метод 3: Обновление цены базового актива

```javascript
const saved = localStorage.getItem('calculatorState');
const state = JSON.parse(saved);

// Обновляем цену
state.underlyingPrice = 6000.50;

localStorage.setItem('calculatorState', JSON.stringify(state));

// Хедер автоматически обновится
```

---

## Чеклист разработки

### Этап 1: Хук useExtensionData
- [ ] Создать `/frontend/src/hooks/useExtensionData.js`
- [ ] Реализовать парсинг `?contract=` и `?price=` из URL
- [ ] Реализовать чтение `calculatorState` из localStorage
- [ ] Реализовать подписку на `storage` event
- [ ] Реализовать логику приоритета: цена из URL > цена из localStorage
- [ ] Экспортировать все необходимые значения (contractCode, urlPrice, underlyingPrice, options, ticker, expirationDate)

### Этап 2: Интеграция в калькулятор
- [ ] Подключить `useExtensionData` в `OptionsCalculatorBasic.jsx`
- [ ] Реализовать инициализацию из localStorage при первой загрузке
- [ ] Реализовать синхронизацию при обновлении через storage event
- [ ] Убедиться, что цена из URL используется при инициализации

### Этап 3: UI изменения
- [ ] Добавить хедер с контрактом и ценой (отображать данные из URL/localStorage)
- [ ] Адаптировать UI под работу только с расширением (команда определит необходимые изменения)

### Этап 4: OptionsTable
- [ ] Адаптировать таблицу опционов под работу с данными от расширения
- [ ] Убедиться, что таблица корректно обновляется при изменении данных

### Этап 5: "Волшебная кнопка"
- [ ] Реализовать функцию записи команды в `tvc_refresh_command`
- [ ] Реализовать отслеживание прогресса через `tvc_refresh_result`
- [ ] Реализовать UI для отображения прогресса (progress bar, статус)
- [ ] Реализовать обработку статусов: collecting, complete, error
- [ ] Реализовать автоматическую загрузку данных после завершения сбора

### Этап 6: Отключение внешних источников
- [ ] Отключить API запросы (loadStrikesForDate, loadOptionDetails, loadExpirationDates)
- [ ] Отключить WebSocket в useMarketStatus
- [ ] Отключить Sequential Data Loader

### Этап 7: Тестирование базовой функциональности
- [ ] Тест: открытие с `?contract=ESH26&price=6910.75`
- [ ] Тест: хедер отображает контракт и цену из URL
- [ ] Тест: приоритет цены из URL над localStorage
- [ ] Тест: опционы из localStorage отображаются в таблице
- [ ] Тест: storage event обновляет таблицу без перезагрузки
- [ ] Тест: обновление цены в localStorage обновляет хедер

### Этап 8: Тестирование "Волшебной кнопки"
- [ ] Тест: команда `tvc_refresh_command` корректно записывается в localStorage
- [ ] Тест: прогресс отображается при status='collecting'
- [ ] Тест: успешное завершение при status='complete'
- [ ] Тест: обработка ошибок при status='error'
- [ ] Тест: данные автоматически загружаются после завершения сбора
- [ ] Тест: различные сценарии фильтрации (ближние/дальние экспирации, OTM/ITM)

---

## Важные замечания

### 1. storage event — ключевая особенность
`storage` event срабатывает **только** при изменении localStorage из **другого контекста** (другая вкладка, расширение). Это идеально подходит для нашей задачи — расширение пишет, калькулятор автоматически читает.

### 2. Приоритет цены из URL
**ВАЖНО:** Если в URL есть параметр `?price=`, он имеет **приоритет** над ценой из localStorage. Это позволяет расширению передавать актуальную цену при открытии калькулятора.

```javascript
// Правильная логика:
const underlyingPrice = urlPrice 
  ? parseFloat(urlPrice)  // Приоритет URL
  : (state.underlyingPrice || 0);  // Fallback на localStorage
```

---


## Ожидаемый результат

После реализации:

1. **Калькулятор открывается с `?contract=ESH26&price=6910.75`**
   - Хедер: "📡 TradingView | ESH26 | $6910.75"
   - Цена из URL имеет приоритет над localStorage
   - Опционы из localStorage отображаются в таблице

2. **Расширение добавляет новый опцион**
   - localStorage обновляется
   - storage event срабатывает
   - Таблица автоматически обновляется БЕЗ перезагрузки

3. **Расширение обновляет цену**
   - localStorage обновляется
   - Хедер автоматически обновляет цену
   - График P&L пересчитывается

4. **Никаких внешних запросов**
   - API запросы не выполняются
   - WebSocket не используется
   - Sequential Loader отключен

---

## Структура файлов

```
frontend/
├── src/
│   ├── hooks/
│   │   ├── useExtensionData.js       # Новый хук для работы с данными расширения
│   │   ├── useMarketStatus.js        # Модифицировать
│   │   └── useSequentialDataLoader.js # Модифицировать
│   ├── pages/
│   │   └── OptionsCalculatorBasic.jsx # Модифицировать
│   └── components/
│       └── CalculatorV2/
│           ├── OptionsTable.jsx       # Модифицировать
│           └── OptionsTable/
│               └── index.jsx          # Модифицировать
```

---

**Дата создания:** 19 января 2026  
**Версия:** 1.0  
**Тип:** Промпт для разработки с нуля
