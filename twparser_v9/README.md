# TradingView → Optioner Bridge v9

Chrome-расширение для сбора данных опционов с TradingView и передачи в калькулятор.

## Возможности

- 📊 **Сбор всех данных по контракту** — автоматический обход всех экспираций
- 🔄 **Синхронизация с калькулятором** — данные автоматически появляются в localStorage
- 🛡️ **Защита от бана** — рандомные задержки, лимиты, cooldown
- 📱 **Команды от калькулятора** — запуск сбора по кнопке в калькуляторе

## Установка

1. Открой `chrome://extensions`
2. Включи "Режим разработчика"
3. Нажми "Загрузить распакованное"
4. Выбери папку `twparser_v9`

## Использование

### Способ 1: Через popup расширения

1. Открой страницу опционов на TradingView
2. Нажми на иконку расширения
3. Нажми "📊 Собрать все данные"
4. Дождись завершения

### Способ 2: Через калькулятор (v9)

Калькулятор может запустить сбор данных, записав команду в localStorage:

```javascript
localStorage.setItem('tvc_command', JSON.stringify({
  action: 'collectFullChain',
  ticker: 'ESM2026',
  maxExpirations: 10,
  timestamp: Date.now()
}));
```

## Формат данных

### tvc_full_chain

После сбора данные доступны в `localStorage.tvc_full_chain`:

```javascript
{
  collectedAt: "2026-01-18T20:45:00.000Z",
  ticker: "ESM2026",
  expirations: [
    {
      date: "Mar 20",
      dateCode: "20260320",
      options: [
        {
          type: "CALL",
          strike: 6950,
          bid: 224.5,
          ask: 226.0,
          iv: 14.1,
          delta: 0.61,
          gamma: 0.0009,
          theta: -1.29,
          vega: 11.24,
          rho: 7.08
        },
        // ...
      ]
    },
    // ...
  ]
}
```

## Конфигурация

В `background.js` можно настроить:

```javascript
const COLLECTOR_CONFIG = {
  MIN_DELAY: 2000,        // Мин. задержка между переходами
  MAX_DELAY: 5000,        // Макс. задержка
  MAX_EXPIRATIONS: 5,     // Лимит экспираций
  COOLDOWN: 30000,        // Cooldown между сборами
};
```

## Поддерживаемые сайты

- `https://*.tradingview.com/options/*` — парсинг опционов
- `http://localhost:3000/*` — локальный калькулятор
- `https://futures.optioner.online/*` — продакшн калькулятор

## Документация

- [PLAN.md](./PLAN.md) — архитектура и API для интеграции с калькулятором
